---
title: "apprafter argocd-password"
description: "Print the Argo CD admin password (decrypted), fetching it from the cluster on first use."
audience: reference
status: stable
---

<!-- generated by docsgen; do not edit -->

# `apprafter argocd-password`

Print the Argo CD admin password (decrypted), fetching it from the cluster on first use. The plaintext is cached age-encrypted in state for subsequent O(1) reads

```text
Usage: apprafter argocd-password [OPTIONS]
```

| Flag | Value | Default | Required | Description |
| --- | --- | --- | --- | --- |
| `--refresh` | flag | — | no | Force a re-fetch of the secret even if a cached password is already in state |

Examples:

```sh
apprafter argocd-password
apprafter argocd-password --refresh  # after the admin secret is rotated
```
