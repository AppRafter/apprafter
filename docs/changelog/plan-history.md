# AppRafter — Plan change history

> Plan-level change ledger: one row per (sub-)subphase or patch release. Extracted from
> `plan.md` (formerly its in-file "Plan change history" table) so the roadmap document stays
> forward-looking. For per-release Keep-a-Changelog detail see [`UNRELEASED.md`](./UNRELEASED.md);
> the actionable roadmap and phase ledger live in `plan.md`. Rows preserve source order
> (broadly oldest-first).

| Date | Change | Release / Stream |
| --- | --- | --- |
| 2026-05-06 | First version of the plan from spec.md rev.4 | initial |
| 2026-05-06 | Closed subphase 0.1 — monorepo structure and README | initial |
| 2026-05-06 | Closed subphase 0.2 — LICENSE / NOTICE / SPDX template | initial |
| 2026-05-06 | Closed subphase 0.3 — ADR template + 12 ADRs; 0007 reassigned to SealedSecrets/OpenBao | initial |
| 2026-05-06 | Closed subphase 0.4 — CUE module `apprafter.io` + skeleton of 9 CRDs + lint script | initial |
| 2026-05-06 | Closed subphase 0.5 — CI workflows, GitHub meta, lefthook, SPDX/commit-msg scripts | initial |
| 2026-05-06 | Closed subphase 0.6 — flake.nix, devcontainer, mise.toml, Justfile, contributing/setup | initial |
| 2026-05-06 | Closed subphase 0.7 — mkdocs skeleton, governance files, docs-serve/-build targets | initial |
| 2026-05-06 | Closed subphase 0.8 — spec.md M0 fully closed, UNRELEASED changelog established | initial |
| 2026-05-06 | Phase 1 started — version `0.1.x` (minor=phase, patch=subphase) | initial |
| 2026-05-06 | Closed subphase 1.1 — Cargo workspace `cli/` + skeleton subcommands; v0.1.1 | initial |
| 2026-05-06 | 1.2 (server-CRUD branch) — `HetznerCloudProvider` (apply/destroy/idempotent CX22); v0.1.2 | initial |
| 2026-05-06 | 1.2 (SSH-keys branch) — `HetznerCloudProvider.ssh_keys` + APPRAFTER_SSH_PUBLIC_KEY; v0.1.3 | initial |
| 2026-05-06 | 1.2 (Network + Firewall branch) — default 10.0.0.0/16 net + SSH/HTTPS firewall, server attached; v0.1.4 | initial |
| 2026-05-06 | 1.2 (CUE Infrastructure parsing branch) — APPRAFTER_MANIFEST overlays defaults; v0.1.5 | initial |
| 2026-05-08 | Phase 1 patch — Hetzner `cx22` retired upstream; default flipped to `cpx22` + pre-flight validate_server_type lookup; v0.1.42 | initial |
| 2026-05-09 | Phase 1 patch — cloud-init drops ufw (silent initcaps failure on noble); fail2ban + Hetzner Cloud Firewall became the only layers; v0.1.43 | initial |
| 2026-05-09 | Phase 1 patch — tracing logs → stderr (fix for stdout pollution of the `kubeconfig`/`argocd-password` pipes); ∞.7 bug #4 ✅; v0.1.44 | initial |
| 2026-05-09 | Phase 1 patch — k3s installer passes `--flannel-backend=none --disable-network-policy` (fix for cilium_vxlan VXLAN-port collision, unblocks cluster-bootstrap); ∞.7 bug #5 ✅; v0.1.45 | initial |
| 2026-05-10 | Phase 1 patch — per-cluster `.apprafter/known_hosts` for SSH (fix for host-key collision on recycled Hetzner IPs); ∞.7 bug #1 ✅; v0.1.46 | initial |
| 2026-05-10 | Phase 1 patch — `destroy()` poll-wait after delete_server (fix for async-cleanup race with 409 on delete_network); ∞.7 bug #2 ✅; v0.1.47 | initial |
| 2026-05-10 | Phase 1 patch — cert-manager `installCRDs` → `crds.enabled` (drop deprecation warning); v0.1.48 | initial |
| 2026-05-10 | Phase 1 patch (docs-only) — operator-guide `recovery.md` for the Hetzner Rescue Mode runbook; ∞.7 bug #3 ✅ closed via the docs route; v0.1.49 | initial |
| 2026-05-10 | Phase 1 patch — retry-on-`resource_in_use` for `delete_firewall`/`delete_network` (second layer of protection against Hetzner async-cleanup lag after v0.1.47); v0.1.50 | initial |
| 2026-05-10 | Phase 1 patch — `default-deny` NP is now Ingress-only with allow for same-ns + kube-system (fix for silent breakage of workloads — DNS+Service routing was blocked since v0.1.0-mvp); ∞.7 bug #6 ✅; v0.1.51 | initial |
| 2026-05-10 | Phase 1 patch — GHCR release workflow + `apprafter-operator/Dockerfile`; unblocks §5 (operator image is now pullable, not "build your own"); v0.1.52 | initial |
| 2026-05-10 | Phase 1 patch — `e2e/mvp.sh` uses `curlimages/curl:latest` instead of the nonexistent `:8`; ∞.7 bug #7 ✅; v0.1.53 | initial |
| 2026-05-10 | Phase 1 patch — closure returns `ureq::Request` (not `Result`), fixes CI clippy 1.95 `result_large_err`; v0.1.54 | initial |
| 2026-05-10 | Phase 1 patch — release-operator workflow lowercases `repository_owner` for the ghcr.io tag (Docker registry requires lowercase); v0.1.55 | initial |
| 2026-05-10 | Phase 1 patch — simplified `operator-*/Dockerfile` (drop fragile dep-prebuild trick) + `operator/.dockerignore`; cargo-chef as a follow-up; v0.1.56 | initial |
| 2026-05-10 | Phase 1 patch — bump rust pin 1.83 → 1.85 (transitive dep `hashbrown-0.17.1` requires cargo `edition2024` feature, stabilized in 1.85); v0.1.57 | initial |
| 2026-05-10 | Phase 1 patch — Dockerfile pin moved from a fixed version to `rust:stable-alpine` (transitive deps bump the MSRV unpredictably, reactive patches are expensive); MSRV → 1.88; v0.1.58 | initial |
| 2026-05-10 | Phase 1 patch — `rust:stable-alpine` does not exist on Docker Hub, replaced with the canonical `rust:alpine`; v0.1.59 | initial |
| 2026-05-11 | Phase 1 patch — `helm template` whitespace-trim ate the newline between the SPDX comment and `apiVersion:` in the operator chart's `serviceaccount.yaml`/`rbac.yaml`; v0.1.60 | initial |
| 2026-05-11 | Phase 1 patch — operator pod CrashLoopBackOff: `install_rustls_crypto_provider()` before `Client::try_default` (rustls 0.23+ removed the auto-default); + 2 regression-guard unit tests; v0.1.61 | initial |
| 2026-05-11 | Phase 1 patch — CRD declares a `.status` schema (previously there was only `subresources.status: {}` without `properties.status`, so the operator PATCH failed with `.status: field not declared in schema`); +1 regression-guard test; v0.1.62 | initial |
| 2026-05-11 | Phase 1 patch — hot-reconcile loop caused by `lastTransitionTime = now()` on every reconcile; now preserved when status is unchanged (k8s `meta/v1.Condition` semantics); +2 regression-guard tests; v0.1.63 | initial |
| 2026-05-11 | Phase 1 Level B integration — default-on operator + webhook (§1.14); v0.1.64 | initial |
| 2026-05-11 | Phase 1 Level C GitOps — env-driven Argo CD repo credentials (§1.15); v0.1.65 | initial |
| 2026-05-13 | §1.15 walks bug fixes — tier-1 firewall port 6443 + destroy floating-IP unassign order; v0.1.66 | initial |
| 2026-05-13 | §1.15 walks follow-up — real Hetzner unassign-422 message + 423 locked retry; v0.1.67 | initial |
| 2026-05-13 | §1.15 Q3 security fix — repo-creds Secret applied via SSA (no more PAT leak in the last-applied-configuration annotation); v0.1.68 | initial |
| 2026-05-14 | M1.5 Track A.1 — rename `platform-cli` → `apprafter`, add deprecated shim, sweep user-facing docs, retarget tracing filter; v0.1.69 | initial |
| 2026-05-14 | 1.2 AUDIT (Hetzner IPv6) — wire-type `PublicIpv6` + dual-stack k3s `--cluster-cidr`/`--service-cidr` (ADR 0017) + Hetzner Firewall ICMP allow-rule; v0.1.70 | initial |
| 2026-05-14 | 1.4 AUDIT (Cilium dual-stack) — explicit `ipv4.enabled: true` + `ipv6.enabled: true` in Helm values (ADR 0017); `e2e/mvp.sh` Phase 6.4 — pod-level v4+v6 podIPs assertion; v0.1.71 | initial |
| 2026-05-14 | M1.5 Track A.2 — `cli-core::target` module: types (GlobalConfig/TargetConfig/TargetCredentials/Target/TargetStorePaths) + atomic load/save IO + 0600 enforcement + `<redacted>` Debug; +16 unit tests; v0.1.72 | initial |
| 2026-05-14 | M1.5 Track A.3 — `apprafter target add` (+`t` alias) non-interactive: validators, `--force`/`--renew`, first-target auto-active, env-override `APPRAFTER_CONFIG_DIR`; +33 tests; v0.1.73 | initial |
| 2026-05-14 | v0.1.73 hotfix — `validate_hetzner_token_format` now requires exactly 64 ASCII alphanumeric characters (without the `hcloud_` prefix — v0.1.73 wrongly required it, real Hetzner tokens have no prefix); cli-dx-task.md §11 amended; +regression guard for underscore-at-correct-length; v0.1.74 | initial |
| 2026-05-14 | M1.5 Track A.4a — `cli-providers::validators` + `HetznerCloudValidator` (`GET /v1/locations` ping); `target add` now validates the token by default + `--no-ping`/`APPRAFTER_NO_PING` opt-out; +8 tests (3 validator + 5 integration); v0.1.75 | initial |
| 2026-05-14 | M1.5 Track A.4b — interactive wizard via `inquire`: Text/Password/Select prompts per spec §5.1, inline format+ping validation in Password, region picker via `list_regions()`, tilde expansion for the SSH key, default-when-TTY behavior via `IsTerminal` + `should_use_wizard()`; +7 tests; v0.1.76 | initial |
| 2026-05-14 | v0.1.76 wizard polish — workspace `Cargo.toml` version-bump policy (CLAUDE.md addendum, bumped 0.1.2 → 0.1.77); wizard fires on ANY TTY (drops the "all-required" short-circuit) so optional fields get prompted; HCLOUD_TOKEN-from-env notification; SSH-key `Select` from `~/.ssh/*.pub` + algo/comment label; parallel region-latency probe (`<region>-speed.hetzner.com:443`) with sort + ms display; +10 tests; v0.1.77 | initial |
| 2026-05-14 | v0.1.77 wizard polish #2 — `prompt_name` silent on prefill (consistency with the other prompts); `ℹ <Field>: <value> (from <source>)` for all prefilled wizard fields (name/provider/ssh-key/region/tier — token already had it); `run_renew` rejects an identical token with a hint to generate a new one in the Cloud Console; +3 tests; v0.1.78 | initial |
| 2026-05-14 | M1.5 Track A.5 — target CRUD: `target list` (tabled table with a `*` marker) + `target use` (switch active) + `target show` (details; token masked as `set (N chars)`) + `target rename` (atomic with auto-update of the active pointer) + `target remove` (`--yes` opt-in or interactive Confirm; active → reassign alphabetically); `cli_core::target::rename_target` API; +21 tests; v0.1.79 | initial |
| 2026-05-14 | M1.5 Track A.6 — `apprafter whoami` (identity + active target + best-effort verified status; a failed ping does not break the exit) + hidden `apprafter auth login/logout/status` stubs (friendly redirect to `target add` + a link to the Managed roadmap); +15 tests; v0.1.80 | initial |
| 2026-05-14 | M1.5 Track A.7 — `apprafter doctor` (self-diagnostic): target checks (config readable, creds mode 0600, provider known, token format, token API-verified with latency, ssh-key readable) + env checks (kubectl/helm/ssh on PATH, DNS resolves api.hetzner.cloud); PASS/WARN/FAIL trichotomy → exit 1 on FAIL; +17 tests; v0.1.81 | initial |
| 2026-05-14 | M1.5 Track A.8 — wire `apply`/`destroy`/`import` into the credential resolution chain: new `cli_core::credentials` with `resolve_hetzner_token` + `resolve_hetzner_ssh_public_key` (`--flag > env > target store`), `--target <name>` flag on the three operational commands, error messages enumerate all 3 paths; backwards-compat env-only workflows preserved; +17 tests; v0.1.82 | initial |
| 2026-05-14 | v0.1.82 hotfix — `apply`/`import` now also read `provider`/`region`/`cluster_name` from the active target when state.json is empty (A.8 only wired credentials, but operational commands still required `init`); `init` becomes optional after `target add`; new `cli_core::target::{resolve_active_target_name, load_active_target_config}` helpers; +1 regression-guard test; v0.1.83 | initial |
| 2026-05-14 | M1.5 Track A.9 — `apprafter bootstrap-all` orchestrator (Phase 1 apply → Phase 2 kubeconfig SSH poll up to 300s × 10s interval → Phase 3 cluster-bootstrap) under a single `indicatif::MultiProgress` UX; `--dry-run` prints the plan without provider/cluster mutation; `--target <name>` is propagated to all phases; `commands::kubeconfig::fetch_and_cache` extracted from `run` so the Phase 2 retry-loop does not spawn child processes; `commands::kubeconfig` now uses `resolve_hetzner_token` instead of reading the env directly; `--target` flag added to `Commands::Kubeconfig`; +8 tests (4 unit + 4 integration); v0.1.84 | initial |
| 2026-05-14 | M1.5 Track A.9 hotfix UX — after the manual walk of v0.1.84: `MultiProgress` duplicated spinner lines on every helm/kubectl `println` (the Phase 1 + Phase 3 spinner fought with the tracing logs of apply/cluster-bootstrap over the same row); dry-run printed a `<active target>` placeholder instead of the active target's name and did not explain what each phase does. v0.1.85 rebuilt bootstrap_all: Phase 1+3 without a spinner (`→ start / inner output / ✓ end`), Phase 2 keeps the spinner (the retry loop owns all output), `finish_and_clear()` + static success line; dry-run now loads `default_config_root` + `resolve_active_target_name` + `load_active_target_config` and prints the real active target name + Provider/Region/Tier/Cluster/SSH-key from `config.yaml` + a human-readable description of each phase; +2 integration tests (empty store hint + active target resolved name); v0.1.85 | initial |
| 2026-05-14 | M1.5 Track A.10 — miette diagnostic refinement: `CliError` derives `miette::Diagnostic` with a stable `code(apprafter::*)` + multi-line `help(...)` on 9 user-facing variants (cue_not_found, hetzner_api_error, server_type_unavailable, state::corrupt, target::invalid_config, target::not_found, io/json/yaml, cli::other); the binary entry point switched from `color-eyre` to `miette::set_hook` with a `fancy` reporter; `NO_COLOR` is respected; the `color-eyre` workspace + platform-cli deps were removed; +11 tests (8 unit on the `.code()`/`.help()` accessor surface + 3 subprocess-based integration tests on rendered stderr — the `help:` block, diagnostic code substrings, ANSI-free under `NO_COLOR`); v0.1.86 | initial |
| 2026-05-14 | M1.5 Track A.10 walk-fix — `target add` with a broken token exited under the generic `apprafter::cli::other` because `commands/target.rs::ping_provider` and `commands/target_wizard.rs::prompt_token` wrapped the typed `CliError::Hetzner { status: 401, .. }` in `CliError::Other(format!(...))`, losing the diagnostic code and rotation help. v0.1.87 added two typed variants with a `#[diagnostic_source]` cause chain: `apprafter::target::token_rejected` (401 — rotation hint + console URL + clipboard newline trap) and `apprafter::target::provider_unreachable` (non-401 / transport — `apprafter doctor` + status page + `--no-ping`); a shared `classify_ping_error(provider, err)` helper at both call sites; miette now renders a two-layer cascade — an outer summary with rotation help + a chained inner `hetzner_api_error` with a full 401/403/429/5xx breakdown; +2 unit tests on the new variants (`provider_token_rejected_carries_rotation_hint_and_chains_cause` proves that `diagnostic_source()` reaches the inner code, `provider_api_unreachable_targets_outage_path_not_rotation` guarantees that an outage does not suggest rotation); 2 prior target_test integration tests migrated their assertions from the wrapper to diagnostic codes (rendered output line-wraps, substrings such as `(status 401)` are unreliable, codes are reliable); v0.1.87 | initial |
| 2026-05-14 | M1.5 Track A.11 — semantic colors + subcommand aliases: a new `cli_core::style` module on top of `owo-colors` (auto-honours `NO_COLOR` via the `supports-colors` feature) with 6 helpers (ok/warn/fail/info/dim/bold); color applied to `bootstrap-all` phase markers (`→` cyan, `✓` green, `✗` red) + `doctor` glyphs (green ✓ / yellow ⚠ / red ✗); 6 new subcommand aliases (`kubeconfig` ↔ `kc`, `cluster-bootstrap` ↔ `cb`, `bootstrap-all` ↔ `up`, `target list/show/remove` ↔ `ls`/`info`/`rm`), preserving the prior `target` ↔ `t` for chained `apprafter t ls`; +2 unit (style ANSI-strip under non-TTY) + 7 integration (alias subprocess routing including chained `t ls`); v0.1.88 | initial |
| 2026-05-14 | M1.5 Track A.11 walk-fix — after the walk of v0.1.88: `apprafter up --dry-run` remained monochrome (I had only colored the wet path); only the `INFO` label from the tracing-subscriber was glowing green. v0.1.89 covered the dry-run plan: `bootstrap-all` heading + `Target:` label + active target name via `style::bold`; `(active)`/`(via --target override)` tag via `style::info` cyan; `[1/3]`/`[2/3]`/`[3/3]` phase numbers cyan (echoing the wet path's `→`); `Provider:/Region:/Tier:/...` labels + `<unset — ...>` placeholders + bottom hint via `style::dim` so that defaults do not distract from the real values; script(1)-replay confirms ANSI bytes are present in a TTY and absent under `NO_COLOR=1`; v0.1.89 | initial |
| 2026-05-15 | M1.5 Track A.12 — docs + ADR (Track A closure): a new ADR 0030 codifies 4 Track A design decisions (target store, credential resolution chain, miette diagnostics, aliases+color); `docs/operator-guide/quickstart.md` rewritten for the post-Track-A flow (`target add` + `bootstrap-all` instead of env-var + `cargo run --bin apprafter -- init`); a new `docs/operator-guide/target-store.md` (file layout + chain reference); a new `docs/operator-guide/troubleshooting.md` (catalogue of all 11 diagnostic codes + a worked cause-chain example); a new `docs/reference/cli.md` (every subcommand + global env vars table + aliases reference); `docs/operator-guide/index.md` and `docs/reference/index.md` updated; `mkdocs.yml` nav expanded; the Track A backlog (A.9c Phase 2 polish: SSH ConnectTimeout + label rename) explicitly tracked for follow-up; v0.1.90 — closes Track A | initial |
| 2026-05-15 | M1.5 Track A.9c (Phase 2 polish backlog closure): `SshKubeconfigFetcher::build_command` adds `-o ConnectTimeout=5` so the first kubeconfig-poll attempt fails in 5s instead of the kernel-default ~30s while cloud-init brings up sshd on the new cpx22; the Phase 2 label rename `[2/3] kubeconfig` → `[2/3] k3s-ready` (the old label was misleading — ~60s is cloud-init+k3s startup, not the fetch itself) applied consistently: spinner, success/failure markers, dry-run plan, total breakdown summary; the spinner message is now "waiting for cloud-init + k3s on the new node…"; docs (quickstart, troubleshooting, cli reference) updated synchronously; +1 regression test (`ssh_fetcher_caps_connect_timeout_at_five_seconds`); the existing `bootstrap_all_dry_run_prints_three_phase_plan_without_provider_calls` integration test updated for `[2/3] k3s-ready`; a typical Phase 2 on a Hetzner cpx22 + Ubuntu 24.04 is expected to drop from ~60s to ~20-40s; v0.1.91 — closes the entire Track A backlog | initial |
| 2026-05-15 | M1.5 Track B.1.66 — platform-stack scaffold: a new top-level `platform-stack/cue/` (flat layout — CUE treats subdirs as separate package instances even when the `package` declaration matches, which broke cross-file `_components` merging; discovered during the design walk via `cue export -e direct_cilium`). 12 CUE files: `platform.cue` (umbrella schema — `#Version`/`#Channel`/`#Tier`/`#ComponentSource`/`#Component`/`#ComponentSet`/`#PlatformValues`/`_components`), 8 component declarations (`component_cilium.cue`, `component_cert-manager.cue`, `component_argocd.cue`, `component_apprafter-operator.cue`, `component_admission-webhook.cue`, `component_backstage.cue`, `component_network-policies.cue`, `component_argocd-cue-cmp.cue` — the last declared but disabled by default until the wiring step in 1.69), 2 tier overlays (`tier_solo.cue` tier 1 + `tier_team.cue` tier 2 with their own `enabled`/replicas/Hubble settings), `compatibility.cue` with a `#ChangeClass`+`#VersionRecord` schema and an initial 0.2.0 entry; the design walk surfaced a gotcha: the `[NAME=string]: #Component & { name: NAME }` autobinding in `#ComponentSet` re-applies `#Component` on each overlay-unification and strips the concrete `namespace`/`version` — replaced with a plain `[string]: #Component` + an explicit `name:` per component; `_components: #ComponentSet` also cannot be typed (the same problem), left as a plain `{}` with local type-conformance at the declaration site; a `Chart.yaml.tmpl` template + `README.md` (full layout + contribution model + distribution + forking + design-walk decision rationale) + `CHANGELOG.md` (0.2.0 planned entry); `scripts/lint-cue.sh` + `scripts/check-spdx-headers.sh` extended for `platform-stack/cue/...` + `platform-stack/Chart.yaml.tmpl`; `cue vet -c` passes, lint clean, all 565 Rust tests remain green; v0.1.92 — opens Track B | initial |
| 2026-05-15 | M1.5 Track B.1.67 — `cue cmd render` pipeline + umbrella chart generation: `platform-stack/cue/render_tool.cue` declares `command: render: { ... }` with 9 tasks (3 `file.Mkdir` + 6 `file.Create`) and a `$dep` chain for DAG ordering; emits Chart.yaml v2 (with the annotations apprafter.io/change-class+apprafter.io/operator-version from compatibility.cue), values.yaml (defaults to tier1), values.schema.json (hand-rolled Helm-native draft-2020-12 — CUE's auto-export targets draft-07 which Helm does not understand), templates/applications.yaml (a single Go template iterating over .Values.components → one Argo CD Application per enabled entry, conditional helm.valuesObject only when source.chart is set, labels apprafter.io/{component,tier,channel}), compatibility.yaml, examples/values.{solo,team}.yaml, README.md inside the rendered chart; `platform-stack/Makefile` with targets `render`/`render-only`/`lint`/`clean`/`help`, auto-detect cue+helm with nix fallback, version resolved from tier1.version via `cue export` without hardcoding; the `Justfile` gained `platform-stack-render` + `platform-stack-check` wrappers; `dist/` is already gitignored project-wide; README local-dev section updated with the real commands + a schema-gate sanity check; verified — `helm lint` clean (a single INFO about the chart icon), `helm template` default → 6 tier-1 Apps, with team values → 7 Apps (Backstage on), `--set tier=99` → schema rejects "value must be one of 1, 2, 3, 4"; v0.1.93 | initial |
| 2026-05-15 | M1.5 Track B.1.68 — CI OCI publish workflow + cosign signing: `.github/workflows/platform-stack-publish.yml` triggers on a `platform-stack/v*` tag (+ `workflow_dispatch` with a `version:` input); validates `compatibility.cue` via `scripts/check-platform-stack-version.sh` (cue export -e compatibility[<v>], exits 1 with a pointer-to-fix if the entry is missing); renders the chart (`make -C platform-stack render-only`); `helm lint` + tier-1/tier-2 smoke template assertions (6 / 7 enabled Apps); `helm package` → `.tgz`; `docker login` to ghcr.io via GITHUB_TOKEN; `helm push` to `oci://ghcr.io/<owner>` (Helm 3.8+ native OCI); resolve immutable digest via `helm show chart --version`; cosign keyless sign by digest (Sigstore OIDC + `id-token: write` permission, no managed keys); cosign sign-blob on the `.tgz` → `.tgz.sig` + `.tgz.pem` for the GitHub Release attachment path; `oras tag :latest` on stable releases with a graceful warning if the CLI is absent; `gh release create` with heredoc-built notes via mktemp+`--notes-file` (install snippets Argo CD + plain Helm + cosign verify for both) + `.tgz` / `.sig` / `.pem` attachments + `--prerelease` flag on a pre-release; security hardening — all dynamic inputs (github.ref_name, github.repository_owner, github.event.inputs.version, github.repository) routed through env: binding (the same anti-injection pattern as release-operator.yml); `platform-stack/RELEASE.md` — full maintainer procedure (semver rules, pre-release checklist, tagging, after-publish actions, failure-mode recovery); local validation: yamllint clean, scripts/check-platform-stack-version.sh tested on the happy (0.2.0 → YAML) + unhappy (99.99.99 → exit 1) paths; final acceptance ⏳ verifies after the first real `platform-stack/v0.2.0-rc1` push (CI-side, not locally reproducible); v0.1.94 | initial |
| 2026-05-15 | M1.5 Track B.1.68 walk-fix — GitHub Actions rejected the workflow on push with the error "Line: 232, Col: 14: Unexpected symbol: '…'" because one of the comments inside a `run: |` block contained `${{ … }}` (a literal Unicode ellipsis inside expression-syntax braces) — GHA parses `${{ }}` in `run`-body scalars before the shell sees the script. v0.1.95 rewrote that comment without `${{ }}` syntax, verified with grep that there are no other expression-style braces in `run` bodies (header comments outside scalars are discarded by yamllint — line 29 there is harmless); yamllint clean, the remaining gates green; the workflow is now syntactically valid for the GitHub Actions parser; v0.1.95 | initial |
| 2026-05-15 | M1.5 chart-versioning policy decision — the first published platform-stack version = **0.1.0** instead of the previously planned 0.2.0. Rationale: the chart MINOR tracks the phase number of the AppRafter monorepo (Phase 1.5 → chart 0.1.x), not the milestone target. When Phase 2 services land → the chart MINOR will bump to 0.2.0 alongside `v0.2.0-services`. Patch versions of the chart and the monorepo are independent (they share only MINOR/MAJOR semantics). v0.1.96 flipped: `tier_solo.cue` + `tier_team.cue` `version: "0.1.0"`, `compatibility.cue` entry rename, `platform.cue` doc-comment, 4 component doc-comments (cilium/cert-manager/argocd-cue-cmp/operator/webhook mentioning "platform-stack X.Y.Z"), `CHANGELOG.md` section + version notes, `RELEASE.md` versioning rules + tagging examples; re-rendering produces `dist/platform-stack-0.1.0/`, helm lint clean, `check-platform-stack-version.sh 0.1.0` → success, `0.2.0` → exit 1; v0.1.96 | initial |
| 2026-05-19 | M1.5 Track B.1.68 refactor — chart-version single-source-of-truth + the workflow inverts the tag↔publish ordering. Walk-found: tier_solo/tier_team/compatibility-key all hardcoded "0.1.0" as a literal → potential drift. Also, the workflow triggered on push:tags, which allowed "accidental tag push → unconditional publish"; the user asked to reverse the direction — the workflow creates the tag after a successful publish, not the other way around. v0.1.97-equivalent (without a monorepo bump — the CLI did not change): added `currentVersion: #Version & "0.1.0"` to platform.cue as the canonical source; `tier_solo`/`tier_team` are now `version: currentVersion`; compatibility.cue received a CUE-level invariant `compatibility: (currentVersion): #VersionRecord` — bumping currentVersion without a matching entry fails on `cue vet -c` with an incomplete-fields diagnostic; the workflow trigger switched to `workflow_dispatch` ONLY (no push:tags), with an optional `version_override:` input for emergency re-publish; the workflow reads `currentVersion` via `cue export -e currentVersion`, checks that the tag does not exist on origin, runs the compat-gate + render + lint + push + sign, and at the very end `gh release create` creates the tag + release in a single call (via `--target $GITHUB_SHA`); `scripts/check-platform-stack-version.sh` without arguments auto-reads currentVersion; RELEASE.md fully rewritten for the new model — "two-line version bump in platform.cue+compatibility.cue", `gh workflow run` instead of `git tag && git push`, failure-mode recovery simplified; cli/Cargo.toml rolled back to 0.1.96 (the CLI did not change, no monorepo tag is created) | n/a |
| 2026-05-19 | M1.5 Track B.1.68 auto-trigger + drift detection — `platform-stack-publish.yml` now triggers on `push: branches: [master], paths: ['platform-stack/**', …]` (plus a `workflow_dispatch` style-override); the job is split into `detect` + `publish`: detect resolves currentVersion and checks that `platform-stack/v<version>` does not exist on origin, and if it already does → `should_publish=false` and the publish job is skipped (the commit was not a bump, but a refactor / docs / drift). A new workflow `platform-stack-check.yml` triggers on PRs + pushes to master with the same paths and enforces: cue fmt --check + cue vet -c (the invariant catches bump-without-compat) + render + helm lint + tier-1/tier-2 smoke + **drift detection** — if currentVersion matches a tag on origin AND files in `platform-stack/cue/*.cue` or `Chart.yaml.tmpl` differ from that commit → fail with an 80-line diff and a hint about currentVersion in platform.cue. This makes "chart source changed without a version bump" a blocking CI error at PR time; on master the same check works as a post-merge safety net. `actions/checkout` with `fetch-depth: 0` so the drift check has access to remote tags. RELEASE.md updated for the auto-trigger model — "Normal flow: bump + PR → check workflow + merge → publish workflow auto-detects bump"; a PR-time guards section added. yamllint clean, all cue/spdx/cargo gates green. The CLI did not change → no monorepo tag is created, the versions in Cargo.toml are not touched | n/a |
| 2026-05-19 | M1.5 Track B.1.69 — CUE CMP sidecar (ADR 0029): a new top-level `argocd-cue-cmp/` flat directory (Dockerfile, plugin.yaml, entrypoint.sh, VERSION='0.1.0', README.md). An Alpine 3.20 multi-stage build pulls the cue v0.10.0 tarball, the runtime image runs as UID/GID 999 (matching the argocd-repo-server CMP contract; the pre-existing Alpine ping group at gid 999 deleted to free the slot). entrypoint.sh wraps `cue export ./... --out yaml` with structured error output — the first error line as `::cue-cmp:: CUE compile failed: <summary>` to stderr, the full block below, exit 0/1 correctly. New paired workflows `argocd-cue-cmp-publish.yml` + `argocd-cue-cmp-check.yml` — the same detect/publish split + drift detection pattern as platform-stack-*; the image's own semver track `argocd-cue-cmp/v*` is independent of the monorepo/chart/operator versions. Chart bumped 0.1.1 → 0.1.2: `component_argocd-cue-cmp.cue` updated for the new image registry + version pin, `component_argocd.cue` gains a `repoServer.extraContainers` block with the cue-cmp sidecar (image pull via CUE interpolation `_components."argocd-cue-cmp".values.image.{repository,tag}` — a one-line bump of the cue-cmp version), compatibility.cue entry for 0.1.2 (change: safe, references ADR 0029). SPDX gate extended for `argocd-cue-cmp/{Dockerfile,plugin.yaml,entrypoint.sh}` (170 → 175). Locally verified: docker build clean, entrypoint happy + error paths exit-code/output correct, helm template 0.1.2 shows extraContainers + the cue-cmp:v0.1.0 image. CI-side acceptance ⏳ verified at the first push (the cue-cmp publish workflow + the chart publish workflow both trigger in parallel) | n/a |
| 2026-05-19 | M1.5 Track B.1.70 — Minimal cluster-bootstrap rewrite (ADR 0025): `commands/cluster_bootstrap.rs` rewritten from a ~1250-line imperative install (Cilium + Gateway + Application CRD + default-deny + Argo CD + cert-manager + ClusterIssuer + operator + webhook + Backstage) to a 4-step GitOps loader (~450 lines). Step 1: `helm upgrade --install argocd argo/argo-cd` with loader-only values (replicas=1, dex off). Step 2: `kubectl wait --for=condition=Available deployment/argocd-server` (gates the root Application apply until CRDs are installed). Step 3: kubectl apply the root Application (`name: platform, source.repoURL: oci://ghcr.io/<owner>/platform-stack, chart: platform-stack, targetRevision: 0.1.2`). Step 4: `kubectl wait --for=jsonpath='{.status.health.status}'=Healthy application/platform` — after Healthy all child Applications reconcile under Argo CD. Argo CD handles drift correction + prune semantics + idempotent re-apply. The existing `cli-providers::k8s::*_yaml` renderers remain (the chart's parallel source-of-truth until the 1.71 migration). `cli-providers::k8s` exports 3 new constants: `RELEASED_PLATFORM_STACK_VERSION = "0.1.2"`, `APPRAFTER_PLATFORM_STACK_DEFAULT_REPO = "oci://ghcr.io/apprafter"`, `APPRAFTER_PLATFORM_STACK_CHART_NAME = "platform-stack"`. The `KubectlRunner` trait extended with a `wait_for_condition()` method (supports `--for=condition=` + `--for=jsonpath=` flavours). 13 imperative-install tests deleted, 5 GitOps-loader tests added. 549 passed (down from 565 net). The CLI binary changes significantly → monorepo tag v0.1.97. Real-cluster acceptance ⏳ verified at the first walk after push | v0.1.97 |
| 2026-05-19 | M1.5 Track B.1.70 walk-fix — the real-Hetzner walk of v0.1.97 surfaced a pre-install hook timeout: `helm install argocd argo/argo-cd 7.7.7` fails on single-node k3s with `failed pre-install: timed out waiting for the condition`. The root cause — the upstream chart defaults `redis-ha.enabled: true` → 3 redis pods with `requiredDuringSchedulingIgnoredDuringExecution` podAntiAffinity, which cannot schedule on a single node. The v0.1.x in-tree baseline explicitly disabled this (`cli-providers::k8s::argocd_values_yaml`); the v0.1.97 rewrite accidentally dropped the flag. v0.1.98 fix: restored `redis-ha.enabled: false` + `notifications.enabled: false` + `server.service.type: ClusterIP` in the loader values (cluster_bootstrap.rs) and in the chart's `component_argocd.cue` (so that self-reconcile does not re-enable redis-ha on adoption); +2 tests (`argocd_loader_values_disables_redis_ha_for_single_node_k3s`, `argocd_loader_values_keep_server_at_cluster_ip_until_chart_exposes_it`); 551 passed; chart bumped 0.1.2 → 0.1.3 with a compat entry + a warning in the 0.1.2 notes; CLI bumped 0.1.97 → 0.1.98, `RELEASED_PLATFORM_STACK_VERSION` → "0.1.3" | v0.1.98 |
| 2026-05-19 | M1.5 Track B.1.70 walk-fix #2 — the second real-Hetzner walk of v0.1.98 surfaced **a catch-22 the rewrite created**: the v0.1.98 redis-ha fix helped (chart 0.1.3 applied), but the Argo CD pre-install Job `argocd-redis-secret-init` remained Pending with `0/1 nodes are available: 1 node(s) had untolerated taint(s)`. The root cause: k3s starts without a CNI (`--flannel-backend=none`), the node is `NotReady` with `node.kubernetes.io/not-ready:NoSchedule`; the Argo CD pre-install Job pod does not tolerate the taint → helm timeout. The v0.1.x baseline installed Cilium FIRST (the node became Ready before Argo CD); the v0.1.97 rewrite reversed the order — Argo CD first, Cilium via the chart, but the chart cannot reconcile without Argo CD, which cannot start without a CNI. v0.1.99 fix: Cilium returns to the CLI loader as Step 0 (uses the existing `cli-providers::k8s::cilium_values_yaml` + `CILIUM_CHART_VERSION = 1.16.5`); Step 0b `kubectl wait --for=condition=Ready node --all` (timeout 180s — the image pull dominates). The chart's `component_cilium.cue` remains the owner of upgrades via Argo CD adoption (same release name + namespace, `prune: false`). The `KubectlRunner::wait_for_condition` API changed: `namespace: &str → namespace: Option<&str>` (cluster-scoped resources must not carry `-n`). 3 new tests (`wait_command_emits_namespace_flag_when_some`, `wait_command_omits_namespace_flag_when_none`, `cilium_installs_before_argocd_so_node_can_become_ready` — a regression guard on the ordering); the existing main test renamed for the new sequence; FakeKubectl in `cluster_bootstrap` + `argocd_password` updated for `Option<&str>`. 554 passed (was 551). The chart does NOT change — `RELEASED_PLATFORM_STACK_VERSION` remains "0.1.3". CLI bumped 0.1.98 → 0.1.99 | v0.1.99 |
| 2026-05-19 | M1.5 Track B.1.70 walk-fix #3 — the third real-Hetzner walk of v0.1.99 surfaced **two coupled bugs** in the GitOps loader path: (1) Argo CD does not infer the OCI Helm protocol from the `oci://` scheme — it shells out to `helm pull --repo oci://... <chart>` which `helm` rejects with `object required`; OCI registries require `helm pull oci://<repo>/<chart>` (chart-name inline), and Argo CD generates the correct form ONLY when the repo is registered via `Secret(label=repository, enableOCI: "true")`. (2) `kubectl wait --for=jsonpath={.status.health.status}=Healthy` on the root Application — a false positive: a freshly-created Application with zero rendered children is trivially Healthy (no resources to fail), even when Sync=Unknown (the chart pull errored). The v0.1.99 loader matched empty-Healthy and returned successfully, leaving the cluster without child Applications. v0.1.100 fix: (a) `APPRAFTER_PLATFORM_STACK_DEFAULT_REPO` "oci://ghcr.io/apprafter" → "ghcr.io/apprafter" (bare URL), (b) `argocd_loader_values_yaml` gains a `configs.repositories.apprafter` block (url + type: helm + enableOCI: "true"), (c) the chart's `component_argocd.cue` mirrors the same block in values so that self-reconcile preserves the registration on adoption, (d) the chart's `component_apprafter-operator.cue` + `component_admission-webhook.cue` dropped the `oci://` prefix in repoURL, (e) `perform_bootstrap` step 4 split into 4a (wait Synced) + 4b (wait Healthy) — Synced is the correct first signal that the chart pulled and children rendered. 3 new tests (`argocd_loader_values_register_apprafter_oci_repo` with a per-line oci:// negative guard, `root_application_repourl_is_bare_without_oci_scheme`), the main bootstrap test extended for 4 waits. 556 passed (was 554). Chart bumped 0.1.3 → 0.1.4 with a compat entry + a known-issue note in the 0.1.3 notes; CLI bumped 0.1.99 → 0.1.100, `RELEASED_PLATFORM_STACK_VERSION` → "0.1.4". Manual diagnosis on the live cluster: creating `Secret(repo-apprafter)` + patching the root Application's repoURL → 6 child Applications appeared instantly (cert-manager Synced/Healthy first; the rest Sync=Unknown initially — the controller catches up) | v0.1.100 |
| 2026-05-19 | M1.5 Track B.1.70 walk-fix #4 — the fourth real-Hetzner walk on chart 0.1.4 / CLI v0.1.100: the root `platform` Synced/Healthy + 6 children appeared, but only `cert-manager` Synced; the other 5 failed for three independent reasons. **Bug A** — the `apprafter-operator` + `admission-webhook` helm charts were never published to OCI (`release-operator.yml` only pushed container images); the `apprafter-operator` chart existed at `0.1.29` (drifted from the `v0.1.91` pin), the `apprafter-admission-webhook` chart was **absent**. Fix: a new `apprafter-admission-webhook` helm chart created (Chart.yaml + values + _helpers + Certificate + Service + Deployment + ValidatingWebhookConfiguration; templates derived from `cli-providers::k8s::admission_webhook_yaml`), the `apprafter-operator` Chart.yaml bumped `0.1.29 → v0.1.91`, a new `helm-charts` job added to `release-operator.yml` (needs the operator+webhook image jobs; uses azure/setup-helm + `helm package` + `helm push oci://ghcr.io/<owner>`; the chart version is read from Chart.yaml, not from github.ref_name — the platform-stack pin = the single source of truth). **Bug B** — `cilium` + `argocd` failed the structured-merge diff with `terminatingReplicas: field not declared in schema`; k3s v1.35 surfaces the Kubernetes 1.31+ field `Deployment/DaemonSet/StatefulSet.status.terminatingReplicas`, which Argo CD 2.13.1 does not know. Fix: the `#Component` schema in platform.cue gains an optional `ignoreDifferences: [...{group, kind, jsonPointers?, jqPathExpressions?}]` (default `[]`); `render_tool.cue` template emits the block in the Application spec when non-empty; `component_cilium.cue` ignores Deployment+DaemonSet, `component_argocd.cue` ignores Deployment+StatefulSet `/status/terminatingReplicas`. **Bug C** — `network-policies: app path does not exist` because `manifests/tier-1/network-policies/` was never created during the v0.1.97 imperative-to-GitOps rewrite. Fix: `manifests/tier-1/network-policies/default-deny.yaml` created (content from `cli-providers::k8s::network_policy::default_deny_network_policy_yaml` — ingress allow same-ns + kube-system, no egress block, matches the v0.1.x baseline). Chart bumped 0.1.4 → 0.1.5 with a full compat entry + a known-issue note in 0.1.4; CLI bumped 0.1.100 → 0.1.101, `RELEASED_PLATFORM_STACK_VERSION` → "0.1.5". SPDX gate 175 → 184 (8 new chart-side files + 1 new manifest). Also a parallel perf(operator) commit (without a bump, CI-only): a cargo-chef multi-stage Dockerfile for the operator + webhook images — a typical source-only push CI build goes from ~6 min to ~1-2 min via the chef cook layer cache. Real-cluster acceptance ⏳ verified at the next walk after push (the operator helm charts should be published by the workflow, the platform-stack 0.1.5 chart should be published by the publish workflow, the walk should show all 6 children Synced/Healthy) | v0.1.101 |
| 2026-05-19 | M1.5 Track B.1.70 walk-fix #5 — the fifth walk on chart 0.1.5 / CLI v0.1.101: bootstrap passed, the root `platform` Synced/Healthy, but 5 of 6 children in a degraded state. **Bug D** — the `admission-webhook` Deployment rejected by the API: `selector does not match template labels`; in my new webhook chart's `_helpers.tpl` I defined `labels` WITHOUT including `selectorLabels` via `include`, and `Deployment.spec.template.metadata.labels` did not contain the `app.kubernetes.io/{name,instance}` against which the selector matched. Fix: mirrored the operator chart's pattern (labels include selectorLabels via include), standardised the webhook selector on the `app.kubernetes.io/{name,instance}` convention. **Bug E** — `apprafter-operator` + `admission-webhook` still `terminatingReplicas: field not declared in schema`; in v0.1.101 chart 0.1.5 I added `ignoreDifferences` only to cilium + argocd, forgetting operator + webhook. Fix: added the same `ignoreDifferences: [{group: apps, kind: Deployment, jsonPointers: [/status/terminatingReplicas]}]`. **Bug F** — `network-policies: app path does not exist`; `component_network-policies.cue` pinned `version: v0.1.91` (the operator chart's AppVersion anchor), but in that git tag the directory `manifests/tier-1/network-policies/` did not yet exist (created only in v0.1.101). Fix: bump the pin to "v0.1.102" — the current tag which ships 0.1.6 + the directory. **Bug G** — the `admission-webhook` Certificate failed with `no endpoints available for service cert-manager-webhook`; Argo CD applied the Certificate in parallel with the cert-manager rollout, the webhook did not yet have endpoints. Fix: introduced sync ordering via `argocd.argoproj.io/sync-wave` annotations. The `#Component` schema gained an optional `syncWave: int | *0`; `render_tool.cue` emits the annotation on the rendered Application metadata; cilium = -20 (CNI prerequisite), argocd = -15 (self-adopt early), cert-manager = -10 (the webhook+CRDs live before cert-manager.io/v1 applies), the rest = 0 (default). Argo CD waits for the prior wave's `Sync=Synced` before the next wave. **Bug H** — the `argocd-redis-secret-init` Job (pre-install,pre-upgrade hook) re-fires on adopt, the image pull is slow; plausibly just timing (the Bug B+G mitigations should help indirectly), left for verification on the next walk. Chart bumped 0.1.5 → 0.1.6 with a full compat entry + a known-issues note in 0.1.5; CLI bumped 0.1.101 → 0.1.102, `RELEASED_PLATFORM_STACK_VERSION` → "0.1.6". All CLI gates clean (556 passed, fmt+clippy+SPDX+cue vet) | v0.1.102 |
| 2026-05-19 | M1.5 Track B.1.70 walk-fix #6 — the sixth walk on chart 0.1.6 / CLI v0.1.102: bootstrap passed, the sync-wave order works (cilium → argocd → cert-manager → rest), platform + network-policies OK, but **cilium-operator CrashLoopBackOff** ⇒ cascading failures: cert-manager/webhook not Ready (no endpoints), webhook Certificate validation fails, the argocd redis-secret-init Job hangs (no network). **Bug J** — root cause: `kubectl describe pod cilium-operator` showed env `KUBERNETES_SERVICE_HOST: auto` (a literal string), the Cilium operator cannot dial `auto:` and crashes. `helm get values cilium` showed the loader values (127.0.0.1), but the live ConfigMap + Deployment had the **chart values** (`enable-ipv6: "false"`, `KUBERNETES_SERVICE_HOST=auto`). Mechanic: Argo CD does not do a `helm upgrade` of the loader release — it renders the chart templates with the CHART's values and applies them as plain manifests, overwriting the loader's. Two owners for the same Deployment + ConfigMap; the chart-rendered one wins. Through all of walks 1-5 I thought this was a helm upgrade, but it is a plain manifest overlay. Fix: `component_cilium.cue` values mirror `cli-providers::k8s::cilium_values_yaml` byte-by-byte — `kubeProxyReplacement: true` (bool not string), `k8sServiceHost: "127.0.0.1"`, `k8sServicePort: 6443`, `ipv4.enabled: true`, `ipv6.enabled: true`; a banner comment reminds that editing BOTH sides is required until the B.1.71 central values. **Bug I** — `component_cert-manager.cue` was missed in the ignoreDifferences pass of the previous fixes (cilium+argocd in 0.1.5, operator+webhook in 0.1.6, cert-manager skipped). Fix: the same one-element block. **Bug H** (argocd redis-secret-init) — plausibly caused by cilium-agent being down (no network → no schedule → the Job hangs); the Bug J fix should indirectly resolve it. Verify on the next walk. Chart bumped 0.1.6 → 0.1.7 with a full compat entry + a known-issues note in 0.1.6; CLI bumped 0.1.102 → 0.1.103, `RELEASED_PLATFORM_STACK_VERSION` → "0.1.7". 556 CLI tests passed, all gates clean. User feedback on CI build time: after the cargo-chef migration builds slowed to 7-8 min (vs a 6 baseline) — a known cache-warmup issue after the layer restructure; the first 2-3 builds write new cache keys, subsequent ones will be fast. A separate perf optimization commit is upcoming: pin `rust:alpine` to a specific version (to avoid floating-tag invalidation) + a binary `cargo-chef` install (~10s) instead of `cargo install --locked` (~1-2 min compile) | v0.1.103 |
| 2026-05-20 | M1.5 Track B.1.70 walk-fix #7 — bootstrap hung on `kubectl wait application/platform Synced` (a 10-min timeout). Chart 0.1.7 published OK (gh release list confirmed). The root cause: `kubectl describe application platform -n argocd` showed `Application referencing project default which does not exist`; `kubectl get appproject -n argocd` → `No resources found`. **Bug K** — the Argo CD chart 7.7.7 ships `configs.projects: {}` by default, and argocd-server 2.13.1 does NOT recreate the `default` AppProject on startup. Every Application with `spec.project: default` (including the root `platform`) failed. Past walks (#4-#6) apparently hit this lazily (the retry-loop appeared to handle it); the v0.1.103 run was deterministic. Fix: a `configs.projects.default` block added on both sides — `cli-providers::k8s::argocd_loader_values_yaml` (the loader creates the AppProject in the initial `helm install`, before the root Application apply) + `platform-stack/cue/component_argocd.cue` (the chart's self-reconcile keeps it alive on adopt). The spec mirrors Argo CD's historical implicit default — `sourceRepos: ["*"]`, unrestricted `destinations`, full-kind whitelists. Admins wanting a restricted default edit the fork's overlay. +1 regression test `argocd_loader_values_create_default_app_project` (557 passed, was 556). Chart bumped 0.1.7 → 0.1.8 with a full compat entry + a known-issue note in 0.1.7; CLI bumped 0.1.103 → 0.1.104, `RELEASED_PLATFORM_STACK_VERSION` → "0.1.8". A recovery doc for stuck-on-0.1.7 clusters in UNRELEASED.md (a one-liner `kubectl apply` AppProject + a hard-refresh annotation on the root Application). Real-cluster acceptance ⏳ verified at the next walk after push | v0.1.104 |
| 2026-05-20 | M1.5 Track B.1.70 walk-fix #8 — after the manual default AppProject (the v0.1.104 fix of Bug K) 4 children are green, but operator + webhook are still broken. **Bug M** — `kubectl describe pod operator`: `CreateContainerError` + "failed to generate spec: no command specified". A `kubectl run` test pulling image v0.1.91 showed `stat /apprafter-operator: no such file or directory` — **the binary is absent from the image manifest**. Image v0.1.91 was published months ago at the closing tag of Track A, apparently a partial / stale Dockerfile that did not reach the COPY step. The image has been broken for months — but was never exercised because the v0.1.x cluster-bootstrap installed the operator chart from a local path, not OCI. Fix: the operator + webhook chart Chart.yaml bumped to `version: v0.1.92`, `appVersion: "v0.1.105"` (the current monorepo tag — the release-operator.yml workflow reliably rebuilds the images with the cargo-chef Dockerfile). Plus defence in depth: an explicit `command: ["/apprafter-operator"]` + `command: ["/admission-webhook"]` in the chart deployment templates — no longer dependent on the image manifest's ENTRYPOINT, so future image-build accidents do not reproduce silently. **Bug L** — `kubectl get clusterissuer` → No resources found. The webhook chart's Certificate references `kind: ClusterIssuer, name: apprafter-selfsigned`, but the cert-manager chart 1.16.2 does not ship default issuers, and in v0.1.x the creation of the ClusterIssuer via `cli-providers/k8s/issuer.rs` migrated out of the CLI during the v0.1.97 rewrite — but was **never ported to a chart template**. Fix: a new template `operator/charts/apprafter-admission-webhook/templates/clusterissuer.yaml` ships the `apprafter-selfsigned` ClusterIssuer (`selfSigned: {}` spec — matches the v0.1.x baseline) together with the Certificate in a single chart. Also a **decorative drift** fixed: `RELEASED_OPERATOR_VERSION` "v0.1.64" → "v0.1.105" (3 months stale per the CLAUDE.md rule). Chart bumped 0.1.8 → 0.1.9 with a full compat entry + a known-issues note in 0.1.8; CLI bumped 0.1.104 → 0.1.105, RELEASED_PLATFORM_STACK_VERSION → "0.1.9". 557 CLI tests still pass; all gates clean | v0.1.105 |
| 2026-05-20 | M1.5 Track B.1.70 walk-fix #9 — on v0.1.105 the image first actually ran the webhook code (the v0.1.91 image was broken, the binary missing, the webhook code did not execute for 8 walks). Surfaced **Bug N** — `thread 'main' panicked at rustls-0.23.40/src/crypto/mod.rs:249: Could not automatically determine the process-level CryptoProvider`. rustls 0.23+ removed the auto-default; the operator binary had the fix `install_rustls_crypto_provider()` since v0.1.61, but the webhook crate missed it (jumping straight to `RustlsConfig::from_pem_file` without the install). A long-standing latent bug, masked by the broken v0.1.91 image for months. Fix: `operator/admission-webhook/src/lib.rs` gained `install_rustls_crypto_provider()` (the same shape as the operator's — `aws_lc_rs::default_provider().install_default()`, idempotent); `src/main.rs` calls it as the first line of `async fn main()`, before the TLS server init; `Cargo.toml` a direct `rustls = { version = "0.23", features = ["aws-lc-rs"] }` dep for `default_provider` resolution; +2 regression tests (`install_rustls_crypto_provider_sets_a_process_level_default`, `_is_idempotent`) mirroring the operator's. Chart-side: the operator + webhook charts both bump to `version v0.1.93 / appVersion v0.1.106` in lockstep (the operator chart bumps even though its code did not change — syncing appVersion prevents future drift). Chart bumped 0.1.9 → 0.1.10 with a compat entry + a known-issue note in 0.1.9; CLI bumped 0.1.105 → 0.1.106, RELEASED_PLATFORM_STACK_VERSION → "0.1.10", RELEASED_OPERATOR_VERSION → "v0.1.106". 557 cli + 62 operator tests passed; all gates clean | v0.1.106 |
| 2026-05-20 | M1.5 Track B.1.70 walk-fix #10 — argocd Application Synced/Degraded; the new argocd-repo-server pod (with the cue-cmp sidecar) stuck Init:0/1 with `MountVolume.SetUp failed for volume "cue-cmp-config": configmap "cue-cmp-plugin-config" not found`. **Bug O** — `component_argocd.cue` added the cue-cmp sidecar with a volumeMount on the ConfigMap `cue-cmp-plugin-config` in chart 0.1.2 (Track B.1.69), but **the ConfigMap itself was never created**. The bug was latent through 6 chart versions (0.1.2 → 0.1.10), masked by the previous blockers (broken image, missing ClusterIssuer, rustls panic) — the repo-server pod never got a chance to schedule. Walk #10 was the first clean run where the cue-cmp sidecar actually attached + the mount was evaluated. Fix: `component_argocd.cue` gains an `extraObjects` block with the ConfigMap `cue-cmp-plugin-config` containing the verbatim plugin.yaml content (the Argo CD CMP contract — `apiVersion: argoproj.io/v1alpha1, kind: ConfigManagementPlugin, name: cue, discover.find.glob: "**/apprafter*.cue", generate.command: sh -c /usr/local/bin/entrypoint.sh`). The source of truth remains `argocd-cue-cmp/plugin.yaml` for now, this embedding is duplication until a future `cue cmd` step in the chart renderer reads the source file directly — a comment marks the lockstep edit requirement. Chart bumped 0.1.10 → 0.1.11 with a full compat entry + a known-issue note in 0.1.10; CLI bumped 0.1.106 → 0.1.107, RELEASED_PLATFORM_STACK_VERSION → "0.1.11". 557 cli tests passed; all gates clean | v0.1.107 |
| 2026-05-20 | M1.5 Track B.1.70 walk-fix #11 — walk-fix #10 ConfigMap landed OK, the new argocd-repo-server pod is now stuck on image pull instead of mount: `Back-off pulling image "ghcr.io/apprafter/argocd-cue-cmp:v0.1.0": ErrImagePull MANIFEST_UNKNOWN`. **Bug P** — `crane ls` showed the image **exists**, but the tag = `:0.1.0` (without `v`), while the chart pin = `:v0.1.0`. The `argocd-cue-cmp-publish.yml` workflow tag line was `${IMAGE}:${VERSION}` where VERSION = `0.1.0` (without v) from the VERSION file. The git tag was created as `argocd-cue-cmp/v<version>` (with `v`), but the image tag was without it. Inconsistency with the operator + webhook workflows (there `image:${github.ref_name}` = `:v0.1.x`). Latent since chart 0.1.2 (Track B.1.69), masked in walks #5-10 by upstream blockers (the broken ConfigMap prevented the pull from happening). Fix: (1) workflow `tags:` line gets a `v` prefix: `${IMAGE}:v${VERSION}`; (2) `Tag :latest` source + release notes example updated; (3) `argocd-cue-cmp/VERSION` 0.1.0 → 0.1.1 (the workflow detect gates on git tag existence, without a bump it would skip publish); (4) `component_argocd-cue-cmp.cue` pin to `v0.1.1`. The v0.1.1 image is a re-publish of source v0.1.0 with the corrected tag form. The v0.1.0 image stays on the registry as a historical artefact. Chart bumped 0.1.11 → 0.1.12 with a full compat entry + known-issue note in 0.1.11; CLI bumped 0.1.107 → 0.1.108, RELEASED_PLATFORM_STACK_VERSION → "0.1.12". 557 cli tests passed; all gates clean | v0.1.108 |
| 2026-05-20 | M1.5 Track B.1.71 closure — chart as single source of truth. `cli/cli-providers/build.rs` extracts `_loaderValues.{cilium,argocd}` + `currentVersion` from `platform-stack/cue/` at compile time, emits `CILIUM_VALUES_YAML`, `ARGOCD_LOADER_VALUES_YAML`, `RELEASED_PLATFORM_STACK_VERSION` as generated Rust constants. `cluster_bootstrap.rs` swaps hand-rolled YAML for these. 12 dead `*_yaml` renderers deleted (admission_webhook, application_crd, argocd_gateway, argocd_repo_secret, backstage_app_config, backstage_manifests, bootstrap_app, cert_manager_values, cilium_values, issuer, network_policy, operator_chart, operator_values) plus 3 dead examples. The CUE invariant `_components.cilium.values ≡ _loaderValues.cilium` makes walk-fix #6's drift class structurally impossible; the Argo CD chart's `values:` derives from `_loaderValues.argocd & { ...extras... }` so the loader stays a strict subset by construction. The `RELEASED_PLATFORM_STACK_VERSION` drift class is also gone. Chart bumped 0.1.12 → 0.1.13 (refactor, no rendered-output change); CLI bumped 0.1.108 → 0.1.109. Test count 557 → 479 (~80 deleted renderer tests, 4 new loader_values regression guards). Deferred: `RELEASED_OPERATOR_VERSION` from the operator chart's Chart.yaml (cross-workspace path), `argocd-cue-cmp/plugin.yaml` embedded in component_argocd as a string literal | v0.1.109 |
| 2026-05-20 | M1.5 Track B.1.71b closure — closed the remaining 6 version-duplication classes from B.1.71's deferred follow-ups. Cilium + Argo CD upstream chart versions migrated to `_loaderValues.{cilium,argocd}.chartVersion` in CUE; chart-side invariants + build.rs-derived `CILIUM_CHART_VERSION` / `ARGOCD_CHART_VERSION` constants. Hand-maintained consts in `helm.rs` + the entire `argocd_values.rs` deleted. Operator + admission-webhook container image tags now sourced from `operator/charts/<chart>/Chart.yaml#appVersion` via a tiny grep-based reader in build.rs; the chart's `values.image.tag` dropped so the Helm template's `.Chart.AppVersion` fallback drives the image. The hand-maintained `RELEASED_OPERATOR_VERSION` in `image_ref.rs` deleted. The cue-cmp `VERSION` plain-text file replaced by `argocd-cue-cmp/version.cue` (package at `apprafter.io/argocd-cue-cmp`); the chart imports via CUE, the publish + check workflows read via `cue export -e version --out text`. Chart bumped 0.1.13 → 0.1.14 (refactor, byte-equivalent rendering); CLI bumped 0.1.109 → 0.1.110. Test count +3 new regression guards. B.1.71's deferred-follow-up section is now empty | v0.1.110 |
| 2026-05-29 | M1.5 Track B.1.79b **closure** (bookkeeping catch-up — the work shipped across v0.1.161–v0.1.174 but the plan.md checkboxes + this ledger row lagged) — CLI app ergonomics: `app open` + scaffolding + runtime templates. Parts: `apprafter app open <name>` (part 1, v0.1.161 — `kubectl port-forward` wrapper resolving Application→Service via the operator's `app.kubernetes.io/name` label, primary-port detection, browser launch, graceful Ctrl+C teardown via stdout/stderr drainer threads); runtime detection primitives (part 2, v0.1.166 — filesystem-marker → runtime mapping with High/Medium/Low/Fallback confidence, multi-stack monorepo surfacing); `apprafter app scaffold` + an embedded `.cue.hbs` template engine (part 3, v0.1.167 — `{{key}}` substitution over `include_str!`-embedded templates, `--runtime`/`--name`/`--namespace`/`--path`/`--force`, exit-2 on existing without `--force`); scaffold wizard wired into `app add` step 0 + interactive runtime picker (part 3b, v0.1.168). Real-cluster manual walk surfaced walk-fixes post-B.1.79b #1–#4 (v0.1.162–165: app-open label resolution off the operator's label, kubectl stderr surfacing on early exit, `app status --resources` child workload state, Cargo.lock/bun-smoke sync) and post-Part-3b #1–#6 + #11 (v0.1.169–174 + platform-stack 0.1.48 / argocd-cue-cmp v0.1.6: scaffold UX/SPDX/namespace/cred-hint polish, `spec.source.path` absolute-path normalisation, CUE-import drop, schema inline→vendored CUE module under `apprafter/cue.mod/`, image-ref derived from git origin, CMP entrypoint cds into the package dir); each landed with a regression-guard test. Design divergences (accepted): the template engine ships 2 consolidated `.cue.hbs` (default + blank) instead of 12 per-runtime files (per-runtime port defaults baked into `defaults_for`); the multi-stack default picks first-High-in-order rather than alphabetical. Deferred to Part 4 (not yet shipped): `examples/applications/` reference manifests, `docs/user-guide/cli/app-scaffold.md`, the quickstart update, `spec.resources`/`spec.healthcheck` template fields (v1alpha1-only for now), `--lang ru\|en` template comments (English-only). The SourceCredential private-repo credential flow (ADR 0039) was carved out as the separate new item 1.79c — it does NOT replace 1.79b. | v0.1.174 + platform-stack 0.1.48 + argocd-cue-cmp v0.1.6 |
| 2026-05-31 | M1.5 (SR pull-up) item **1.79c closure** — private-repo credential flow via the `SourceCredential` CRD (ADR 0039). One gated, plaintext-free credential path covering git-read (Argo CD repo-cred) + registry-pull (kubelet dockerconfigjson) from a single config-only CR, repairing three defects of the 1.79a raw-resource flow (ungated raw Argo Application + repo-creds Secret; plaintext token at rest; missing pull-secret). **Sliced S0–S5.** S0 (v0.1.175): bitnami sealed-secrets controller component + native-Rust client-side seal (RSA-OAEP-SHA256 ⊗ AES-256-GCM, strict scope, bitnami wire-format; cert fetched over the kube API service-proxy) + `apprafter secret seal`. S1 (v0.1.176): `SourceCredential` CRD across CUE + kube-rs + OpenAPI v3 + admission validator + bootstrap CRD-wait. S2/S3 (operator): new `operator-controllers/sourcecredential` crate deriving prefix-matched Argo `repo-creds` (in `argocd`) + canonical host-matched `dockerconfigjson`, with **Seam A** — the Application controller host-matches the rendered image, projects a per-workload pull-secret copy, and sets `Deployment.imagePullSecrets`. S4 (v0.1.177): CLI `repo creds` becomes a thin front-end over `SourceCredential` (seal + CR; `list`/`show` read `.status` only; `rotate` re-seals; `remove` behind the reverse-dep gate); legacy raw repo-creds cut over. **Real-cluster walk-fixes #1–#6** (CLI v0.1.179–183; operator v0.1.136 / platform-stack 0.1.50): #1 seal strict-scope label needs the `namespace/name` slash; #2 inferred ghcr registry host lowercased (OCI refs are lowercase, operator host-match is case-sensitive); #6 the Argo cascade finalizer was misspelled `resources-finalization` → corrected to `resources-finalizer.argocd.argoproj.io/background` (a research workflow over the Argo docs found the one-morpheme typo; the dead-end #3/#5 workarounds were reverted, leaving the clean split — Argo cascade-prunes the synced CR, the operator GCs the Deployment via ownerReference, the CLI manages only the Argo Application) + the #4 operator `deletionTimestamp` reconcile guard. First acceptance validated end-to-end (private repo clone + private ghcr pull + clean removal). **S5 (operator v0.1.137 / platform-stack 0.1.51 / CLI v0.1.184):** live validity probe (a representative repo/image from a matching Argo / AppRafter Application probed over git smart-HTTP / a scoped registry v2 token exchange — coverage is a prefix, so a concrete object is needed; conservative mapping, a blocked egress reports `Unverified` not `Invalid`); coverage-gate `present`|`confirmed` (`app add --coverage-gate`); derived-Secret GC finalizer (cross-namespace Secrets have no ownerReference cascade); scoped CLI credential-author Role seed (unbound; no read on derived Secrets); `SourceCredentialMigrationStrategy.detect_destructive` classifier (live plan-creation wiring co-deferred with application-scope B.1.77). Deferred with rationale: config-repo (GitOps) delivery mode (opt., → Phase-3 Backstage). | v0.1.184 + operator v0.1.137 + platform-stack 0.1.51 |
| 2026-06-01 | M1.5 **scoped close** — items 1.81 (e2e suite) + 1.82 (docs). **1.81:** modernised the e2e harness on the project's local **k3d** cluster. Shared `e2e/lib.sh` (phase/elapsed/require_env/retry/k3d_up/down + an `apprafter()` cargo-run wrapper); `e2e/mvp.sh` rewritten 9-step → 3-step (`init` → `bootstrap-all` → Application smoke, real Hetzner, nightly) with a new assertion that the platform components are Argo CD-managed (`applications.argoproj.io` Synced); `e2e/gitops-walk.sh` (k3d) proves the CMP-render → Argo-sync → operator-reconcile loop end to end against a fixture app served by a local `git daemon`; `e2e/migration-platform.sh` (k3d) stands up a `--registry-create` local OCI registry, publishes a 0.2.0(safe)+0.3.0(breaking) platform-stack pair from the dist tree, pins 0.2.0, and asserts the breaking publish creates a platform-scope MigrationPlan that `apprafter migration approve` advances / `reject` reverts. Wired into `Justfile` (`e2e`/`e2e-gitops`/`e2e-migration-platform`) + a PR-triggered `e2e-k3d.yml` (renders the dist chart first); `mvp.sh` stays nightly. **Scoped out:** `e2e/migration-app.sh` deferred to Phase 2 (`needs.pg` end-to-end provisioning — `needs` is removed from the v1alpha1 surface); `e2e/fork.sh` dropped (item 1.80 not built). The k3d scripts are `shellcheck`-clean + CI-wired but not yet run green (the assistant sandbox cannot run k3d — Podman without `/var/run/docker.sock`); the first green CI run is the M1.5 closure verification (spec.md §6 milestone box flips then). **1.82:** rewrote `operator-guide/quickstart.md` to the 3-step Argo-CD-managed flow (CX22→CPX22 factual fix, Application-CRD smoke), added `platform-management.md` (PlatformStack lifecycle, channels, upgrade/freeze/rescue; fork = one deferred-note line), `migration-plans.md` (destructive change, approve/reject by scope per ADR 0027), `dev-guide/application-cue.md` (Application.cue authorship + CMP + multi-env), updated `gitops-walk.md` for the Application-CRD path, refreshed `mkdocs.yml` nav + README links; `mkdocs build --strict` clean. 1.80 (`platform fork`) + 1.9 (Dev Mode, SR bucket D) explicitly deferred. | e2e+docs commits `bfcd95d`/`a7a4051`/`690051f`/`6921670`/`a7efe69` (no monorepo tag — test+docs only) |
| 2026-06-02 | M1.5 1.81 **e2e CI-gating round** (bootstrap walk-fixes + migration-platform descope). The first real `e2e-k3d` runs surfaced three latent bootstrap bugs the assistant sandbox could never have caught: (1) **AppProjects missing** — argo-cd chart 7.7.7 silently ignores `configs.projects`, so a fresh bootstrap deadlocked the root `platform` Application on `InvalidSpecError: project platform does not exist`; `cluster-bootstrap` now SSA-applies the three AppProjects (default/platform/platform-providers) directly between the argocd-server wait and the root-App apply (`render_app_projects()`), byte-mirroring the umbrella's sync-wave -30 projects (v0.1.188). (2) **admission-webhook race** — Argo syncs the `platformstacks` CRD and the webhook as separate child Applications, so the default-PlatformStack SSA apply at bootstrap step 5 raced the webhook's Endpoints (`no endpoints available for service admission-webhook`); step 5 now retries 30×10s past webhook startup (v0.1.189). (3) `e2e/lib.sh bootstrap_with_retry` no longer `helm uninstall`s argocd on failure (it orphaned argocd-server so the retry's install couldn't adopt it). With those fixed, bootstrap + `gitops-walk.sh` reach green; the run then exposed that **`migration-platform.sh` is unrunnable on k3d** — descoped (Option C). Root cause is environmental, not a gate bug: the PlatformController's OCI client is HTTPS-only (`oci-distribution ClientConfig::default()`) while k3d's `--registry-create` registry is plain HTTP, so the in-cluster controller cannot pull fixture compat-docs → transition classification fails → no MigrationPlan, even at CR level. Compounding: PlatformController propagates only `targetRevision`+`helm.valuesObject` to the parent App (not `repoURL` — real redirected-registry deploy is fork story 1.80, deferred), and patching `targetRevision` to a ghcr-absent version wedges the in-flight gate; the original script was also written against a wrong trigger model (availability-auto-creates-plan vs the shipped pin→breaking). The MigrationPlan gate (1.78) itself is correct and stays covered by operator unit + integration tests; a real-infra (HTTPS-registry, pin-driven) migration walk is a tracked nightly-Hetzner follow-up. Script deleted; `Justfile e2e` = `e2e-gitops` only; `e2e-k3d.yml` drops the dist-render step. Follow-ups noted: env-gated insecure/HTTP OCI in the controller (dev-mode/1.9), and `apprafter migration approve` poking PlatformStack for prompt post-approve bump (today it lands on the next reconcile, ≤`checkInterval`, since PlatformController doesn't watch MigrationPlan). CLI 0.1.187 → 0.1.188 → 0.1.189 | v0.1.188, v0.1.189 |
| 2026-06-02 | **M1.5 — Self-managing platform rethink — CLOSED.** The `e2e-k3d` gate went green (run `26815926016`, `gitops-walk.sh`, 3m16s): `cluster-bootstrap` brought up Cilium + Argo CD + the platform-stack 0.1.52 umbrella with `PlatformStack/default` (tier=1), then `apprafter app add` → CUE CMP render → Argo CD Synced/Healthy → operator-reconciled Deployment Available, and a source change (replicas 1→2) committed to the fixture git daemon propagated end to end ("Loop proven: CMP render → Argo sync → operator reconcile"). Per the established closure criterion (plan.md 1.81 acceptance) this first green run flipped **spec.md §6 Milestone M1.5 → ✅** (13 of 15 boxes checked) and bumped the spec **Revision 9 → 10**. Two boxes deferred with rationale: `apprafter platform fork` (item 1.80, power-user OCI fork — no launch dependency) and the Backstage MigrationPlan queue plugin (M3 portal track); `platform channel` → M2. The CI-fix that unblocked this run: `just` is now installed from the casey/just GitHub release (pinned 1.51.0) instead of the just.systems installer CDN, which 403'd GHA egress (run 26815686851). No architecture change — M1.5's design was actualized in spec Rev 7; this is the formal closure marker. Next: SR 2.1 (ServiceProvider CRD) opens Phase 2. Milestone tag `v0.2.0-self-managing` is the user's call (spec/plan/changelog bookkeeping commit carries no monorepo patch tag) | spec Rev 10 (docs-only) |
| 2026-05-24 | M1.5 walk-fix #12 post-B.1.79a — the `apprafter app add` wizard defaults the destination namespace to `apprafter`. After walk-fix #11 permitted `Namespace` in the `apps` AppProject, landing apps still failed with `namespaces "apprafter" not found`. Root cause: the wizard's `build_application_manifest` hardcoded `destination.namespace = name` (app-name-based), so the first sync created an orphan `Namespace/apprafter-landing-web` (`CreateNamespace=true` creates ONLY the destination namespace, never the manifest's own) and then failed on applying the Application CR to the manifest's `metadata.namespace: apprafter` (which did not exist). Fix: a new `--namespace` flag on the `AppCommand::Add` clap surface with default `apprafter` + a wizard sixth prompt "Destination namespace" with DNS-1123 validation; `build_application_manifest()` receives an explicit `destination_namespace: &str` parameter (name-based defaulting removed); a `DEFAULT_DESTINATION_NAMESPACE = "apprafter"` constant in `app_wizard.rs` as a load-bearing anchor (anchored unit test). +4 new unit tests (renamed `..._routes_to_argocd_namespace` → `..._routes_argocd_cr_to_argocd_ns_destination_to_param`, new `..._destination_namespace_honours_explicit_caller_value`, `default_destination_namespace_constant_is_apprafter`, `validate_dns_1123_for_namespace_accepts_kubernetes_namespace_shapes`). Folded in fmt-fix `a500144` (style-only — the pre-commit local gate ran clippy+test but skipped `cargo fmt --check`, CI lint flagged two call sites that fit on one line after rustfmt; updated `feedback_cue_bin_local.md` memory with an explicit "clippy ≠ fmt; run all three gates in a single `&&`-chain"). Existing user apps require a one-off `kubectl -n argocd patch applications.argoproj.io <name> --type=merge -p '{"spec":{"destination":{"namespace":"apprafter"}}}'` migration step (documented in UNRELEASED.md). CLI 0.1.159 → 0.1.160. Chart unchanged | v0.1.160 |
| 2026-05-24 | M1.5 walk-fix #11 post-B.1.79a — `Namespace` cluster resource permitted in the `apps` AppProject. After walk-fix #10 unblocked CMP discover, the landing apps progressed further and failed with `SyncFailed: resource :Namespace is not permitted in project apps`. Wizard-generated user Applications carry `syncOptions: [CreateNamespace=true, ServerSideApply=true]`; Argo CD on the first sync generates a synthetic `Namespace` resource for the destination namespace and tries to apply it as cluster-scoped. The `apps` AppProject's `clusterResourceWhitelist: []` (fully closed, B.1.79a part 1 baseline) blocked any cluster resource including Namespace. Fix: narrow the whitelist to `[{group: "", kind: "Namespace"}]` — only Namespace, nothing else cluster-scoped reaches the user-app surface (no CRD installs, no ClusterRole/Binding, no PV). Phase 4 AccessGrant will lay runtime RBAC on this structural foundation. Caveat — orphan destination namespace: the wizard's default `destination.namespace = <app-name>` does not match the manifest's `metadata.namespace`; after walk-fix #11 the sync passes but the destination ns remains empty (the Application CR lands in its own manifest namespace per the manifest's metadata, where the operator creates the Deployment+Service). Wizard polish for defaulting the destination to the manifest's namespace is deferred to walk-fix #12. Compat entry `compatibility: "0.1.47"` shipped with a full diagnosis. No CLI bump per the v0.1.159 channel-latest design — the PlatformController polls upstream and rolls existing installs; new bootstraps pick it up through `resolve_latest_platform_stack_version()`. Chart 0.1.46 → 0.1.47 | platform-stack 0.1.47 |
| 2026-05-24 | M1.5 walk-fix #10 post-B.1.79a — CMP discover stdout fix (cue-cmp v0.1.4 → v0.1.5, chart 0.1.45 → 0.1.46). After walk-fix #8 (command-based discover) + walk-fix #8b (cue-cmp v0.1.4 image rebuild), landing apps STILL failed with the same `Failed to unmarshal "package.json": Object 'Kind' is missing` (cached). After `kubectl exec deployment/argocd-redis -- redis-cli FLUSHDB` + the `argocd.argoproj.io/refresh=hard` annotation on each Application, the error reproduced identically — not a stale-cache artefact, the CMP plugin genuinely did not match the repo. Diagnosis: `kubectl -n argocd logs deployment/argocd-repo-server -c cue-cmp` showed the discover shell snippet running, exiting 0, but each invocation was followed by `Plugin command returned zero output`. Root cause: Argo CD's `MatchRepository` in `cmpserver/plugin/plugin.go` keys on the **stdout** of the discover command, not the exit code; the v0.1.4 snippet piped `find -print -quit` through `grep -q .`, and `-q` (quiet) suppresses stdout entirely. Even when find matched the real `landing/web/apprafter/Application.cue`, no output reached runCommand's return value, the plugin was marked `IsSupported: false`, and the directory-mode fallback re-fired the `package.json` parse exactly as before walk-fix #8. Fix: drop the `\| grep -q .` pipe from BOTH branches of the discover snippet in the `argocd-cue-cmp/plugin.yaml` source-of-truth and the chart's `extraObjects` ConfigMap mirror in `platform-stack/cue/component_argocd.cue`. `find -print -quit` itself prints the first matched path on hit, prints nothing on miss; both code paths exit 0. Stdout emptiness IS the signal Argo CD reads. New `argocd-cue-cmp/test-discover.sh` regression test: extracts the discover shell snippet from plugin.yaml directly (no duplication — drift would bite), runs it against 5 fixture directories (parent-dir convention `apprafter/Application.cue` subdir, cwd-is-apprafter depth-1, filename-prefix `apprafter*.cue` at root, plain repo without apprafter files must return empty stdout, generic `.cue` without apprafter prefix must not match) — asserts that stdout non-emptiness matches the expected match/no-match signal, NOT just the exit code. Plus a hard string-match guard `grep -Fq 'grep -q'` fails the test immediately if reintroduced. Wired into `.github/workflows/argocd-cue-cmp-check.yml` between the image build and drift detection. Memory `feedback_cmp_discover_test.md` saved with the rule: every change to plugin.yaml must run test-discover.sh; Argo CD reads stdout not the exit code; specific traps to watch (`grep -q`, `>/dev/null`, `cmp /dev/null -`, etc.). No CLI bump per the v0.1.159 policy. cue-cmp v0.1.4 → v0.1.5 (image rebuild with the baked plugin.yaml), chart 0.1.45 → 0.1.46 (`_components.argocd-cue-cmp.values.image.tag` follows via CUE import + ConfigMap content updated). Compat entry `compatibility: "0.1.46"` shipped | argocd-cue-cmp v0.1.5 + platform-stack 0.1.46 |
| 2026-05-24 | M1.5 walk-fix #9 post-B.1.79a — the CLI resolves the latest platform-stack version from the upstream GitHub Releases API at bootstrap time. The operator asked why the CLI had to bump on every chart-only release; investigation surfaced that `cluster_bootstrap::platform_stack_version()` returned the baked-in `RELEASED_PLATFORM_STACK_VERSION` constant verbatim. The constant tracks `platform.cue`'s `currentVersion` via `cli-providers/build.rs` at compile time. This coupling forced a CLI rebuild + release on every chart bump just to keep fresh installs current — wrong by design (the CLI binary has no business knowing the latest chart version at compile time; the latest chart is whatever is published to the upstream release stream). Fix: a new `cli_providers::k8s::channel_latest` module with a `resolve_latest_platform_stack_version()` entry point. It GETs `https://api.github.com/repos/apprafter/apprafter/releases?per_page=100` with a 3s timeout, filters `tag_name` to entries starting with `platform-stack/v`, strips the prefix, parses as semver, picks the highest with stable preferred over prerelease. Any failure path (network / parse / no matching tags) falls back to the baked constant so air-gapped / firewalled installs still bootstrap. `cluster_bootstrap::platform_stack_version()` now calls the resolver. Stable / prerelease semantics: when both stable and prerelease are published, picks stable; only when ALL candidates are prereleases (fresh channel before the first stable cut) returns one. Uses the GitHub Releases API (the same surface as `version_check.rs`) rather than the OCI registry tag list — no auth token needed, same rate-limit story (the resolver fires only at bootstrap, rare). +7 unit tests on the pure `pick_latest_platform_stack_tag(releases)` helper covering the mixed-streams happy path, the no-platform-stack-tags fork case, malformed entries skipped, non-array defensive, stable vs prerelease precedence, prerelease fallback when no stable, two-digit-patch semver vs lexical regression guard. **Versioning policy update:** going forward chart-only bumps do NOT bump the CLI — the v0.1.158 bump that motivated this question was the last of its kind. Future chart releases are picked up by existing CLI binaries through the new resolver, no CLI rebuild needed. CLI 0.1.158 → 0.1.159. Chart unchanged. cue-cmp unchanged | v0.1.159 |
| 2026-05-24 | M1.5 walk-fix #8b post-B.1.79a — cue-cmp image bump alongside chart 0.1.45. The push of v0.1.157 (which bundled chart 0.1.44 with the new command-based CMP discover in plugin.yaml) tripped the `argocd-cue-cmp-check` workflow's drift detection: `Image source under argocd-cue-cmp/ changed since argocd-cue-cmp/v0.1.3 was published, but version.cue is still 0.1.3`. The walk-fix #8 reasoning that "the ConfigMap mount overrides the image's plugin.yaml at runtime" is correct for chart-installed clusters, but operators who install cue-cmp manually (attaching to a stock Argo CD without the AppRafter chart) get the image's baked-in plugin.yaml — stale image content = wrong behaviour for that path. The drift policy is enforced rightly. Fix: bump cue-cmp v0.1.3 → v0.1.4 (the publish workflow auto-fires on the new tag, rebuilds the image with the new plugin.yaml baked in) + chart 0.1.44 → 0.1.45 (the image pin follows the CUE import) + CLI 0.1.157 → 0.1.158. plugin.yaml content unchanged from walk-fix #8 — same command-based discover. The argocd-repo-server's sidecar image reference flips on chart upgrade, the kubelet rolls the pod, the operator hard-refreshes affected apps. Recovery is the same as walk-fix #8 just with chart 0.1.45 instead of 0.1.44 | v0.1.158 |
| 2026-05-24 | M1.5 walk-fix #8 post-B.1.79a — CMP discover switches from glob to command (chart 0.1.44) + the wizard strips the trailing `apprafter` segment. An operator on chart 0.1.43 registered landing apps via `apprafter app add --path landing/cms`; the Argo CD UI surfaced `Failed to unmarshal "package.json": Object 'Kind' is missing` for both `landing-web` and `landing-cms`. Diagnostics confirmed the chart at 0.1.43, the configmap with the new brace-alternation glob, the `MatchRepository` gRPC calls returning OK (success) but the absence of any downstream `generate` activity implied all match calls returned false. Root cause: chart 0.1.42-0.1.43 plugin.yaml used `discover.find.glob: "{**/apprafter*.cue,**/apprafter/**/*.cue}"` — brace alternation that `doublestar/v4` supports, but Argo CD 2.13.1's vendored doublestar either treats the braces literally or never received v4 brace support. CMP returned no-match; Argo CD fell back to the default directory mode, walked `landing/cms/`, and tried to parse `package.json` as a k8s manifest. Fix: switch `discover.find.glob` to `discover.find.command` (a find-based shell snippet running from the path's directory, exits 0 on match) — handles both conventions in one expression: cwd basename `apprafter` → match any `.cue` at depth 1, otherwise → match any `.cue` inside the `apprafter/` subdir OR with a filename starting `apprafter`. `-print -quit | grep -q .` short-circuits after the first match. No doublestar dependency. The cue-cmp Docker image is unchanged at v0.1.3 — the entire fix lives in the chart-managed `cue-cmp-plugin-config` ConfigMap content. A chart upgrade + argocd-repo-server rollout-restart picks up the new discovery rule. Wizard polish bundled: a new pure `strip_apprafter_tail(rel)` helper strips the trailing `apprafter` segment so `cd landing/cms/apprafter && apprafter app add` defaults to `landing/cms` (the parent-of-apprafter form is the cleaner convention; CMP handles either with the new command-based discover, but the wizard suggests the canonical shape). +5 unit tests on `strip_apprafter_tail`. Operator recovery: `apprafter platform upgrade --to 0.1.44` + `kubectl rollout restart deployment/argocd-repo-server -n argocd` + a hard-refresh of affected apps. CLI 0.1.156 → 0.1.157. Chart 0.1.43 → 0.1.44 | v0.1.157 |
| 2026-05-24 | M1.5 walk-fix #7 post-B.1.79a — migration helper observability + cross-mount safe rename + richer-legacy preference. Two operator-impacting bugs surfaced on the v0.1.155 walk: (a) the operator ran `apprafter app add` from `cli/`, hit `no cached kubeconfig in state` — inspection found `cli/.apprafter/state.json` (5375 bytes, a real hetzner_cloud + kubeconfig) AND `~/.config/apprafter/state/dev/.apprafter/state.json` (412 bytes, a placeholder with round-number IDs server_id=42 firewall_id=200 floating_ip_ids=[300], kubeconfig null). The v0.1.154-155 migration helper's silent "new-wins by existence" branch preserved the placeholder and left the rich legacy file unmigrated, with no stderr signal — the operator had no idea why their real state was not loading. (b) After manually deleting the placeholder per-target file to force migration, hit `Invalid cross-device link (os error 18)`. The initial "different physical partitions" framing was inaccurate: the operator's `/projects/` and `$HOME` were on the SAME btrfs filesystem, just different subvolumes mounted at separate paths (a common NixOS layout). EXDEV fires on ANY mount-point boundary crossing: btrfs subvolumes, ZFS datasets in one pool, bind mounts, overlay/fuse mounts — all return EXDEV on rename across the boundary regardless of whether the bytes physically share a block device. `std::fs::rename` is a single-syscall with no fallback. Three coordinated fixes: (1) richer-legacy preference — when both files exist, parse both and compare hetzner_cloud presence via a new pure `read_has_hetzner_cloud(path)`; if the legacy is richer than the per-target (legacy has hetzner_cloud, target does not), back the placeholder up to `state.json.bak` and migrate the legacy as authoritative. (2) A loud stderr notice on the genuine skip path (both files carry hetzner_cloud — two real clusters case); silent skip removed entirely. (3) A new `cross_fs_safe_rename(src, dst)` helper tries `fs::rename` first, falls back to `fs::copy` + `fs::remove_file` on EXDEV (os error 18 on Linux + macOS); all three rename callsites in the migration helper (state move, known_hosts move, backup rename) routed through it. +2 unit tests: `migrate_legacy_prefers_richer_legacy_when_per_target_lacks_hetzner_cloud` (the exact v0.1.155 walk scenario), `migrate_legacy_keeps_per_target_when_both_carry_hetzner_cloud` (defensive — two real clusters case). Total cli-state migration tests: 5 → 7. CLI 0.1.155 → 0.1.156. Chart unchanged | v0.1.156 |
| 2026-05-24 | M1.5 post-B.1.79a #7 — the `apprafter app add` wizard defaults the path prompt to cwd-relative-to-repo-root. Operator follow-up after the v0.1.154 state migration: registering landing manifests from `/projects/.../apprafter/landing/cms/` had the wizard suggest `/` (the clap default) — the operator had to manually type `landing/cms`, then was unsure whether to suffix `apprafter` (the convention dir name) or not. The fix lands two coordinated UX bits: (1) a new pure helper `detect_path_relative_to_repo_root()` in `app_wizard.rs` shells out to `git rev-parse --show-toplevel`, computes cwd relative to the result, canonicalises both ends (dodges macOS `/var → /private/var` symlinks), returns `Some("landing/cms")` for subdirs, `Some("/")` when cwd IS the repo root, `None` when git is missing or cwd is outside the work tree. The `WizardInputs.detected_path` field carries it; `app::add_via_wizard` populates it via the new detector. The wizard's `run` chooses the path default by precedence: explicit non-default `--path X` → cwd-detect → clap default `/`. (2) Documentation + verification confirms the `apprafter/` subdirectory is implicit — operators can specify the project root (`landing/cms`) instead of the convention dir (`landing/cms/apprafter`); the CMP plugin's discovery glob (`{**/apprafter*.cue,**/apprafter/**/*.cue}` from chart 0.1.42) matches files at any depth, and `cue export ./...` recurses through subdirectories. A local `bash argocd-cue-cmp/entrypoint.sh` against both `landing/cms` and `landing/cms/apprafter` produces identical YAML doc streams. CLI 0.1.154 → 0.1.155. Chart unchanged | v0.1.155 |
| 2026-05-24 | M1.5 polish post-B.1.79a #6 — state migrates from per-cwd `<cwd>/.apprafter/state.json` to per-target `<config>/state/<active-target>/.apprafter/state.json`. Operator complaint: ran `apprafter apply` from the project root then opened a second shell at `landing/cms/` to register the first Application via `apprafter app add` — hit `Error: state has no hetzner_cloud section; run apprafter apply first` even though apply had just succeeded. Root cause: `StatePaths::for_root(&cwd)` was the only constructor; every operational command opened with `std::env::current_dir()? → for_root(&cwd) → State::load_or_default(&paths)`, so the second invocation's cwd had its own (empty) `.apprafter/` directory rather than the one created at the project root. The per-target store scaffolding had existed at `cli-core/src/target.rs:186` (`TargetStorePaths::state_dir(name)` returning `<store>/state/<target>/`) since Track A.2 — that landed Track A.8's "wire the existing per-CWD flow over here" comment but the wiring itself was deferred and never revisited. Fix: a new `StatePaths::for_active_target(&TargetStorePaths, &str)` factory points at `<store>/state/<target>/.apprafter/` (the intermediate `.apprafter` segment kept so the on-disk shape mirrors the legacy layout operators grep for); a new `migrate_legacy_state_if_present(legacy_root, target_paths)` helper moves the v0.1.153 file (and the sibling `known_hosts`) into the new per-target slot on the first run after upgrade, printing one stderr notice; idempotent + new-wins (the legacy file left alone when the per-target slot is already populated). A new shared `commands::state_paths::resolve_state_paths(target_override)` threads the three steps every command repeated: resolve `TargetStorePaths` via `default_config_root()`, resolve the active target name (with `--target` override), build the per-target `StatePaths`, run migration. It returns a bundle carrying the `TargetStorePaths` too so credential resolution downstream skips a redundant env probe. Eager existence check when `--target <name>` is supplied — surfaces `target X not found (available: …)` instead of the much later generic "no provider configured" when operators typo a name. Callsites updated: `apply.rs`, `destroy.rs`, `import.rs`, `kubeconfig.rs`, `argocd_password.rs`, `cluster_bootstrap.rs`, `init.rs`, `status.rs`, `plan.rs`, `k8s_helpers.rs` (the latter fixes the `platform` / `migration` / `app` / `repo creds` / `open` thin wrappers transparently). Dead code removed: the `discover_from` / `discover_or_root` helpers added speculatively for a walk-up-from-cwd approach were never adopted (walk-up cannot help when cwd is `landing/cms/` and state is per-target, not per-project-root). +5 unit tests on the migration helper (`for_active_target_lays_out_state_under_store_state_target`, `migrate_legacy_no_op_when_legacy_absent`, `migrate_legacy_moves_state_when_only_legacy_exists`, `migrate_legacy_leaves_existing_target_state_untouched`, `migrate_legacy_moves_known_hosts_alongside_state`). Integration test fixtures across 6 suites (`import_test`, `kubeconfig_test`, `argocd_password_test`, `aliases_test`, `cli_smoke`, `miette_render_test`) updated to seed `APPRAFTER_CONFIG_DIR` + an active target before running `init` so the per-target state slot is writable; state-file paths in assertions switched from `<cwd>/.apprafter/state.json` to `<cfg_dir>/state/default/.apprafter/state.json`. CLI 0.1.153 → 0.1.154. Chart unchanged. Operator / admission-webhook unchanged | v0.1.154 |
| 2026-05-24 | M1.5 walk-fix #5b post-B.1.79a — the cue-cmp entrypoint accepts both unwrapped and named-wrapper source layouts. The v0.1.152 push triggered the `argocd-cue-cmp-check` CI workflow; the entrypoint smoke step failed `entrypoint did not render Application kind`. Root cause: the walk-fix #5 entrypoint enumerated `to_entries[]` looking for NESTED objects-with-apiVersion+kind (style B — `landingWeb: v1alpha1.#Application & {...}`); the smoke fixture writes the manifest UNWRAPPED at package scope (style A — `apiVersion: ... kind: ... metadata: ... spec: ...` directly), so the top-level `apiVersion` is a string, the filter rejected everything, the loop exited without emitting. Two legitimate CUE source layouts exist (style A single-resource files, style B multi-resource files); walk-fix #5 handled only B. Fix: dispatch before enumeration via a jq check `(type == "object" and has("apiVersion") and has("kind"))` — style A emits `cue export ./... --out yaml` verbatim, style B falls through to the existing per-key logic. Verified locally against both `/tmp/cue-smoke/apprafter/Application.cue` (style A, single YAML doc) and `landing/web/apprafter/` (style B, two docs with a `---` separator). argocd-cue-cmp v0.1.2 → v0.1.3 (the publish workflow auto-fires on the new tag); platform-stack chart 0.1.42 → 0.1.43 picks up the new image tag via the CUE import; CLI 0.1.152 → 0.1.153 | v0.1.153 |
| 2026-05-24 | M1.5 walk-fix #5 post-B.1.79a — argocd-cue-cmp emits a flat manifest stream + accepts the apprafter/ directory layout. The operator asked how to register AppRafter landing manifests (`landing/web/apprafter/Application.cue`, `landing/cms/apprafter/Application.cue`) via `apprafter app add`. Investigation surfaced two unsurfaced bugs in the cue-cmp plugin path (the CMP path had not been exercised end-to-end until this attempt to register the first user app): (1) the CMP discovery glob `**/apprafter*.cue` matches only files starting with the `apprafter` basename — landing manifests named `Application.cue` inside the `apprafter/` directory did not match, Argo CD fell through to default raw-YAML, syncs zero rendered manifests; (2) the entrypoint runs `cue export ./... --out yaml` raw — for typical source files (`landingWeb: v1alpha1.#Application & { ... }`) it produces NESTED YAML with the manifest under the field key, Argo CD treats it as ONE invalid document (no apiVersion at top level) → sync fails. The fix landed across three artefacts: (a) `argocd-cue-cmp/entrypoint.sh` rewritten — enumerates top-level k8s-shaped values (objects with `apiVersion` + `kind`) via `cue export --out json | jq`, re-exports each via `cue export -e <key> --out yaml`, emits a `---` separator per doc; helper top-level values (shared constants, library defs) are silently skipped through the `apiVersion+kind` filter. (b) `argocd-cue-cmp/Dockerfile` adds `jq` to the Alpine base (~600 KiB) for the enumeration step. (c) `argocd-cue-cmp/plugin.yaml` + the chart's embedded plugin.yaml in `component_argocd.cue`'s extraObjects ConfigMap extend the glob to `{**/apprafter*.cue,**/apprafter/**/*.cue}` via doublestar v4 brace alternation — matches both the filename-prefix convention (`apprafter-web.cue`) and the directory-marker convention (`landing/web/apprafter/Application.cue`). Verification: a local `bash entrypoint.sh` against `landing/web/apprafter/` emits two well-formed YAML docs (landing-web + landing-web-preview) with `apiVersion: apprafter.io/v1alpha1` at the top level of each + `---` between them. argocd-cue-cmp image v0.1.1 → v0.1.2 (the publish workflow auto-fires on the new tag); platform-stack chart 0.1.41 → 0.1.42 picks up the new image tag via the `argocd-cue-cmp/version.cue` import + extends the embedded glob; CLI 0.1.151 → 0.1.152 picks up the chart pin via `RELEASED_PLATFORM_STACK_VERSION`. Operator workflow post-upgrade: `apprafter platform upgrade --to 0.1.42` then `apprafter app add <repo-url> --path landing/web/apprafter --name landing-web` works | v0.1.152 |
| 2026-05-24 | M1.5 walk-fix #4 post-B.1.79a — version_check picks the wrong tag series + 24h → 6h TTL. A user ran the v0.1.142 binary after the v0.1.150 release went live; the banner stayed silent. Cache inspection: `{"latest_tag":"platform-stack/v0.1.38","fetched_at_secs":...}` — a chart-stream tag, not a CLI version. Root cause: the monorepo publishes four release streams (canonical CLI `vX.Y.Z`, chart `platform-stack/vX.Y.Z`, landing `landing-vX.Y.Z`, argocd-cmp `argocd-cue-cmp/vX.Y.Z`), and the `/releases/latest` API endpoint returns the most recently CREATED release across all four. v0.1.149's `version_check` took whatever came back; `trim_start_matches('v')` did not strip the `platform-stack/` prefix; `semver::parse` failed silently; the banner was suppressed. Fix: switch the endpoint to `/releases?per_page=30` (newest-first array). A new pure helper `pick_canonical_cli_tag(releases)` walks the array and returns the first `tag_name` whose stripped form parses as a canonical `semver::Version` — all four prefixed streams fail `Version::parse` naturally without an explicit denylist. The cache also gains defensive validation in `read_fresh_cache`: a non-semver `latest_tag` is treated as a cache miss, clearing leftover garbage from pre-fix caches without operators deleting the file manually. TTL 24h → 6h (the project is in active daily development; the 6h dial keeps operators current without burning GitHub API quota). +8 unit tests on the pure helpers: canonicalise_cli_tag for each of the 4 prefix permutations + empty/garbage + pre-release suffix happy path; pick_canonical_cli_tag for the mixed-stream window happy path + no-CLI-tag-in-window pathological + malformed release-object skip + non-array defensive. Total version_check module: 4 → 13 tests. CLI 0.1.150 → 0.1.151. Chart unchanged | v0.1.151 |
| 2026-05-24 | M1.5 walk-fix #3 post-B.1.79a — the release-cli workflow was missing the CUE install. The first push of v0.1.149 triggered the new `release-cli` workflow; all three matrix builds (linux-x86_64, darwin-x86_64, darwin-aarch64) failed identically with `cli-providers/build.rs panicked: neither cue nor nix run nixpkgs#cue is on PATH`. Root cause: `cli-providers/build.rs` shells out to `cue export` at compile time to extract `_loaderValues` + chart versions from `platform-stack/cue/`. GitHub-hosted runners (ubuntu-latest, macos-latest) do not ship cue by default. The fix was already shipping in `lint.yml` + `nightly.yml` — both install cue via the `cue-lang/setup-cue@v1.0.1` action — but the new release-cli workflow forgot to copy that step. Fix: add an `Install CUE` step between the Rust toolchain setup and `cargo build` in the matrix `build` job, pinned to `v0.10.0` so all three CI workflows share one cue version. CLI 0.1.149 → 0.1.150. Chart unchanged. Strictly a CI fix — Rust code unchanged, the CLI binary is byte-identical to 0.1.149 | v0.1.150 |
| 2026-05-24 | M1.5 polish post-B.1.79a #5 — the CLI release workflow. Walk feedback on v0.1.135's npm-style newer-release banner: the banner never fires even when the CLI is many tags behind the tip — root cause: `version_check::resolve_latest_tag` polls `https://api.github.com/repos/apprafter/apprafter/releases/latest` but the monorepo had no workflow creating GitHub Releases on `v0.*` tag pushes. The Releases page is empty → the API 404s → the fail-quiet banner stays silent. Fix: a new `.github/workflows/release-cli.yml` triggers on `v0.*` / `v1.*` tag pushes (plus a manual `workflow_dispatch` for re-builds), builds the `apprafter` CLI binary for 3 targets (x86_64-unknown-linux-gnu native on ubuntu-latest, x86_64-apple-darwin cross-compiled on macos-latest, aarch64-apple-darwin native on macos-latest), packages each into `apprafter-<tag>-<target>.tar.gz` (stripped binary + README + LICENSE) with a sha256 sidecar, creates a GitHub Release with the assets uploaded. A verify step runs `apprafter --version` against the fresh binary and asserts the output matches the tag minus the `v` prefix — catches forgotten Cargo.toml bumps before they ship a broken courtesy banner. The release body is autogenerated by pulling the first `##` changelog section that mentions the tag from UNRELEASED.md; falls back to a short default for chart-only / landing-stream tags without dedicated entries. Security: every potentially-tainted input (inputs.tag, github.ref_name, extracted changelog text) flows through an `env:` binding before reaching the shell `run:` — no `${{ }}` interpolation inside the body, eliminating the action-injection class. The tag-glob trigger constrains ref_name to a known-safe shape. The manual re-run path deletes the existing release first to avoid `gh release create` errors on duplicate asset names. Linux aarch64 deliberately absent — tier-1 ships on x86 only; cross-compile via `cross`/QEMU is a separate complication for Phase 2+. CLI 0.1.148 → 0.1.149. Chart unchanged. First release to land on the GitHub Releases page once tagged + pushed → makes the newer-release banner functional for all future tags | v0.1.149 |
| 2026-05-24 | M1.5 polish post-B.1.79a #4 — `apprafter platform freeze` fallback to the Argo CD Application's targetRevision. Walk feedback from the v0.1.142 closure: `apprafter platform freeze cilium` without `--version` errored with "No effective version found for component 'cilium' in status.componentVersions" because the PlatformController ships in M1.5 without populating `PlatformStack.status.componentVersions` (that field is reserved for a Phase 2 enhancement) — the verb was unusable without manually looking up the version. Fix: a new pure `resolve_effective_pin(stack, argocd_app, component)` helper walks a 3-step fallback chain — (1) `PlatformStack.status.componentVersions.<component>` when present, (2) the Argo CD `Application argocd/<component>.spec.source.targetRevision` (always present for a chart-managed component since the umbrella's `templates/applications.yaml` emits it), (3) a hard error pointing at `--version <v>` + `apprafter platform status`. `freeze()` reads the PlatformStack CR, fetches the Argo CD child Application (404 = `None` treated as "no signal"), then resolves. Source preference order: PlatformStack.status wins over the Argo CD targetRevision because during a bump cycle the Argo CD Application's targetRevision may briefly lag behind the umbrella's reconciled state — the umbrella patches its children's targetRevision on each component cycle, and mid-flight the value reflects the OLD version until the patch lands; PlatformStack.status, when populated, surfaces the operator's intended version. +4 unit tests on the pure resolver: `_prefers_platformstack_status` (both sources present, status wins), `_falls_back_to_argocd_target_revision` (the M1.5-typical state succeeds via the Argo CD fallback instead of erroring — load-bearing for the operator-facing fix), `_errors_when_both_sources_empty` (clean error message with the --version hint), `_handles_argocd_app_without_target_revision` (defensive: a malformed CR with an empty spec.source treated like a 404). Total platform module: 9 → 13 tests. CLI 0.1.146 → 0.1.148 (v0.1.147 taken by the landing-stream release `558acad`; the CLI version skips ahead to 0.1.148 to avoid a tag collision). Chart unchanged | v0.1.148 |
| 2026-05-24 | M1.5 walk-fix #2 post-B.1.79a — AppProjects as standalone umbrella manifests at sync-wave -30. Operator upgrade 0.1.39 → 0.1.40 hit `Unable to refresh admission-webhook: app is not allowed in project "platform", or the project does not exist`. Root cause: chart 0.1.40 (part 1) shipped 4 AppProject definitions via `_loaderValues.argocd.values.configs.projects` — the argocd subchart renders those as AppProject CRs only when it syncs (sync-wave -15). Child Applications at sync-wave 0 (admission-webhook, apprafter-operator, network-policies, backstage, argocd-cue-cmp) reference `spec.project: platform` (the new default from part 1). **Argo CD's app-of-applications model does NOT strictly serialise sync-waves between separate Applications** — only within a single Application's child-resource sync. Across multiple umbrella-managed Applications, Argo CD treats each as an independent reconciler and starts them roughly together. Race: admission-webhook (wave 0) sometimes tries to refresh BEFORE the AppProject `platform` has landed via argocd (wave -15). Fix: ship the AppProject CRs as **standalone manifests in the umbrella chart** at sync-wave -30 (earlier than even Cilium at -20). A new shared `_appProjects` map in `platform-stack/cue/app_projects.cue` carries the 4 definitions; two sites consume it — a new `templates/appprojects.yaml` template (umbrella) emits one `kind: AppProject` per entry at sync-wave -30 on every umbrella sync; `_loaderValues.argocd.values.configs.projects` keeps the same definitions for the initial `apprafter cluster-bootstrap` install before any umbrella sync has run. Both sites describe identical resources (same group/kind/name/namespace); the Argo CD reconciler treats them as one logical object — the duplication is idempotent. A new CUE shape `#AppProjectSpec` constrains entries to the AppProject fields the umbrella actually sets (description, sourceRepos, destinations, cluster/namespace resource whitelists). `#PlatformValues` gains a top-level `appProjects: [string]: #AppProjectSpec`; the tier overlays (tier_solo + tier_team) wire `appProjects: _appProjects`. The fix is structural — fresh installs on 0.1.41+ and 0.1.40 → 0.1.41 upgrades both work without manual intervention. Existing 0.1.40 operators who want immediate refresh-error clearance (instead of waiting for the chart-pull + reconcile cycle) can apply the 4 AppProjects inline via `kubectl apply -f` — an optional unblocker, not a workaround. CUE renders cleanly; `helm template` against the rendered chart emits 4 AppProject CRs at sync-wave -30 alongside the existing child Applications. Chart 0.1.40 → 0.1.41. CLI 0.1.145 → 0.1.146 (the CLI picks up the chart bump via `RELEASED_PLATFORM_STACK_VERSION`). Operator chart unchanged | v0.1.146 |
| 2026-05-24 | M1.5 polish post-B.1.79a #3 — wizards for `apprafter app add` and `apprafter repo creds add`. Same gate as the `target add` wizard from v0.1.76: fires when stdin + stdout are TTYs AND `--no-interactive` is not set; pre-fills every prompt from the flag value or auto-detected context; falls through into the non-interactive code path with `no_interactive=true` to avoid recursion. `app add` wizard prompt order: Git URL (Text, default = the `--git-url` flag or the cwd's `git remote get-url <--remote>` normalised through `normalise_git_url`), Application name (Text, DNS-1123 validated inline, default = `--name` or `derive_app_name(url)`), Branch / revision (Text, default = `--branch` or `git symbolic-ref --short HEAD` or `main`), Path (Text, default `/`), AppProject (Select: apps/platform/platform-providers, default = `--project`). Two pure helpers `detect_git_origin(remote)` / `detect_git_branch()` shell out to `git` non-fatally — a missing binary / non-repo cwd / unconfigured remote returns `None` so the wizard works from any cwd. `repo creds add` wizard prompt order: Friendly name (Text, DNS-1123), URL prefix (Text, validated for scheme+host shape — accepts `https://...` / `git@host:...`, rejects bare paths that could never prefix-match an Argo CD repoURL), Auth type (Select: pat/basic), Username (Text, default `git`), Token (Password masked via `PasswordDisplayMode::Masked`, `without_confirmation`); inline provider-aware format validation runs as the operator types unless `--no-validate` flowed through. New CLI flags `apprafter app add --no-interactive` and `apprafter repo creds add --no-interactive`. Visibility bumps: `repo_creds::AuthType` and `validate_token_format` promoted to `pub` so the wizard can reuse the rules without duplication. +11 unit tests across `app_wizard` (5) + `repo_creds_wizard` (6); coverage: TTY gate, DNS-1123 happy/sad, URL prefix happy/sad (bare-path rejection load-bearing), non-empty trim, parse_auth_type_safe garbage fallback. CLI 0.1.144 → 0.1.145. Chart unchanged | v0.1.145 |
| 2026-05-24 | M1.5 polish post-B.1.79a #2 — `apprafter platform status` UX (dates / sort / width). Walk feedback: raw RFC3339 timestamps unreadable at a glance ("when did that check happen?"), the `versionHistory` table ordered by JSON document position rather than `appliedAt` desc (brittle across server-side re-sorts), the Conditions table sprawled past 80-col terminals because the old heuristic wrapped MESSAGE to a flat 60 chars without accounting for total layout width. Three coordinated fixes: (1) a new pure `format_timestamp_with_relative(raw, now)` helper parses RFC3339 via `chrono`, renders as `"2026-05-24 14:30 UTC (2 hours ago)"`; the humanise scale uses operator-relevant granularity (under 45s → "just now", under 1h → minutes, under 1d → hours, under 30d → days, under 1y → months, else years); unparseable input surfaces verbatim so audit data is never lost. (2) a new `collect_history_rows(status, now, take)` sorts by parsed `appliedAt` desc using `DateTime::parse_from_rfc3339`; entries with unparseable timestamps sink to the bottom so corrupt / mid-write CRs do not dominate the visible head. (3) a new `render_conditions_table(conditions)` queries `terminal_size::terminal_size()` for the TTY width, computes a budget by subtracting non-MESSAGE column max widths + separator overhead, wraps MESSAGE to the remainder; falls back to 100 columns when stdout is piped. New workspace deps: `chrono` (clock+serde, no default features) and `terminal_size`. The `print_status` signature gains a `now: DateTime<Utc>` parameter so tests can pin a deterministic reference moment for relative-date assertions. +7 unit tests on the pure helpers (format_timestamp_renders_absolute_and_relative, format_timestamp_handles_unparseable_input, humanise_relative_uses_just_now_under_45_seconds, humanise_relative_handles_minutes_hours_days_months_years span coverage, collect_history_rows_sorts_by_applied_at_desc, collect_history_rows_puts_unparseable_timestamps_last, collect_history_rows_caps_at_take). Total platform module: 2 → 9 tests. CLI 0.1.143 → 0.1.144. Chart unchanged | v0.1.144 |
| 2026-05-24 | M1.5 polish post-B.1.79a #1 — CLI Cyrillic → English sweep. Walk feedback on the B.1.79a closures pointed out that CLI output, comments, and error messages in the new modules (`app.rs`, `repo_creds.rs`, `platform.rs`, `open.rs`, etc.) contained mixed Cyrillic/English text: both fully-Russian sentences (e.g. "No effective version found for component 'cilium' in status.componentVersions") and Cyrillic letters used as Latin look-alikes inside English text (Cyrillic glyphs visually resembling the Latin letters in words like `to`, `and`, `with`, and `a`). Some shells in CI / container environments fail to render Cyrillic correctly, and mixed-language strings break the grep filters used by `apprafter doctor`/log scraping tools. Mass translation across 10 CLI files (cli.rs 101 → 0 Cyrillic occurrences, main.rs 2 → 0, commands/app.rs 121 → 0, commands/repo_creds.rs 74 → 0, commands/open.rs 35 → 0, commands/platform.rs 31 → 0, commands/cluster_bootstrap.rs 9 → 0, commands/version_check.rs 7 → 0, commands/migration.rs 6 → 0, commands/k8s_helpers.rs 4 → 0) + a bonus single Cyrillic word stripped from cluster_smoke_test.rs. Repo-wide `grep -cP '\p{Cyrillic}'` returns 0 across the entire `cli/` tree. Standardised common phrases: confirmation prompts read `Confirm?`, cancellation prints `Cancelled.`, hints prefixed `Hint:`. Test names and assertion messages translated alongside production code so test bodies match the new English error strings. CLI 0.1.142 → 0.1.143. Chart unchanged | v0.1.143 |
| 2026-05-22 | M1.5 Track B.1.79a **closure** + leftovers from 1.79 — `apprafter platform freeze/unfreeze/rescue`. `freeze <component> [--version <v>]` patches `PlatformStack.spec.overrides.<component>.pin`; without `--version` it reads the effective version from `status.componentVersions.<component>` and locks that. The CRD schema (`schemas/v1alpha1/platformstack.cue`) already supports the `overrides` map (B.1.74 era), so no chart-side change is required. `unfreeze <component>` — an RFC 7396 merge-patch with null strips the `overrides.<component>` entry; the component falls back to the chart's curated pin (per umbrella). `rescue [--yes]` — a thin wrapper over `apprafter cluster-bootstrap` with a recovery confirmation banner ("re-apply Cilium / Argo CD / CRDs / operator chain as the initial bootstrap — all Applications will lose Sync/Healthy for several reconcile cycles"); use case: Argo CD self-adopt stuck. A non-interactive shell without `--yes` → refuses. **1.79a closure**: AppProjects + project field (part 1), `open argocd` polish (part 2), `app add/list/status/remove` (part 3), `app logs/rollback` (part 4), `repo creds add/list/show/rotate/remove` (part 5), `platform freeze/unfreeze/rescue` + context-aware error hints + plan.md acceptance boxes (this commit). Deferred with rationale: `platform channel` (Phase 2 multi-channel), interactive wizards (batched with future `target add` wizard polish), inline PAT prompt (hint sufficient), CMP plugin user-app project default (Phase 2 concern), `open backstage/grafana/hubble` (Tier 2+ services). Per the CLAUDE.md phase-closure discipline, the plan.md acceptance boxes reflect what shipped vs deferred; a manual walk against a real Hetzner cluster confirmed (see the 0.1.136 walk-fix). CLI 0.1.141 → 0.1.142. Chart unchanged | v0.1.142 |
| 2026-05-22 | M1.5 Track B.1.79a part 5 — the `apprafter repo creds` subcommand family (add/list/show/rotate/remove) with the alias-free top-level `apprafter repo creds`. Closes the private-repo onboarding loop — operators no longer hand-write YAML for the Argo CD repo-creds Secret. Per the Argo CD declarative-setup contract, writes the Secret in the `argocd` namespace with the labels `argocd.argoproj.io/secret-type: repo-creds` (load-bearing — the repo-server filters by it) + `apprafter.io/managed-by: apprafter` + `apprafter.io/cred-name: <name>`, the annotations `apprafter.io/auth-type: <pat|basic>` (consumed by `rotate` for re-validation) + `apprafter.io/source: cli`, `stringData` (NOT `data` — kubectl base64-encodes server-side) with `url` / `username` / `password`. The Argo CD repo-server prefix-matches the Application's `spec.source.repoURL` against the registered `url` — register `https://github.com/myorg` → every `myorg/*` repo inherits the creds automatically. `add <name>`: `--url-prefix` required, `--type pat\|basic` (`ssh` rejected with a Phase 2 deferral hint), `--username git` default, `--token` via flag/env/masked-stdin-prompt; provider-aware regex validation (`github_pat_*` 80+, `ghp_*` exactly 40, `glpat-*` 20+, generic 20+ fallback for Gitea/Forgejo), `--no-validate` skip. Pre-flight refuses on a name collision. `list`: table NAME/URL PREFIX/TYPE/USERNAME. `show <name>`: detail with masked password + a pointer to `kubectl get secret -o jsonpath ...` for plaintext decode. `rotate <name>`: in-place merge-patch on `data.password` (base64-encoded since patching `data` not `stringData`); re-validates against the recorded auth-type annotation; the repo-server picks it up on the next pull cycle. `remove <name>`: dependency check by default via the pure helper `find_apps_matching_prefix(applications_json, url_prefix)` (refuses if Application(s) have a prefix-matching `spec.source.repoURL`); `--force` skips the dependency check, `--yes` skips only the confirmation prompt. The inline PAT prompt in `apprafter app add` remains deferred (a sufficient surface hint pointing at `repo creds add` is already in v0.1.139's auth-failure message). +18 unit tests on the pure helpers: parse_auth_type happy/ssh-deferral/unknown, validate_token_format for each provider format (github fine-grained 80+, ghp_ exactly 40 reject both shorter/longer, glpat-, generic 20+, short refuse with --no-validate hint, basic accepts any non-empty), validate_dns_1123 for the creds name, build_repo_creds_secret carries the secret-type label + managed-by + cred-name labels + routes to the argocd namespace + uses stringData without the data field, find_apps_matching_prefix filters by repo URL prefix + skips helm-only apps without repoURL. CLI 0.1.140 → 0.1.141. Chart unchanged | v0.1.141 |
| 2026-05-22 | M1.5 Track B.1.79a part 4 — `apprafter app logs/rollback`. Closes the delete-debug-revert loop without an operator detour to raw kubectl / the Argo CD UI. `app logs <name>`: a wrap over `kubectl logs` scoped to the workload namespace from the Application CR's `spec.destination.namespace`. Default — selector mode `-l app.kubernetes.io/instance=<app-name>` (Argo CD's documented standard label stamped on every child resource); `--pod <name>` overrides the selector with a direct pod name. Selector mode additionally emits `--prefix=true` (distinguish multi-pod stream lines) + `--max-log-requests=10` (cap the fan-out). Pod mode drops both for clean single-pod output. Flags: `-f/--follow`, `--tail <N>` (`-1` default = no limit), `--container <c>` (disambiguate multi-container pods — kubectl's error message surfaces verbatim when required). Pure helpers `build_kubectl_logs_target(app_name, pod)` + `build_kubectl_logs_args(target, namespace, follow, tail, container)` separate arg-construction from the process spawn for exhaustive testability. `app rollback <name> [--to <rev>]`: reads `status.history` (Argo CD's chronologically-ordered list, newest last); without `--to` picks the second-to-last entry's `revision` via the pure `pick_previous_revision(app)` helper, with `--to <rev>` uses the explicit value verbatim. Pre-flight refuses when the target revision matches the current `spec.source.targetRevision` (no-op). Interactive `inquire::Confirm` (default No) or `--yes` non-interactive. Patches `spec.source.targetRevision` via a JSON merge-patch; Argo CD's automated sync rolls forward on the reconcile cycle. +6 unit tests (target selector default, target pod override, args selector form with prefix+max-requests, args pod form drops prefix, pick_previous_revision happy 3-entry history, pick_previous_revision errors on 0/1/missing history with a pointer to the --to flag). Total app crate: 12 → 18 tests. CLI 0.1.139 → 0.1.140. Chart unchanged | v0.1.140 |
| 2026-05-22 | M1.5 Track B.1.79a part 3 — `apprafter app add/list/status/remove`. A new CLI subcommand family with the alias `apprafter a` for the user-application lifecycle. `app add [<git-url>]`: without an argument it detects the git origin from cwd (`git remote get-url <--remote>`, default `origin`), normalises the URL to HTTPS (SCP-style `git@host:org/repo` → `https://host/org/repo`, `ssh://git@host/...` → `https://host/...`, strips `.git`); options `--name` (default — derived from the repo basename, lowercased, invalid chars → dashes, validated DNS-1123), `--branch` (default — the cwd current branch if detectable, otherwise `main`), `--path` (default `/`), `--project` (default `apps`), `--remote` (default `origin`), `--no-ping` (skip the `git ls-remote` reachability check for air-gapped CI). The reachability check via `git ls-remote --exit-code <url> HEAD`; auth failures surface a CLI hint pointing at `apprafter repo creds add` (lands in v0.1.141). Pre-flight refuses when the Application already exists with a pointer to `app status`/`app remove`. Writes the Argo CD `Application` CR in the `argocd` namespace with `metadata.labels.apprafter.io/managed-by: apprafter` (load-bearing — the `list` filter relies on it), `metadata.annotations.apprafter.io/source: cli`, `spec.project`, `spec.source.{repoURL,path,targetRevision}`, `spec.destination.{server: kubernetes.default.svc, namespace: <app-name>}`, `spec.syncPolicy.automated.{prune: true, selfHeal: true}` + `CreateNamespace=true,ServerSideApply=true` syncOptions. `app list`: `--project` (default `apps`) / `--all-projects` (`conflicts_with`) / `--all-managed` (drops the `apprafter.io/managed-by` label filter); table NAME/PROJECT/REPO/REV/SYNC/HEALTH; an empty result surfaces a context-aware hint pointing at `--all-managed`. `app status <name>`: detail view (project/repo/revision/path/destination namespace/sync state/health) + recent revisions (the last 3 from `status.history` reversed); handles status-less fresh CRs without a panic. `app remove <name>`: interactive `inquire::Confirm` (default No); refuses in a non-interactive shell without `--yes`; `--keep-data` flips `syncPolicy.automated.prune: false` via a merge-patch BEFORE `kubectl delete` so Argo CD tears down only the CR leaving child resources (PVCs/ResourceClaims) for re-attach. Deferred: `app logs`/`app rollback` → v0.1.140; inline PAT prompt → v0.1.141; AppRafter Application CR conditions + the pending MigrationPlan section in status → v0.1.140/Phase 2. +12 unit tests on the pure helpers (normalise_git_url for https/scp-style/ssh schemes + dotgit strip, derive_app_name last-segment + invalid char sanitisation, validate_dns_1123 happy/sad paths, build_application_manifest managed-by label + argocd namespace destination + project/revision carry, print_status status-less fresh CR without a panic). CLI 0.1.138 → 0.1.139. Chart unchanged | v0.1.139 |
| 2026-05-22 | M1.5 Track B.1.79a part 2 — `apprafter open argocd` polish. `OpenUi::Argocd` extends with `--project <name>` (default `apps`) + the `--all-projects` flag (`conflicts_with = "project"`). A new pure `build_argocd_url(local_port, project_filter)` helper renders `https://localhost:<port>/applications?proj=<name>` or the bare base URL when the filter is `None`/empty. The password is copied to the system clipboard via a new `arboard` workspace dep (`default-features = false`); fail-quiet on headless / no-clipboard environments — `(copied to clipboard)` / `(clipboard unavailable — copy manually)` distinct trailing markers per the `ClipboardStatus` enum. Output banner formalised: `Opening Argo CD UI…` heading + `URL` / `Username` / `Password` + clipboard marker + blank line + `✓ Browser opened` / `ℹ Browser open failed — paste the URL into your browser` + `ℹ Press Ctrl+C to stop port-forward`. The URL username pre-fill (`?username=admin`) was checked empirically against Argo CD 7.7.7 — not supported; left as display + clipboard only (negative-result closure). `apprafter open backstage` deferred to Tier 2+. +3 unit tests on `build_argocd_url` (None default, explicit filter rendering for `apps`/`platform`, empty-string defensive). CLI 0.1.137 → 0.1.138. Chart unchanged | v0.1.138 |
| 2026-05-22 | M1.5 Track B.1.79a part 1 — AppProjects + a per-component project field. The `_loaderValues.argocd.values.configs.projects` map grows from a single `default` entry → 4 (default + platform + platform-providers + apps). `platform` for the core chart components (cilium/argocd/cert-manager/network-policies/apprafter-operator/admission-webhook/backstage/argocd-cue-cmp), `platform-providers` for Phase 2+ ServiceProviders, `apps` for user Applications with a tightened whitelist (`destinations.server: https://kubernetes.default.svc`, `clusterResourceWhitelist: []`, `namespaceResourceWhitelist` limited to `apprafter.io/Application` + `ConfigMap` + `Secret` + `HTTPRoute`). `default` retained as the legacy + ad-hoc fallback. The AppProjects ship in the initial Argo CD install via the loader values → they exist before the first Application with a new project syncs. New `#Component.project: string & =~"^[a-z0-9][a-z0-9-]{0,62}[a-z0-9]$" | *"platform"` (DNS-1123 constrained, default `platform`). The render_tool.cue template emits `spec.project: {{ default "platform" $component.project | quote }}`. The CLI loader's root platform Application (`cluster_bootstrap::render_root_application`) moved from `project: default` to `project: platform` — safe because the AppProject ships in the initial install. RBAC enforcement via the AppProject sourceRepos/destinations/resourceWhitelists is NOT activated in M1.5; the visual role (the UI selector groups Applications by project) + the foundation for Phase 4 AccessGrant enforcement. Upgrade: existing operators 0.1.39 → 0.1.40 see every chart-managed Application drift `spec.project` from `default` to `platform` — a metadata-only change, no pod restart, no resource churn, Argo CD reconciles via the normal sync path. +1 regression test (`render_root_application_joins_platform_app_project`). CLI 0.1.136 → 0.1.137; platform-stack 0.1.39 → 0.1.40. Operator chart unchanged | v0.1.137 |
| 2026-05-22 | M1.5 Walk-fix #1 post-B.1.79 — `apprafter open argocd` SIGPIPE early-exit. Walk of v0.1.135: the command printed the credentials banner and immediately exited to the shell prompt without blocking on port-forward. Root cause: `wait_port_forward_ready` took `child.stdout` via `take()`, read lines until `Forwarding from`, and returned — the `BufReader`+`ChildStdout` were dropped, closing the read-end of the pipe. kubectl is a Go binary; Go's default SIGPIPE handler terminates the process on the next write to a closed stdout, and after the initial ready line `kubectl port-forward` emits another `Forwarding from [::1]:…\n` → SIGPIPE → kubectl exit → `child.wait()` returned instantly. Stderr had the same latent class of bugs — `Stdio::piped()` without a drainer; the 64KiB pipe buffer could overflow and block the child on its next write. **Fix:** spawn drainer threads for both pipes. `spawn_ready_drainer` reads stdout line by line, signals readiness via `mpsc::sync_channel::<()>(1)` on the first `Forwarding from` line, then **keeps draining to EOF** — the pipe stays open for the entire lifetime of the child. If EOF arrives before the banner, the sender drops, `recv()` → `Err` → the caller surfaces "exited before binding local port". `spawn_silent_drainer` drains stderr to EOF without surfacing it. +4 unit tests driven by `std::io::Cursor` fakes (without real kubectl): `ready_drainer_signals_on_forwarding_line` (happy path), `ready_drainer_continues_draining_after_signal` (load-bearing — wraps the reader in a Tracker counting consumed bytes, asserts ALL bytes including post-banner ones are read), `ready_drainer_yields_recv_err_when_eof_before_banner` (error surfacing), `silent_drainer_reads_to_eof` (stderr drain coverage). CLI 0.1.135 → 0.1.136. Chart unchanged (CLI-only IO handling defect) | v0.1.136 |
| 2026-05-22 | M1.5 Track B.1.79 closure — CLI thin wrappers + Argo CD MigrationPlan Lua action. New CLI subcommands in the `apprafter` binary: `apprafter platform status` (reads PlatformStack/default via kubectl shellout; prints channel/pin/autoUpgrade/tier + current/target/available versions + conditions table + last-5 versionHistory; tabled render with 60-char message wrap), `apprafter platform upgrade [--to <v>]` (merge-patch `spec.pin: <v>`; without `--to` clears the pin + flips `autoUpgrade=true` for channel-following mode), `apprafter migration list` (table of MigrationPlans in apprafter-system: name/scope/classification/phase; defaults phase to `pending-approval` for CRs without status), `apprafter migration approve <name>` / `reject <name>` (status-subresource merge-patch with phase=approved/rejected; application-scope rejects denied by webhook per ADR 0027 — apiserver denial bubbles up verbatim), `apprafter open argocd` (spawns `kubectl port-forward svc/argocd-server -n argocd 8080:443`, waits for the "Forwarding from" stdout line, prints URL+admin+password, cross-platform browser open via `xdg-open`/`open`/`cmd /c start`, blocks on child.wait() so Ctrl+C tears down both). A shared `commands::k8s_helpers` module centralises kubectl shellout (kubectl_get_json with 404→None, kubectl_merge_patch with optional subresource, ensure_kubeconfig_tempfile decrypting the cached age blob via cli_core::secrets) — three new wrappers share one implementation. npm-style version-check banner: `maybe_warn_about_newer_version()` fires before clap parse, fetches `api.github.com/repos/apprafter/apprafter/releases/latest` with a 3s timeout, caches the result in `~/.cache/apprafter/version-check.json` with a 24h TTL, semver-aware `newer_than` comparison (strips the `v` prefix). **Fail-quiet** — network errors / GitHub rate-limit / JSON parse / unparseable versions are all swallowed silently (debug log only) per courtesy-not-prerequisite semantics. Chart-side: `platform-stack/cue/component_argocd.cue` `configs.cm` map extended (`configs: cm: {<key>: <val>}` form to accommodate sibling entries) with a `resource.customizations.actions.apprafter.io_MigrationPlan` Lua resource-action block. The discovery script returns `actions["approve"]={disabled=…}` + `actions["reject"]={disabled=…}` based on `status.phase` (decidable iff phase=="" or "pending-approval"); the action bodies mutate `status.phase` to "approved"/"rejected"; Argo CD routes the mutation through the status subresource automatically. **Deferred to 1.79a:** `apprafter platform channel/freeze/unfreeze/rescue`, `apprafter open backstage/grafana/hubble` (multi-channel UX waits for Phase 2; Backstage/Grafana/Hubble are not tier-1 resident; freeze/unfreeze paired with ResourceClaim CRUD in 1.79a). Tests: +13 unit (cli-side: `commands::version_check::tests` x4 covering newer_than v-prefix/equal/older/garbage; `commands::platform::tests` x2 for print_status minimal + full fixtures; `commands::migration::tests` x2 for plan_row default phase + full extraction). Total platform-cli crate: existing + 13. CLI 0.1.134 → 0.1.135 (cli/Cargo.toml workspace.package.version); platform-stack 0.1.38 → 0.1.39 (chart Lua action only — byte-equivalent templates vs 0.1.38). Operator chart unchanged (no operator-binary delta) — appVersion stays v0.1.134, RELEASED_OPERATOR_VERSION constant untouched | v0.1.135 |
| 2026-05-23 | M1.5 Walk-fix #8 post-B.1.78 — destructive classification was per-target-version, not per-transition. The user pointed out: a cluster on 0.1.35 with walk-platform-1's `platform-0-1-35-to-0-1-36` MigrationPlan in rejected phase. Chart 0.1.37 publishes as safe. autoUpgrade tries to bump 0.1.35 → 0.1.37 — plan name `platform-0-1-35-to-0-1-37` (a different pair from the rejected one) — fetch_change_class returns Safe for 0.1.37's single record → straight bump. **It silently bypasses the operator's reject decision on 0.1.36's breaking content.** Per spec.md §3.11 implied semantics ("any path to the target must respect the strictest class encountered"), classification should be path-aware. Fix: a new `fetch_path_max_change_class(url, from, to)` pulls the compat doc at `to`'s tarball, walks records in the `(from, to]` half-open range, returns the strictest class via the `path_max_change_class` pure helper. The reconcile destructive check replaces `fetch_change_class(url, target)` → `fetch_path_max_change_class(url, current_target, desired)`. Edge cases: from==to → Safe (no-op); from>to (downgrade) → Safe (spec.md is silent on the downgrade direction; conservative default; future work for cumulative reverse-direction semantics if a real use case surfaces); unparseable version key → skipped without affecting other entries. Classification ordering (Safe < RequiresRestart < DataMigration < Breaking) via an internal `class_order` helper. +8 regression unit tests (path-max strictest-in-range, excludes-from, no-op, downgrade, requires-restart > safe, data-migration > requires-restart, breaking > data-migration, skip unparseable). Total platform-stack crate: 68 → 76. CLI 0.1.133 → 0.1.134, operator+webhook chart v0.1.114 → v0.1.115 + appVersion v0.1.133 → v0.1.134, platform-stack 0.1.37 → 0.1.38 | v0.1.134 |
| 2026-05-23 | M1.5 Walk-fix #7 post-B.1.78 — `PlatformMigrationStrategy.reject` failed for channel-following clusters (snapshot.pin=null). The Phase B.1.78 reject test walk on chart 0.1.36 (breaking) created plan `platform-0-1-35-to-0-1-36`, the user patched it to phase=rejected. The MigrationController saw rejected → invoked strategy.reject → built an SSA-apply body `{"spec":{"pin":null}}` (snapshot.pin=null since the cluster is channel-following). Apiserver 422: `spec.pin: Invalid value: "null": spec.pin in body must be of type string` (CRD schema `type: string` without `nullable: true`). The error propagated → reconcile errored → walk-fix #3 sealing's `status.rejectedAt` write never ran → the marker stayed null → the next reconcile retried the same path → infinite loop. Cluster blocking-by-rejected-plan still worked (plan phase=rejected set by the user patch; PlatformController GET-by-name found a non-completed phase → blocked the bump), but there was error log churn + broken sealing. Fix: three-branch dispatch in `PlatformMigrationStrategy.reject` — (1) Api::get the current PlatformStack; (2) a `pins_equal` helper compares the current spec.pin vs snapshot.pin treating missing/null/explicit-null as equivalent ("channel-following"); (3) dispatch: pins equal → no-op success; snapshot=Some(String) and they differ → SSA force=true (existing path); snapshot=None/Null and pin set → JSON merge-patch `{"spec":{"pin":null}}` (RFC 7396: null deletes the field, works regardless of CRD nullable). Side-effect: the walk-fix #3 sealing path now reaches completion for null-snapshot clusters (it was masked by this bug); subsequent reconciles see the rejectedAt marker and skip strategy.reject. +4 regression tests (pins_equal: missing/null/explicit-null equivalence + same string equal + different string distinct + null vs string distinct). Total migration crate: 14 → 18. CLI 0.1.132 → 0.1.133, operator+webhook chart v0.1.113 → v0.1.114 + appVersion v0.1.132 → v0.1.133, platform-stack 0.1.36 → 0.1.37 | v0.1.133 |
| 2026-05-23 | M1.5 Track B.1.78 closure — PlatformController MigrationPlan integration per spec.md §3.11 + ADR 0027. The PlatformController reconcile body gains a destructive-transition gate: it synthesizes a deterministic plan name `platform-<from>-to-<to>` (dots → dashes for DNS-1123), GETs by name in `apprafter-system`, then runs a 4-branch flow: (1) plan exists + phase=completed → bump (operator approved + ran the migration); (2) plan exists + other phase (pending/approved/executing/failed/**rejected** — rejected blocks too per ADR 0027 explicit decision) → block the bump, surface MigrationPending=True/<class from existing plan> + UpgradeAvailable=True/BlockedByMigrationPlan with `apprafter-system/<plan-name>` in the message; (3) no plan + classification destructive (`breaking|data-migration|requires-restart` per spec.md §3.11 — extended beyond the prior code's `breaking|data-migration` only) → CREATE a MigrationPlan via SSA with scope.type=platform, scope.platform.components=["platform-stack"] (a simplification — single conservative entry; future enhancement: diff per-component), trigger.{type=platform-classification, field=spec.pin, from, to}, risks.classification, **previousSpecSnapshot.pin** = current spec.pin (or JSON null when unpinned — `PlatformMigrationStrategy.reject` B.1.76 reads it back for revert), block the bump, surface conditions; (4) no plan + safe → bump as before. **The annotation source per the plan.md placeholder was rewritten to a structured `spec.previousSpecSnapshot.pin` field** (already in the B.1.75 CRD). Approve flow: the MigrationController completes the plan → the PlatformController's next reconcile sees completed → bumps. Reject flow: B.1.76's PlatformMigrationStrategy.reject reverts the pin. Removed: the `PolicyHooks` trait + `NoOpHooks` stub (a forward-compat placeholder from B.1.73 that never had a real impl); inline plan creation replaces it. policy.rs deleted; the Context.hooks field + `Policy(#[from] PolicyError)` Error variant removed. RBAC: the operator chart's ClusterRole's `migrationplans` rule gains the `create` verb (was `get list watch patch update`); without `create` an SSA-create 403's. +7 unit tests (synthesize_platform_plan_name DNS-1123/deterministic, change_class_to_string enum, build_platform_migration_plan_cr shape + null-pin variant, plan_classification getter); −1 test (NoOpHooks removed). Total platform-stack crate: 62 → 68. CLI 0.1.131 → 0.1.132, operator+webhook chart v0.1.112 → v0.1.113 + appVersion v0.1.131 → v0.1.132, platform-stack 0.1.33 → 0.1.34 | v0.1.132 |
| 2026-05-23 | M1.5 Walk-fix #6 post-B.1.77 — the webhook config was missing the `migrationplans/status` rule. Phase 3.4 retest walk on v0.1.130: we applied an app-scope MigrationPlan, patched it with `--subresource=status --type=merge -p '{"status":{"phase":"rejected"}}'`. The webhook should have denied it per ADR 0027 (walk-fix #2 validator guard), but the patch succeeded. Root cause: the ValidatingWebhookConfiguration's `migrationplans.apprafter.io` webhook listed only `resources: [migrationplans]`. `kubectl patch --subresource=status` routes through the apiserver's `/status` SUB-resource endpoint — a separate path from the main resource. Webhook configs must explicitly list `<resource>/status` to intercept status-subresource writes; without it, status patches bypass the webhook entirely → the walk-fix #2 ADR 0027 guard and the phase transition FSM are never invoked for status changes. Fix: chart `operator/charts/apprafter-admission-webhook/templates/validatingwebhookconfiguration.yaml` — add `migrationplans/status` to the migrationplans webhook's `rules.resources` list. No Rust code change; the operator + webhook binaries are identical to v0.1.130 (image v0.1.131 tagged via standard chart lockstep, same binary content). **Bonus:** chart 0.1.33 pins the identical image v0.1.131 as the v0.1.130 cluster runs after chart 0.1.32 — pinning to 0.1.33 triggers no pod restart, enabling clean isolated walk-fix #5 verification on a stable pod without chart-upgrade pod-cycle artifacts (which caused the Phase 6 second-bump regression — the bump landed on an intermediate v0.1.127 pod without walk-fix #5, the strip pattern wiped the entry). CLI 0.1.130 → 0.1.131, operator+webhook chart v0.1.111 → v0.1.112 + appVersion v0.1.130 → v0.1.131, platform-stack 0.1.32 → 0.1.33 | v0.1.131 |
| 2026-05-23 | M1.5 Walk-fix #5 post-B.1.77 — versionHistory SSA ownership-release bug. The walk-fix #4 observability in v0.1.129 showed that settled-state reconciles always log `include_version_history=false new_history_len=0` — the strip pattern is active every cycle. Combined with the CRD schema verified (versionHistory present) + apiserver pruning ruled out + the Kubernetes SSA Apply docs being explicit: "If a field is no longer in the applied configuration, the manager's ownership is removed; if no other manager owns it, **the apiserver removes the field**" — this is clearly the root cause of walk-fix #7's "omit field to preserve value" pattern. Sequence: the bump cycle's append + write claims ownership + stores the entry; immediately the next settled-state reconcile strips the field + releases ownership → the apiserver deletes it within ~30s. All earlier walks saw null versionHistory because the reads happened AFTER ownership release. **Fix:** drop the "omit field" pattern; replace it with a server-state-read + merge pattern. Before each `patch_status`, `Api::get_status` reads the authoritative server state; the `merge_version_history(server, local)` helper (new in `status.rs`) preserves server entries, appends local-only ones, dedupes by `(version, appliedAt)` pair, enforces the cap. The patch body ALWAYS includes `versionHistory`. Cost: an extra `Api::get_status` per write; settled cycles skip via the `write_status_if_changed` shortcut (no-op writes early-return). It sees through the cache-stale-overwrite race that walk-fix #7 was originally protecting against — it never reads the cache for versionHistory, always reads the server. The `_include_version_history` parameter is retained as a no-op for binary-compat with existing call sites. +4 regression tests in `status.rs`: `merge_version_history_keeps_server_entries_when_local_is_empty` (load-bearing — settled state preserves server entries), `_appends_local_only_entries` (bump cycle), `_dedupes_by_version_and_applied_at` (rollback semantics), `_caps_at_max` (ring buffer). Total platform-stack crate: 58 → 62. CLI 0.1.129 → 0.1.130, operator+webhook chart v0.1.110 → v0.1.111 + appVersion v0.1.129 → v0.1.130, platform-stack 0.1.31 → 0.1.32 | v0.1.130 |
| 2026-05-23 | M1.5 Walk-fix #3 + #4 post-B.1.77 bundled. **#3:** the MigrationController seals rejected plans via a persistent `status.rejectedAt` marker. The acceptance walk showed that the platform-scope plan walk-platform-1 (rejected, previousSpecSnapshot.pin="0.1.25") forced PlatformStack.spec.pin="0.1.25" on every operator pod restart (the chart auto-upgrade triggered a Deployment rolling update → cold-start cache replay → rejected plans re-reconciled → strategy.reject() re-invoked → SSA-patched the pin back to the snapshot). This overrode the user's `kubectl patch ... pin=null` patches and jammed the PlatformController in a `fetch_change_class("0.1.25")` registry error loop (0.1.25 is not published). Fix: a persistent marker `status.rejectedAt: Option<String>` (RFC3339 timestamp). The reconcile's `"rejected"` branch checks the marker — if present, skip strategy.reject + `Action::await_change()`; if absent, call strategy.reject + set the marker + write status. The plan is sealed after the first reject; subsequent reconciles on cold-start are no-ops. The CRD schema (operator chart's `crd-migrationplan.yaml`) + CUE source (`schemas/v1alpha1/migrationplan.cue`) were extended with `status.rejectedAt: string format=date-time` (optional). The Rust type `operator_core::MigrationPlanStatus.rejected_at: Option<String>`. +2 regression tests: `rejected_plan_with_rejected_at_marker_is_sealed`, `rejected_plan_without_rejected_at_marker_is_not_sealed`. Total in the migration crate: 12 → 14. **#4:** PlatformController bump-cycle observability — two `info!()` logs around the append + write_status decision: (1) before append — `target_changed`, `appended_history`, `target_for_patch`, `current_target`, `prior_history_len`; (2) before write — `include_version_history`, `new_history_len`. The Phase 6 walk (artificial pin downgrade+upgrade test) showed versionHistory empty after multiple successful bumps; the logs showed nothing diagnostic (only generic "fired"/"completed"). These are production-useful logs (not debug); a future walk-fix may follow with the actual versionHistory write fix once the logs reveal the offending branch. CLI 0.1.128 → 0.1.129, operator+webhook chart v0.1.109 → v0.1.110 + appVersion v0.1.128 → v0.1.129, platform-stack 0.1.30 → 0.1.31 | v0.1.129 |
| 2026-05-22 | M1.5 Walk-fix #2 post-B.1.77 — the webhook FSM closes the ADR 0027 bypass on app-scope `rejected` via first-write. The acceptance walk Phase 3.4 on v0.1.127: an app-scope plan applied + `kubectl patch --subresource=status -p '{"status":{"phase":"rejected"}}'` — the webhook **accepted** it (it must have denied it per ADR 0027). Root cause: the `is_allowed_phase_transition` first-write branch (`old_phase.is_empty()` — a fresh CR without status) returned `true` for any plausible new_phase **regardless of scope**; the scope check for rejected was only in the `("pending-approval", "rejected")` match arm later. So a fresh-CR + status patch slipped straight to a sealed `rejected` without tripping ADR 0027. Fix: the ADR 0027 guard moved BEFORE the first-write fast-path — `new_phase=="rejected" && scope_type=="application" → false` covers all paths (fresh → rejected, pending → rejected, approved → rejected defensive, executing → rejected defensive). The error message extended to "ADR 0027" for any new_phase=rejected on app-scope (was only the pending → rejected case). No code damage from the slip — `ApplicationMigrationStrategy.reject` is Ok-no-op per design; semantically it is just an audit-trail violation. +3 regression unit tests: `rejects_application_scope_first_write_to_rejected_per_adr_0027` (load-bearing), `allows_platform_scope_first_write_to_rejected` (counterpart pin for platform), `rejects_application_scope_approved_to_rejected_per_adr_0027` (defensive). Total admission-webhook lib: 75 → 78. CLI 0.1.127 → 0.1.128, operator+webhook chart v0.1.108 → v0.1.109 + appVersion v0.1.127 → v0.1.128, platform-stack 0.1.29 → 0.1.30 | v0.1.128 |
| 2026-05-22 | M1.5 Walk-fix #1 post-B.1.76 — SSA `.force()` on MigrationController + PlatformController status writes. The acceptance walk B.1.74→B.1.77 on v0.1.126 hit a phase=approved freeze on a MigrationPlan: `kubectl patch ... --subresource=status --type=merge` registers the `kubectl-patch` field manager as owner of `status.phase`; the controller's SSA patch carrying `phase=executing` (under the `migration-controller` field manager) 409s with a managedFields conflict; error_policy retries every 15s forever on the same conflict. Symptom: the plan freezes at `approved`, no transition. The walk diagnosis confirmed the root cause; the workaround for the active walk = pass `--field-manager=migration-controller` on the kubectl patch (kubectl pretends to be the controller, no conflict — the controller's next write reuses the same manager, owns the field). The real fix: `.force()` on `PatchParams::apply(FIELD_MANAGER)` in `operator-controllers/migration::reconcile::write_status` + preventively `operator-controllers/platform-stack::reconcile::write_status` (latent — the PlatformController hasn't surfaced the bug since it was the sole writer in every walk so far, but the structural shape is identical). The Application controller's `apply_status` already uses `.force()` (built-in since the B.1.7 era) — the walk-fix brings migration + platform in line. **No** regression test is added — this is a runtime SSA conflict requiring a real apiserver; it is covered by the next walk's re-run of phase 3.2. CLI 0.1.126 → 0.1.127, operator+webhook chart v0.1.107 → v0.1.108 + appVersion v0.1.126 → v0.1.127, platform-stack 0.1.28 → 0.1.29 | v0.1.127 |
| 2026-05-22 | M1.5 Track B.1.77 closure — Application reconciler integration: gate pause/resume per spec.md §3.8 + ADR 0027. The `operator-controllers/application` reconciler gains a pause gate that runs BEFORE child resource patches: it lists MigrationPlans in `apprafter-system`, filters by scope.type=application + scope.application.ref matching + scope.application.environment match (or wildcard when env is None), checks `plan_is_blocking` (phase != completed && phase != rejected). If found: skip the child apply, write `Application.status.phase = AwaitingMigrationApproval` + `Ready=False/MigrationPending` + `MigrationPending=True/MigrationPlanPending` conditions (plan name embedded in the message); EndpointURL preserved; requeue 30s. The helpers are extracted as pure fns (`pick_blocking_plan`, `plan_is_blocking`, `build_paused_status`, `migration_pending_condition`) testable without kube::Client. **Detection is NOT invoked in reconcile**: `ApplicationMigrationStrategy::detect_destructive(old, new) -> Option<DestructiveChange>` is a concrete fn landed on the strategy struct, but the impl always returns None in 1.77 — the current v1alpha1 Application schema (image/replicas/expose/env) carries no destructive operations per spec.md §3.8; Phase 2.x services populate it. `create_plan_for(change, plan_name, app_ns, app_name, env) -> MigrationPlan` is a builder for future callers. The `DestructiveChange` type is in `operator-core` (trigger_type + field + from + to + classification — mirrors `MigrationPlan.spec.trigger` + `spec.risks.classification`). `PHASE_AWAITING_MIGRATION_APPROVAL` + `COND_MIGRATION_PENDING` constants in `operator-core/application.rs`. Argo CD UI: the chart's `argocd-cm` ConfigMap gains a custom resource-health Lua script under `configs.cm.resource.customizations.health.apprafter.io_Application`. It returns `Degraded` with the MigrationPlan name (read from `status.conditions[type=MigrationPending].message`) when `phase=AwaitingMigrationApproval`; `Healthy` on `phase=Ready`; `Progressing` otherwise. The custom health surfaces in the Argo CD UI as a Degraded card. Tests: +9 unit (application crate: 8 `pick_blocking_plan` filter cases + 2 `build_paused_status` shape + 1 `migration_pending_condition` k8s-convention timestamp preservation). The operator-controllers-application crate gains a dep on `operator-controllers-migration`. CLI 0.1.125 → 0.1.126, operator+webhook chart v0.1.106 → v0.1.107 + appVersion v0.1.125 → v0.1.126, platform-stack 0.1.27 → 0.1.28 | v0.1.126 |
| 2026-05-22 | M1.5 Track B.1.76 closure — MigrationController + strategy dispatch per spec.md §3.8 + ADR 0027. The third reconciler in the `apprafter-operator` binary (peer to the ApplicationController + PlatformController, same Lease). A new workspace member `operator/operator-controllers/migration` (Cargo crate). It owns the `MigrationPlan.status.phase` FSM (pending-approval → approved → executing → completed/failed | rejected sealed). The `MigrationStrategy` trait is in `operator-core` (covers `execute_step` + `reject` only; detect_destructive deferred to B.1.77 callers because the Application + Platform detection signatures differ). The trait method execute_step returns a StepOutcome (Succeeded/Failed/Skipped). Impls: `ApplicationMigrationStrategy` (execute Succeeded — free-form action text without machine semantics in 1.75/1.76; reject no-op per ADR 0027) and `PlatformMigrationStrategy` (execute Succeeded; **real** reject — SSA-patches `PlatformStack.spec.pin` back to `plan.spec.previousSpecSnapshot.pin` (or null when the snapshot has no pin) under field manager `migration-controller-strategy`, idempotently). Reconcile loop: pending-approval (no-op, await_change), approved → write executing → requeue, executing → run next step (executed_steps.len() doubles as a progress marker, replay-safe), rejected → strategy.reject() then sealed. Status writes under field manager `migration-controller` via SSA. The admission webhook gains an FSM transition validator (`validate_phase_transition` + `is_allowed_phase_transition`): pending-approval → approved (any) | rejected (platform only); approved → executing | sealed; executing → completed/failed; sealed states immutable. **Acceptance #4 covered**: application-scope `pending-approval → rejected` rejected with an ADR 0027-reference error message. The RBAC ClusterRole extends with migrationplans + /status verbs (get/list/watch/patch/update). The annotation source (`apprafter.io/previous-spec` per plan.md) was redesigned and rewritten to the `spec.previousSpecSnapshot` field (already in the B.1.75 CRD schema) — the annotation approach was an ADR 0027 placeholder, the structured field is cleaner. Detection (`detect_destructive`) is **not** in the trait — per-scope concrete fns (Application diff signature vs version+compat-doc signature) live on each impl; the callers in B.1.77/B.1.78 wire them. Tests: +11 unit (migration crate: 4 reconcile FSM helpers + 5 strategy execute/reject/snapshot extraction + 2 scope dispatch) + +12 unit (webhook FSM transitions: 4 happy paths + 8 rejections including acceptance #4) + +2 integration (server.rs FSM: app-scope reject blocked, platform-scope reject allowed). Total in the migration crate = 11 tests; admission-webhook = 75 lib + 12 integration. CLI 0.1.124 → 0.1.125, operator+webhook chart v0.1.105 → v0.1.106 + appVersion v0.1.124 → v0.1.125, platform-stack 0.1.26 → 0.1.27 | v0.1.125 |
| 2026-05-22 | M1.5 Track B.1.75 closure — unified MigrationPlan CRD + admission webhook validation per spec.md §3.8 + ADR 0027. CUE schema rewrite (`schemas/v1alpha1/migrationplan.cue`) with a scope discriminator (`application` \| `platform`), `trigger`, `risks` (classification + reversibility + backup), `plan[]` (steps), `approvers[]`, `previousSpecSnapshot`. Status: `phase` enum (`pending-approval` → `approved`/`rejected` → `executing` → `completed`/`failed`), `approvedAt/By`, `executedSteps[]`. The OpenAPI v3 CRD ships from the operator chart (`templates/crd-migrationplan.yaml`) at sync-wave -5 alongside Application + PlatformStack — it **does not use a `oneOf` discriminator** (the apiserver rejects most oneOf shapes in a structural schema); instead scope.{application,platform} are both optional CRD-side + the conditional invariant is enforced by the webhook. The admission webhook gains a `validator_migrationplan.rs` module + a dispatch branch for `kind=MigrationPlan` via server.rs (passes `request.oldObject` for the UPDATE-time scope immutability check). Validation: (1) **Scope discriminator** — type=application requires a populated `scope.application.{ref.{name,namespace},environment}`; type=platform requires `scope.platform.components` non-empty; a mismatched sub-object (`application` block on a platform-scope plan etc.) is rejected. (2) **Approver emails** — light RFC5322 (`is_emailish`): a single `@`, non-empty local + domain, a dot in the domain. (3) **spec.scope immutability on UPDATE** — diff `request.object` vs `request.oldObject`; a scope change is rejected (the trait dispatch in 1.76 keys on scope; mutating it mid-plan silently switches the controller path). The ValidatingWebhookConfiguration extended with a third entry for `migrationplans` (CREATE+UPDATE). The Rust `MigrationPlan` type in `operator-core/src/migration_plan.rs` (kube-rs CustomResource derive) — used by the validator only for the type schema today; B.1.76 wires it through the reconcile signatures. Deferred to B.1.76: reject status patches not from the MigrationController — that is a controller-existence-aware concern. Tests: 24 unit (validator_migrationplan) + 3 integration (server.rs MigrationPlan dispatch: accept application-scope, reject platform-scope with empty components, reject UPDATE scope mutation); total +27. The CUE schema vetted via `examples/migrationplans/{parser-pg-selector,platform-0-2-0-bump}.cue` round-trip. CLI 0.1.123 → 0.1.124, operator+webhook chart v0.1.104 → v0.1.105 + appVersion v0.1.123 → v0.1.124, platform-stack 0.1.25 → 0.1.26 | v0.1.124 |
| 2026-05-22 | M1.5 Track B.1.74a closure — yanking support for published platform-stack versions. Extension of the `compatibility.cue` schema: `yanked: bool \| *false` + `yankedReason?: string`. CUE cannot express the conditional invariant "yankedReason required when yanked=true" with a single field constraint, so we added a CI guard in both workflows: `platform-stack-check.yml` (PR time) + `platform-stack-publish.yml` (publish time) — `cue export -e compatibility \| jq` walking entries with yanked=true, failing on an empty `yankedReason`. PlatformController consumption (3 surfaces): (1) **`resolve_non_yanked_latest`** helper in reconcile — walks channel-tag candidates newest-first, skips entries `yanked: true` in the compatibility doc, returns the first non-yanked one. Fresh clusters resolve `availableVersion` to a non-yanked version. (2) **`YankedVersion` condition** — informational (NOT `Ready=False`), True iff `status.currentVersion` matches a yanked entry with the verbatim `yankedReason` in the condition message. Surfaces via `kubectl describe platformstack`. On no-poll cycles (throttled) the prior condition is preserved. (3) **Refactor compatibility.rs + oci.rs**: `fetch_compatibility_doc(url, version_tag) -> CompatibilityDoc` — pull the tarball + parse the full BTreeMap once per poll cycle, reuse it for the yank filter + (future) change_class lookup. `oci::tags_in_channel` returns a Vec descending; `latest_in_channel` remains a wrapper for backward compat. `VersionRecord` extends with `yanked` + `yanked_reason` (`#[serde(rename="yankedReason")]`). Architecture choice: yank handling INLINE in reconcile, not behind `PolicyHooks::is_yanked` — yank is a pure lookup over an already-pulled doc, no extensibility seam. The stub method was removed from the trait; `NoOpHooks` remains a test fixture only for MigrationPlan. **Deferred**: the UI shim for `apprafter platform status` + the Backstage plugin warning banner — visibility via `kubectl describe platformstack` + Events is sufficient until the CLI subcommand / Backstage plugin land. **Walk deferred** to the next sub-phase (the next opportunity exercises the B.1.74a regression). +9 regression tests (2 compatibility — yanked default false / camelCase reason; 2 oci — tags_in_channel sort descending + empty filter; 5 reconcile — resolve_non_yanked_latest covers no-yank/skip-top/consecutive-yanks/missing-entry/all-yanked); -1 test (deleted `no_op_hooks_report_not_yanked`). Total 50 → 58 in the platform-stack controller crate. CLI 0.1.122 → 0.1.123, operator+webhook chart v0.1.103 → v0.1.104 + appVersion v0.1.122 → v0.1.123, platform-stack 0.1.24 → 0.1.25 | v0.1.123 |
| 2026-05-22 | M1.5 Track B.1.74 walk-fix #7 — versionHistory race fix. The B.1.74 acceptance walk on v0.1.121 revealed a controller-cache race: the PlatformController's own SSA-status-write triggers a follow-up reconcile via the watcher cache (kube-rs `Controller`), which lags the apiserver by hundreds of ms. The next reconcile sees a **stale** snapshot of `PlatformStack` (without the just-persisted versionHistory entry); although `target_changed=false` (no append happens), `write_status_if_changed` still detects a `conditions[*].reason` delta (fresh Synced cycle messaging) and patches status — clobbering the apiserver's authoritative versionHistory with the stale-2-entry-vector. Walk symptom: after the bump v0.1.19→v0.1.20 the history briefly shows 3 entries, then silently reverts to 2. Fix (Option A): the SSA patch body now OMITS the `versionHistory` field when the reconcile cycle did NOT append this turn. SSA preserves field values that are ABSENT from the patch body, so the apiserver's existing value stays authoritative across stale-cache reconciles. Implementation: three internal helpers (`build_status_patch`, `write_status`, `write_status_if_changed`) gained an `include_version_history: bool` parameter; `build_status_patch` serializes the status to JSON and conditionally strips the `versionHistory` map key. The reconcile body tracks an `appended_history` flag and passes it through. The in-flight early-return passes `false` (no append ever happens before that branch). Option B (read PlatformStack via `Api::get` bypassing the cache) was rejected — a per-reconcile apiserver round-trip on the hottest path + it doesn't actually close the race (`Api::get` is still subject to read-your-write timing). +2 regression tests: `build_status_patch_omits_version_history_when_not_appended` + `build_status_patch_includes_version_history_when_appended`; the existing `build_status_patch_includes_apiversion_kind_and_name` updated to the new 3-arg signature (`include_version_history: true` for the existing assertion). Total 48→50 in the platform-stack controller crate. Per the user, "Let's do A without a re-walk and go straight to 1.74a" — the walk re-verification is skipped, B.1.74a (yanking) is next. CLI 0.1.121 → 0.1.122, operator+webhook chart v0.1.102 → v0.1.103 + appVersion v0.1.121 → v0.1.122, platform-stack 0.1.23 → 0.1.24 | v0.1.122 |
| 2026-05-22 | M1.5 Track B.1.74 closure — PlatformController status observability polish. Most of plan.md's 1.74 scope already landed in the B.1.73 walk-fixes (periodic check, OCI poll, channel filter, latest semver, status.{availableVersion, lastUpstreamCheck}, UpgradeAvailable condition, autoUpgrade on safe class). B.1.74 closes the two remaining gaps: (1) **`status.versionHistory` ring buffer** capped at `VERSION_HISTORY_CAP=10`, FIFO drop. `append_version_history(status, entry)` helper in `status.rs`; reconcile calls it only when target_changed && target_for_patch != current_target (i.e. a real targetRevision bump, not status-only / values-only patches). Entry: `{version, appliedAt, outcome: "succeeded"}`. (2) **`Ready` condition** — derived from `parent.status.health.status`. True/Healthy iff the parent reports Healthy (Argo CD aggregation from children + workloads); otherwise False/ParentNotHealthy with a message naming the actual health value. Skipped per YAGNI (CLAUDE.md): ETag-aware OCI requests (our `MIN_OCI_POLL_INTERVAL_SECS=60` throttle + cached availableVersion reuse already saturate the bandwidth concern). Breaking-class MigrationPlan auto-create is deferred to B.1.75 (CRD + controller logic); B.1.74 keeps the `MigrationPending=True` condition placeholder. +3 regression guards for the ring buffer (`grows_to_cap`, `caps_at_max_and_drops_oldest`, `starts_from_empty_status`); total 45→48. Live verification piggybacks on the next walk (versionHistory growth on test 1 pin downgrade + Ready visibility immediately post-bootstrap + walk-fix #6 Events audit trail regression). CLI 0.1.120 → 0.1.121, operator+webhook chart v0.1.101 → v0.1.102 + appVersion v0.1.120 → v0.1.121, platform-stack 0.1.22 → 0.1.23 | v0.1.121 |
| 2026-05-22 | M1.5 Track B.1.73 walk-fix #6 — observability polish for foreign-writer detection. A steady-state cluster on v0.1.119 showed: `kubectl patch parent target=0.1.19` → the controller force-reverted it (visible in the Argo CD UI), but there was zero durable trace: the `UnauthorizedSourceModification=True` condition flipped back to `False/Clean` within the next reconcile (sub-second), and the WARN log was lost during the pod restart cascade (chart 0.1.19 has a different operator image pin → Argo CD triggered a pod replacement in the middle of the revert). Fix: emit two Kubernetes Events per violation through `kube::runtime::events::Recorder` — `Warning/ForeignFieldManager` at detection (naming the offending manager) + `Normal/SourceReverted` after a successful force-revert. Both target the PlatformStack singleton with the parent Argo CD Application as `secondary` (`related`). Best-effort publish (failures logged at warn!, do not fail the reconcile). The operator chart's ClusterRole gains an `events.k8s.io/events` create+patch rule (kube-rs `Recorder::publish` uses the events.k8s.io/v1 API, not the legacy core group). Visible via `kubectl describe platformstack default` + `kubectl get events -n apprafter-system`. Survives an operator pod restart (k8s event TTL default 1h). +1 regression test (`parent_object_reference_points_at_argocd_application`) pinning the shape of the `related` ObjectReference. Live verification deferred to the B.1.74 walk (will exercise event emission as a regression check during MigrationPlan tests). CLI 0.1.119 → 0.1.120, operator+webhook chart v0.1.100 → v0.1.101 + appVersion v0.1.119 → v0.1.120, platform-stack 0.1.21 → 0.1.22 | v0.1.120 |
| 2026-05-22 | M1.5 Track B.1.73 walk-fix #5 — the fifth post-closure walk on v0.1.118: the Phase 1-3 acceptance passed cleanly (status correctly populated, conditions sensible, no false-positive `UnauthorizedSourceModification`, managedFields has platform-controller). But the logs showed **hundreds of reconciles per second** — `reconcile fired generation=2` + `reconcile completed` repeating ~350ms infinitely. Root cause: every reconcile unconditionally (1) queried OCI for channel-latest, (2) stamped `status.lastUpstreamCheck = Utc::now()`, (3) SSA-patched status with the new timestamp. The status patch → resource version bump → the watcher fires a fresh event → the next reconcile repeats. A tight self-feedback loop, CPU pegged. Also test 2 was broken: on `kubectl patch` parent target=0.1.15 the controller's force-revert race is lost in noise — Argo CD manages to pull chart 0.1.15, replace the ClusterRole with the old version (without platformstacks rules), the controller becomes locked out. Fix two-pronged: (1) **OCI poll throttle** `MIN_OCI_POLL_INTERVAL_SECS=60` — `reconcile()` reads the prior `status.lastUpstreamCheck`, re-queries OCI only when ≥60s have elapsed. Intermediate reconciles preserve the prior availableVersion + do not touch lastUpstreamCheck. (2) **`write_status_if_changed`** — a new wrapper compares `new_status` vs `stack.status` (PartialEq derived); byte-equal → skip the SSA patch. Combined with `condition()`'s transition-time preservation: a no-op reconcile produces an identical status, the patch never fires, no watch event, the loop dies. Steady-state behaviour is now ~0.017 Hz (1 reconcile/min) vs the 100+ Hz tight loop. +2 regression tests (`status_equality_treats_identical_payloads_as_noop`, `status_equality_distinguishes_timestamp_changes`); total 42→44. CLI 0.1.118 → 0.1.119, operator+webhook chart v0.1.99 → v0.1.100 + appVersion v0.1.118 → v0.1.119, platform-stack 0.1.20 → 0.1.21 | v0.1.119 |
| 2026-05-22 | M1.5 Track B.1.73 walk-fix #4 — the fourth post-closure walk on v0.1.117. The PlatformController logs finally showed the real failure: `parse compatibility.yaml: missing field "compatibility"` every reconcile cycle. The chart's render_tool.cue emits compatibility.yaml as a **top-level map keyed by version** (`"0.1.19":` directly), but the parser expected an outer wrapper `compatibility:`. It also surfaced 4 related problems — a combined 5-fix walk-fix: (1) **Parser fix**: `type CompatibilityDoc = BTreeMap<String, VersionRecord>` — a direct map. (2) **Observability**: info!() logs at every point (spawn, watch loop entry, reconcile fire/finish, SSA patch, foreign detection). Without them walk-fix #1 (RBAC) was invisible — the controller was failing silently. (3) **Loader SSA + whitelist `apprafter-cli`**: step 3 (root App) switched from client-side `kubectl apply` to `apply_manifest_server_side` field manager `apprafter-cli`; the controller's `WHITELISTED_FIELD_MANAGERS` whitelists this manager. It closes the false-positive `UnauthorizedSourceModification=True / ForeignFieldManager: kubectl-client-side-apply` which triggered itself on every bootstrap. (4) **The controller watches the parent Application** via `Controller::watches_with()` — manual kubectl-patches on the parent App now fire an immediate reconcile (previously — wait 6h checkInterval). (5) **Single-writer SSA pattern**: `patch_application` is always `force=true`. The old "patch without force, then force-revert if foreign" deadlocked when the loader's kubectl-client-side-apply already owned targetRevision — SSA without force 409'd, reconcile failed before the revert path. The PlatformController is now THE single writer for `spec.source.{targetRevision, helm.valuesObject}`; foreign detection is only an audit condition + warn log. Known limitation **documented** (not engineered): a manual kubectl-patch of parent target to a chart version pre-0.1.17 (without PlatformStack RBAC) breaks the PlatformController because Argo CD overwrites the ClusterRole with the old version. Recovery: kubectl-patch back to 0.1.17+. An accepted degenerate case per the user (downgrade below the PlatformController-aware era). Tests 40→42 (+2 compatibility parser shape tests). CLI 0.1.117 → 0.1.118, operator+webhook chart v0.1.98 → v0.1.99 + appVersion v0.1.117 → v0.1.118, platform-stack 0.1.19 → 0.1.20 | v0.1.118 |
| 2026-05-21 | M1.5 Track B.1.73 walk-fix #3 — the third post-closure walk on v0.1.116 (with RBAC + TypeMeta from the previous fixes): the PlatformController finally reconciled the CR and populated status, but surfaced two semantic bugs: (1) `UpgradeAvailable=True` fired even when `currentVersion == availableVersion == 0.1.18` — root cause: the old gate `current_target != desired_target ∨ values_differ()` falsely triggered on values_differ because the loader did not set `helm.valuesObject` on the parent App (null vs `{tier: 1}` looks like a diff); (2) `kubectl get app platform -n argocd -o jsonpath='{.metadata.managedFields[*].manager}'` showed only `kubectl-client-side-apply argocd-application-controller` — `platform-controller` made not a single patch because the policy (pin=None + autoUpgrade=false) refused. Fix end-to-end refactor of `reconcile()`: (a) a new `semver_gt(a, b)` helper, `UpgradeAvailable` is now a STRICT semver comparison `channel_latest > target_for_patch` independent of values diffs (fail-safe — false on unparseable); (b) the SSA patch ALWAYS owns both `targetRevision` (kept at current when policy forbids a bump) AND `helm.valuesObject` (values — runtime config, not a version bump, not gated by pin/autoUpgrade); (c) a new `platform_controller_owns_source(parent)` helper triggers a no-op SSA patch on the first reconcile when the manager is not yet registered — so that outside-writer detection works. Plus `MigrationPending` now has an explicit False/Clean; `Synced.reason` = `Patched` or `Reconciled` depending on the patch. +8 regression tests (5 `semver_gt_*` + 3 `platform_controller_owns_source_*`), total 32 → 40. CLI 0.1.116 → 0.1.117, operator+webhook chart v0.1.97 → v0.1.98 + appVersion v0.1.116 → v0.1.117, platform-stack 0.1.18 → 0.1.19 | v0.1.117 |
| 2026-05-21 | M1.5 Track B.1.73 walk-fix #2 — the second post-closure walk on v0.1.115 (with the RBAC fix): the operator pod starts correctly with the new RBAC, the PlatformController watcher works (the CR appears in the reconcile loop), but **every** reconcile fails with `ApiError: invalid object type: /, Kind=: BadRequest (400)` every minute. Root cause: `write_status` sent an SSA patch `{"status": {...}}` without TypeMeta (apiVersion + kind + metadata.name) — the apiserver recommends a full TypeMeta in the SSA patch body to resolve the schema before merge. The Application controller's `apply_status` always emitted TypeMeta correctly, but in the new B.1.73 `write_status` it was dropped. Fix: extracted `build_status_patch(name, status)` + `build_application_patch(desired)` helpers — both emit a full SSA-compliant body (apiVersion+kind+metadata.name+resource-specific). Two regression tests (`build_status_patch_includes_apiversion_kind_and_name`, `build_application_patch_includes_apiversion_kind_name_and_source`) pin the contract — future refactors will not be able to silently strip TypeMeta. All 32 unit tests pass (was 30, +2 new). CLI 0.1.115 → 0.1.116, operator+webhook chart v0.1.96 → v0.1.97 + appVersion v0.1.115 → v0.1.116, platform-stack 0.1.17 → 0.1.18 | v0.1.116 |
| 2026-05-21 | M1.5 Track B.1.73 walk-fix #1 — the first post-closure walk: bootstrap succeeded, Argo CD all green, the default PlatformStack CR applied, but `.status` stays **empty** even after several minutes. Root cause: the operator chart's ClusterRole (B.1.71-era) granted permissions only on `apprafter.io/applications` + owned children. The PlatformController's watcher on `apprafter.io/platformstacks` failed silently on the first list/watch (Forbidden), the controller stream closed without emitting reconciles. The chart also lacked `argoproj.io/applications` get/patch — even if the watcher had worked, an SSA patch of the parent `platform` Application would be rejected. Fix: the chart's ClusterRole gains two rule blocks (platformstacks + /status + argoproj.io/applications). **Parallel optimizations**: both Deployments (operator + admission-webhook) dropped the 5-10s `initialDelaySeconds` padding on the liveness/readiness probes (Rust musl-static boots in ms); a `startupProbe` was added (1s period × 30s failureThreshold) for cold-boot grace without overhead on every restart. The operator workspace `[profile.release]` += `panic = "abort"` — removes the unwinding machinery, ~5-10% smaller musl-static image, faster pull. Net cold-start saving: ~10-20s on each of the two pods + a smaller image. CLI 0.1.114 → 0.1.115, operator+webhook chart v0.1.95 → v0.1.96 + appVersion v0.1.114 → v0.1.115, platform-stack 0.1.16 → 0.1.17 | v0.1.115 |
| 2026-05-20 | M1.5 Track B.1.73 closure — the PlatformController core landed as the second controller inside the `apprafter-operator` binary (adapting plan.md's base "new crate" to the user design "in the existing operator"). A new workspace member `operator-controllers/platform-stack` (peer to the application controller). Reconcile loop: resolve the desired version (pin OR channel-latest via OCI-distribution); read the parent `platform` Application via the dynamic kube API; in-flight detection (sync.status==OutOfSync OR operationState.phase==Running) → requeue 30s; compute the desired source via `desired::build`; fetch the change class via `compatibility.yaml` (chart tarball pull + tar extract); the policy decides (pin set → bump; autoUpgrade=false → status-only `UpgradeAvailable=True`; safe/requires-restart → SSA patch; breaking/data-migration → `MigrationPending=True` condition, deferred to 1.74); SSA patch with field manager `platform-controller`; outside-writer detection via `metadata.managedFields` → force-revert + `UnauthorizedSourceModification=True`; status write (currentVersion/targetVersion/availableVersion/lastUpstreamCheck/conditions) + cadence requeue (parsed from `spec.source.checkInterval`). 30 unit tests pass (5 oci + 4 compatibility + 4 desired + 2 policy + 4 status + 11 reconcile) plus 1 ignored smoke scaffold (APPRAFTER_K8S_SMOKE=1 gate). PolicyHooks trait + NoOpHooks default impl pre-position for 1.74 (MigrationPlan auto-create) + 1.74a (yanking field). **Chart-side breakthrough** — `_applicationsTemplate` extends to consume `.Values.overrides.<component>.{pin,values,enabled}` (pin replaces targetRevision, values mergeOverwrite onto component values, enabled gates emission). The PlatformController writes this block via SSA onto the parent Application's helm.valuesObject from `PlatformStack.spec.overrides`. Chart 0.1.15 → 0.1.16, operator+webhook chart v0.1.94 → v0.1.95 + appVersion v0.1.111 → v0.1.114 lockstep, CLI 0.1.113 → 0.1.114. Out of scope (deferred): yanking field (1.74a), MigrationPlan auto-create (1.74), `minimumKubernetesVersion` env check, multi-stack support (singleton enforced), rollback flow, manifest-level diff (helm render delegated to Argo CD via argocd-cue-cmp) | v0.1.114 |
| 2026-05-20 | M1.5 Track B.1.72 walk-fix #2 — the second post-closure walk: v0.1.112's `kubectl wait crd/applications.apprafter.io --for=condition=Established` failed **immediately** with `NotFound`. `kubectl wait` errors instantly on missing resources, it does not poll for them to appear — a single-stage Established wait racing the operator chart's CRD apply was guaranteed to fail when step 4b's root-App Healthy gave a false-positive. Fix: a two-stage wait per CRD — first `--for=create` (kubectl 1.27+, waits for the CRD object to appear), then `--for=condition=Established` (waits for apiserver discovery registration). New constants: `CRD_CREATE_TIMEOUT_SECS=600` (covers chart pull + apply through cert-manager wave -10 + operator wave 0 under load), `CRD_ESTABLISHED_TIMEOUT_SECS=60` (down from 120, establishment ≤ a few seconds once the CRD exists). Total 4 CRD waits ordered: applications.apprafter.io create → Established → platformstacks.apprafter.io create → Established. Regression tests updated: `waits.len() == 8`, plus assertions that every `--for=create` precedes its matching `--for=condition=Established`. Open question deferred (why step 4b root Healthy false-positives — Argo CD's child-health aggregation timing): step 4c is now non-load-bearing, making root Healthy immaterial for the CRD readiness assertion. CLI bumped 0.1.112 → 0.1.113 (loader-side ordering only — no chart bump, no image rebuild) | v0.1.113 |
| 2026-05-20 | M1.5 Track B.1.72 walk-fix #1 — the first post-closure walk hit `no matches for kind "PlatformStack"` at step 5 (SSA apply of the default PlatformStack singleton). Root cause: step 4b's Healthy wait on `application/platform` returned before the operator chart's CRDs reached `Established=True` AND/OR before the kubectl client's on-disk discovery cache refreshed to include `apprafter.io/v1alpha1.PlatformStack`. Fix: a new step 4c explicitly waits `condition=Established` on both `crd/applications.apprafter.io` + `crd/platformstacks.apprafter.io` between step 4b (root Healthy) and step 5 (SSA apply). New `CRD_ESTABLISHED_TIMEOUT_SECS=120` constant; in practice it resolves in sub-second to a few seconds once the operator child App applied its sync-wave -5 templates. The wait also forces a fresh discovery resolution for the subsequent kubectl invocation (closes the stale-cache angle). Regression guards: the existing `perform_bootstrap_installs_...` test now asserts `waits.len() == 6` with positions [4][5] pinned to the new CRD waits; a new `crd_established_waits_run_after_root_healthy_and_before_platformstack_apply` test asserts the ordering. CLI bumped 0.1.111 → 0.1.112 (no chart bump, no operator image rebuild — the fix is loader-side ordering only) | v0.1.112 |
| 2026-05-19 | M1.5 Track B.1.72 closure — PlatformStack CRD per spec §3.11 + ADR 0026 + restored Application CRD (B.1.71 dropped the imperative shipper without a chart-side replacement). Both CRDs ship from the operator Helm chart at sync-wave -5; the admission webhook gains PlatformStack validation: singleton (name=default, namespace=apprafter-system), channel enum, pin semver shape, source.checkInterval ≥ 1h. The webhook server dispatches by kind (Application/PlatformStack/other). The `ValidatingWebhookConfiguration` template carries a second webhook entry. `cluster-bootstrap` gains step 5: SSA-apply the default PlatformStack CR with field manager `apprafter-cli` after the Healthy wait (tier from the active target via `Tier::level()`, domain omitted). PlatformController logic deferred to B.1.73. Version chain: CLI 0.1.110 → 0.1.111; operator + webhook chart v0.1.93 → v0.1.94 and appVersion v0.1.106 → v0.1.111 (matches the new monorepo tag); platform-stack chart 0.1.14 → 0.1.15 via the CUE invariant chain. spec.md §3.11 prose: `targetVersion` added to the status fields list (additive, no revision bump). Test count: +16 admission-webhook validator tests + 3 server dispatch integration tests + 4 cluster-bootstrap unit tests. All gates clean (fmt, clippy -D warnings, cue vet, helm lint, SPDX, conventional-commits) | v0.1.111 |
| 2026-06-02 | Phase 2 opens — 2.1 ServiceProvider CRD shipped. New namespaced v1alpha1 ServiceProvider CRD: fleshed-out CUE schema (schemas/v1alpha1/serviceprovider.cue, config opaque), hand-rolled OpenAPI v3 CRD from the apprafter-operator chart (crd-serviceprovider.yaml, sync-wave -5, closed type enum pg|jetstream|clickhouse|redis|s3|notifications, status.health), kube-rs ServiceProvider type (operator-core), admission validator (validator_serviceprovider.rs — unknown type / empty backend rejected; the 2.1 acceptance) wired into the webhook server dispatch + a ValidatingWebhookConfiguration entry, plus a cue-vet example. Tier-aware default provider instances deferred to 2.4–2.6. Coordinated version bump to the 0.2 series: operator + admission-webhook charts v0.1.118->v0.2.1 (appVersion v0.1.137->v0.2.1), platform-stack component pins + currentVersion 0.1.52->0.2.1 (compatibility entry, change=safe), cli/Cargo.toml 0.2.0->0.2.1. RELEASED_OPERATOR_VERSION / RELEASED_PLATFORM_STACK_VERSION are build.rs-derived from the charts (no manual constant edit). | v0.2.1 (operator/v0.2.1 + platform-stack/v0.2.1 are workflow-made) |
| 2026-06-02 | 2.1 re-release v0.2.2 — release-pipeline defect fix (the e2e caught it; v0.2.1 abandoned). The v0.2.1 push published BROKEN operator + admission-webhook images: release-operator.yml pushes BOTH the container image (`ghcr.io/apprafter/apprafter-operator:<appVersion>`) AND the Helm chart (`helm push ... oci://ghcr.io/apprafter` -> `apprafter-operator:<chartVersion>`) to the SAME ghcr repo path, and the 0.2.1 uniform bump made chartVersion == appVersion (== v0.2.1), so the chart-publish job (which runs after the image build) OVERWROTE the image tag with the chart .tgz. k3s pulled `apprafter-operator:v0.2.1` as a container image, got a ~10 KB Helm chart, and the operator + webhook pods crash-looped `exec: "/apprafter-operator": no such file`. (The historical chartVersion != appVersion invariant — e.g. chart v0.1.118 vs appVersion v0.1.137 — had hidden this collision.) Fix (Option B): the operator + admission-webhook charts now publish to a dedicated `ghcr.io/apprafter/charts` OCI sub-namespace, with a matching `repositories: "apprafter-charts"` enableOCI registration in platform-stack/cue/loader_values.cue (propagates to both the cluster-bootstrap loader's Argo CD install via build.rs AND the umbrella's Argo CD component) and the two components' `repoURL` repointed there; container images stay at the org root. Charts and images can no longer collide, so uniform chartVersion == appVersion is safe again. Re-released uniformly at v0.2.2 (operator+webhook charts version+appVersion, platform-stack component pins + currentVersion 0.1->0.2.2 with a compatibility entry change=safe, cli/Cargo.toml -> 0.2.2). **One-time manual step on first publish: flip the two new ghcr packages `charts/apprafter-operator` + `charts/apprafter-admission-webhook` to public** (Argo pulls anonymously on Tier 1). | v0.2.2 (operator/v0.2.2 + platform-stack/v0.2.2 workflow-made) |
| 2026-06-02 | 2.2 ResourceClaim CRD shipped at v0.2.3. New namespaced v1alpha1 ResourceClaim CRD mirroring the 2.1 ServiceProvider vertical: CUE schema (resourceclaim.cue +status.conditions) + cue-vet example, hand-rolled OpenAPI v3 CRD (crd-resourceclaim.yaml, sync-wave -5, type enum, selector minProperties 1, size enum, status+conditions), kube-rs ResourceClaim type (operator-core), and the one novel piece — an identity-gated admission validator (validator_resourceclaim.rs: operator-only CREATE, allow iff userInfo.username == the operator SA OR groups contains system:masters, fails closed; UPDATE not gated) wired into server.rs (which now threads request.userInfo + request.operation) + a ValidatingWebhookConfiguration entry. status.conditions is schema-only (writer lands 2.3); nothing creates ResourceClaims until 2.4 (the rule is a forward-looking guard). Uniform version bump v0.2.2->v0.2.3 (operator+webhook charts version+appVersion, platform-stack component pins + currentVersion, cli/Cargo.toml; compatibility entry change=safe). Charts stay in the ghcr.io/apprafter/charts namespace (v0.2.2 fix), so chart==appVersion is safe. | v0.2.3 (operator/v0.2.3 + platform-stack/v0.2.3 workflow-made) |
| 2026-06-02 | 2.3 ResourceClaim scheduler shipped at v0.2.4. New operator-controllers-resourceclaim-scheduler controller (5th peer reconciler in apprafter-operator): a pure matcher (type-equal + label-superset, alphabetical tie-break, cluster-wide provider listing — fully unit-tested) + a thin reconcile that writes status.provider + Scheduled=True on a match, or Scheduled=False + a NoMatchingServiceProvider Warning event + the new apprafter_claim_unmatched_total metric on no-match. New ClusterRole rules (serviceproviders read; resourceclaims/status write). Pending claims re-evaluate on a 300s requeue (a ServiceProvider watch needs a reflector store — deferred). ready/connectionSecretRef + provisioning deferred to 2.4; nothing creates claims until 2.4 so it is a no-op on a fresh cluster. Uniform bump v0.2.3->v0.2.4 (charts version+appVersion, platform-stack component pins + currentVersion, cli/Cargo.toml; compatibility entry change=safe). | v0.2.4 (operator/v0.2.4 + platform-stack/v0.2.4 workflow-made) |
| 2026-06-02 | 2.4b (second sub-subphase of 2.4 needs.pg) — re-add the Application `needs` schema. `needs` is a typed map keyed by `#PlatformServiceType` (`{pg|jetstream|clickhouse|redis|s3|notifications}: {selector?, size?}`), re-added across three hand-rolled mirrors — CUE `application.cue` (`#ServiceNeed` + the field on `#ApplicationSpec`; closed-definition rejects unknown keys at eval), kube-rs `operator-core` (`ServiceNeed` struct + `needs` on `ApplicationBaseSpec`), and the OpenAPI v3 CRD `crd-application.yaml` (under BOTH base and environments; `selector` minProperties 1 + `size` enum) — plus a new admission-webhook rule rejecting unknown `needs` keys in base + per-environment (the structural CRD is open on map keys; the webhook is the runtime enforcer). `f81e350` had removed only the skeleton `needs?: [string]: _` placeholder, so the real shape was designed fresh from spec §3.1. **Pure schema** — no controller generates claims from `needs` yet (Application->ResourceClaim + the AwaitingResourceClaim pause gate are 2.4d; DSN injection + the reserved-`DATABASE_URL` cross-field guard are 2.4e). **Mirror-count correction:** the CLI-side Application CRD (`cli-providers::k8s::application_crd`) was dropped in B.1.71 — the CRD ships only from the operator chart now, so 2.4b touches NO cli/ source. Coordinated operator + platform-stack release v0.2.4 -> v0.2.6 (both charts version+appVersion, platform-stack component pins + currentVersion 0.2.5 -> 0.2.6 with a compatibility entry change=safe, operatorVersion v0.2.6 — re-uniformed after the platform-stack-only 2.4a at 0.2.5); NO cli/Cargo.toml bump, NO monorepo v0.x.y tag. Built subagent-driven (6 commits) + two-stage review (spec compliant / code-quality approved-with-nits -> 3 fixes applied: accurate CUE closedness comment, webhook enum-sync note covering all four sites, multi-bad-key webhook test). | operator/v0.2.6 + platform-stack/v0.2.6 (workflow-made on push; no monorepo v*) |
| 2026-06-02 | 2.4a (first sub-subphase of 2.4 needs.pg) — CloudNativePG operator + pg-integrated ServiceProvider seed, platform-stack-only. New always-on `cloudnative-pg` component (CNPG operator chart 0.28.2 → operator appVersion 1.29.1, namespace cnpg-system, project platform-providers, sync-wave -5, crds.create, replicaCount 1, Deployment terminatingReplicas ignoreDifferences). New data-driven `templates/serviceproviders.yaml` umbrella template (mirrors the appProjects mechanism: `#ServiceProviderSeed` shape + `_serviceProviders` map + render task + `_valuesSchema` entry) seeding one pg-integrated ServiceProvider CR (type pg, backend cloudnative-pg, metadata.labels tier=integrated, namespace apprafter-system, SkipDryRunOnMissingResource=true for the CRD-install race) with tier-aware `instances` (T1=1 / T2=3). NO shared Postgres Cluster seeded — the 2.4c provisioner creates `platform-postgres` lazily on the first matched claim, so solo clusters with no pg apps pay no Postgres-pod cost. The cue-vet fixture + CI smoke (platform-stack-check.yml) extended. **platform-stack-only**: currentVersion 0.2.4->0.2.5 + compatibility entry (change=safe, operatorVersion stays v0.2.4 — no operator image bump); NO operator appVersion bump, NO cli/Cargo.toml bump, NO monorepo v0.x.y tag. Built subagent-driven (6 commits) + two-stage review (spec compliant / code-quality approved-with-nits → breadcrumb fixes applied). Inert on a fresh cluster until 2.4d wires Application -> claim. | platform-stack/v0.2.5 (workflow-made on push; no operator/v* or monorepo v*) |
| 2026-06-02 | 2.4c (third sub-subphase of 2.4 needs.pg) — resourceclaim-provisioner controller, the 6th in-cluster reconciler in apprafter-operator (field manager `resourceclaim-provisioner`). For each ResourceClaim the 2.3 scheduler marked `Scheduled=True` with a `cloudnative-pg` provider it: lazily SSA-applies the shared `platform-postgres` CNPG `Cluster` (created on the first matched claim, so solo clusters with no pg apps pay no Postgres-pod cost); derives a deterministic per-claim Postgres identifier (`claim_<ns>_<name>`, folded to `[a-z0-9_]`, ≤63 bytes); generates a random password + applies a `kubernetes.io/basic-auth` Secret (`cnpg.io/reload`) in cnpg-system; read-modify-writes the Cluster's UNKEYED `spec.managed.roles` list (GET→merge_role→replace, retry up to 5x on 409 — NOT a per-claim SSA merge, which would clobber co-owned entries); applies a declarative CNPG `Database` CR; writes a connection Secret carrying `DATABASE_URL` into the claim's namespace with an ownerReference back to the claim (cascade on delete); and writes ONLY `status.ready`/`status.connectionSecretRef`/`Ready` under its own field manager — NEVER `status.provider`/`Scheduled` (scheduler-owned; a strict SSA split). External CNPG CRs are applied via `kube::api::DynamicObject` + `ApiResource::from_gvk` (no compile-time CNPG types — the CRDs are installed at runtime by the 2.4a `cloudnative-pg` component). **Cleanup is a SKELETON** (decided): the connection Secret cascades via its ownerRef; the role + database are NOT dropped on delete (retained until 2.4f); the provisioner finalizer only logs "role/DB retained pending 2.4f GC" and self-removes, establishing the wiring 2.4f grafts the RetainedClaim snapshot + 7-day grace GC onto. New RBAC: `postgresql.cnpg.io` clusters+databases CRUD (the secrets rule was already cluster-wide). New metric `apprafter_claim_provisioned_total{backend,namespace}`. TDD on the pure helpers (CNPG CR builders + `pg_identifier`/`dsn`/`merge_role` + the `should_provision`/Backend-dispatch/`ready_condition`/`connection_secret_object` decision points); the I/O path is covered by a gated `#[ignore]` real-cluster smoke + the 2.4g manual walk. Coordinated operator + platform-stack release v0.2.6 -> v0.2.7 (both charts version+appVersion, platform-stack component pins + currentVersion 0.2.6 -> 0.2.7 with a compatibility entry change=safe, operatorVersion v0.2.7); NO CRD change, NO cli/Cargo.toml bump, NO monorepo v0.x.y tag. The Application still does not generate claims until 2.4d, so the provisioner is a no-op on a fresh cluster. | operator/v0.2.7 + platform-stack/v0.2.7 (workflow-made on push; no monorepo v*) |
| 2026-06-03 | 2.4d (fourth sub-subphase of 2.4 needs.pg) — the Application controller now generates child ResourceClaims from `needs.*` and gates on them. For each `needs` entry on the effective spec it SSA-applies one child `ResourceClaim` named `{app}-{type}` (DNS-1123-folded), default selector `{tier: integrated}` injected when the need omits one, `size` passed through when present, owner-ref → the Application (controller=true / blockOwnerDeletion=true) for cascade — writing **spec + metadata only, NEVER status** (the 2.3 scheduler owns status.provider/Scheduled, the 2.4c provisioner owns status.ready/connectionSecretRef/Ready; a strict SSA split under the `apprafter-operator` field manager, the apply payload carries no `status` key). The reconcile then pauses in a **new `AwaitingResourceClaim` phase** (mirrors the ADR-0027 `AwaitingMigrationApproval` gate: a `Ready=False`/`ResourceClaimPending` condition + a `ResourceClaimPending=True` condition naming the unready claim(s), `observedGeneration` + `endpointURL` preserved, lastTransitionTime preserved when already True) until every generated claim reports `status.ready==true` AND `connectionSecretRef` (BOTH — closes the half-ready resume race at zero cost). Resume is immediate via a new `.owns(ResourceClaim)` watch re-enqueuing the owning Application the moment the provisioner flips a claim ready. **S0 fix (latent 2.4b gap):** `operator-rendering::effective_spec` now merges `needs` on env override (per-key whole-object replace, mirroring `expose`) — env-scoped needs were silently dropped before. **Two decisions (reversible, noted in compatibility notes):** (1) `needs.*.selector` changes are NON-destructive in 2.4d (no MigrationPlan gate; revisit in 2.5+); (2) readiness predicate is ready AND connectionSecretRef. New operator RBAC: the `resourceclaims` rule gains `create` (SSA-apply that creates requires it) — split from `resourceclaims/status` so the Application controller never gets create on /status. **Scope held:** NO DSN/`DATABASE_URL` injection into the Deployment (that is 2.4e — a needs.pg app resumes WITHOUT DATABASE_URL until then); NO Application-side finalizer/GC (2.4f). Coordinated operator + platform-stack release v0.2.7 -> v0.2.8 (both charts version+appVersion, platform-stack component pins + currentVersion 0.2.7 -> 0.2.8 with a compatibility entry change=safe, operatorVersion v0.2.8); NO CRD schema change, NO cli/Cargo.toml bump, NO monorepo v0.x.y tag. The full generate -> provision -> resume loop is exercised by the 2.4g real-cluster walk. | operator/v0.2.8 + platform-stack/v0.2.8 (workflow-made on push; no monorepo v*) |
| 2026-06-03 | 2.4e (fifth sub-subphase of 2.4 needs.pg) — DATABASE_URL DSN injection into the needs.pg Deployment. Three coordinated changes, the renderer staying a PURE function: (A) `operator-rendering` gains a threaded `needs_secrets: Option<&BTreeMap<String,String>>` param (needs-type → connectionSecretRef) and, for each known need whose claim has a resolved connection Secret, appends a `valueFrom.secretKeyRef{name: <connectionSecretRef>, key: "DATABASE_URL", optional: false}` EnvVar to the workload container — appended AFTER any literal `env`, iterating `needs.keys()` (BTreeMap) so the rendered Deployment is byte-stable across reconciles (SSA no-op; non-deterministic order would spin the operator). A module-level pg→DATABASE_URL table (`NEEDS_ENV_VAR_NAME`) keeps it **pg-only** (jetstream/redis injection is 2.5/2.6). (B) the Application reconcile resolves the ready claims into that map via a new pure helper `resolve_needs_secrets(&[ResourceClaim])` (key=`spec.type_`, value=`status.connectionSecretRef`, skip claims without a secret ref) built from the SAME `current` ready claims the 2.4d gate validated, AFTER the gate passes — the operator only READS provisioner-owned claim status here, NEVER writes it (the SSA split is preserved); threads `Some(&map)` (or `None` when empty / pre-gate) into the render. (C) the admission webhook rejects an Application that declares `needs.pg` AND sets a literal `env.DATABASE_URL` (collision) — HARD reject (not warn; consistent with the platform's hard-enforce posture, revisit at UX polish), GLOBAL/cross-scope (pg in base OR any environment reserves DATABASE_URL everywhere), multi-error (one per offending field, no short-circuit). **Scope held:** pg-only; NO full 2.12 `claim.*`/`secret()` reference engine; NO cross-namespace logic; NO CRD/CUE/operator-core schema change; the 2.4d readiness AND-gate is NOT loosened (injection is strictly post-gate). Coordinated operator + platform-stack release v0.2.8 -> v0.2.9 (both charts version+appVersion, platform-stack component pins + currentVersion 0.2.8 -> 0.2.9 with a compatibility entry change=safe, operatorVersion v0.2.9); NO cli/Cargo.toml bump, NO monorepo v0.x.y tag. The full generate -> provision -> resume -> DSN-injected loop is exercised by the 2.4g real-cluster walk. | operator/v0.2.9 + platform-stack/v0.2.9 (workflow-made on push; no monorepo v*) |
| 2026-06-03 | 2.4f (sixth sub-subphase of 2.4 needs.pg) — RetainedClaim CRD + finalizer snapshot + 7-day grace GC controller. Closes the 2.4c cleanup skeleton (role/DB/Secret no longer leak on delete). A new immutable namespaced `RetainedClaim` CRD (5 hand-rolled mirrors: CUE `retainedclaim.cue` + cue-vet example, OpenAPI v3 `crd-retainedclaim.yaml` with an `x-kubernetes-validations` CEL `self == oldSelf` immutability rule + NO status subresource, kube-rs `operator-core` type, admission `validator_retainedclaim.rs` operator-only CREATE + spec-immutability-on-UPDATE wired into server.rs dispatch + a VWC entry). On a deleting pg ResourceClaim the provisioner finalizer SNAPSHOTS it into a RetainedClaim in **`apprafter-system`** (USER-CHOSEN, leak-safe — a platform namespace that outlives tenant namespaces, so GC fires even if the app ns is torn down; lineage preserved in `spec.claimRef`, `metadata.name = cnpg::k8s_name(claim_ns, claim_name)`, `retainUntil = deletionTimestamp + 7d`) **BEFORE** removing its finalizer (crash-safe: a crash mid-way re-applies the byte-identical idempotent SSA snapshot, then un-finalizes). A new **7th controller** (`gc.rs`, same crate — no new workspace member) watches `Api::<RetainedClaim>::all` and once `retainUntil` passes drops, in order, each step idempotent + 404-tolerant: the per-claim role (RMW the shared Cluster's unkeyed `spec.managed.roles` via new pure `cnpg::remove_role` + 409-retry), the database via **`spec.ensure: absent` SSA-patch — NOT a CR delete** (the Postgres reclaim default is `retain`, so deleting the CR would NOT drop the DB; ensure:absent is the correct drop, and the ~1 KB tombstone self-heals if the app returns), the password Secret (cnpg-ns, no ownerRef → no cascade), then the RetainedClaim itself. The un-e2e-able 7-day timer is factored into PURE `grace.rs` clock fns (`compute_retain_until`/`should_gc`/`remaining_grace`) taking an INJECTED `now: DateTime<Utc>` (production passes `Utc::now()`; unit-tested with FIXED instants — no real wait, no env-gated silent skip; a malformed `retainUntil` logs + requeues, never panics). New `apprafter_claim_gc_total{result,namespace}` metric; new RBAC (`retainedclaims` get/list/watch/create/patch/delete + `delete` on cnpg clusters/databases). SSA split preserved — the finalizer only CREATES the RetainedClaim (never writes ResourceClaim status); the GC reads RetainedClaim spec only. TDD on the pure pieces (`remove_role` 4 cases, the grace clock fns, the `retained_claim_object` snapshot builder, the webhook validator). Coordinated operator + platform-stack release v0.2.9 -> v0.2.10 (both charts version+appVersion, platform-stack component pins + currentVersion 0.2.9 -> 0.2.10 with a compatibility entry change=safe, operatorVersion v0.2.10); NO cli/Cargo.toml bump, NO monorepo v0.x.y tag. The delete -> snapshot -> GC loop (assert the DB is actually DROPPED, not just the CR) is exercised by the 2.4g real-cluster walk. | operator/v0.2.10 + platform-stack/v0.2.10 (workflow-made on push; no monorepo v*) |
| 2026-06-05 | 2.4g (closing sub-subphase of 2.4 needs.pg) — k3d `e2e/needs-pg-walk.sh` (Justfile + nightly CI) + the detailed real-Tier-1 manual walk (scaffold -> add -> provision -> DSN-inject -> delete -> recover -> force-GC). Live-validated the two 2.4f GC walk-fixes (operator v0.2.11): (A) recovery time-bomb defused — re-provision cancels the `RetainedClaim` + a fail-closed GC live-guard skips dropping a live re-attached claim's DB (verified via the `live ResourceClaim present (recovery) … skipping drop` log); (B) role-leak fixed — phased drop (Database `ensure: absent` -> CNPG drops the owner-role -> prune the entry only after CNPG confirms), so `pg_roles` is empty after a full GC. The walk also surfaced a PlatformController version-resolver thread: OCI tag-listing pagination + ghcr `tags: null` crash (v0.2.12), the ADR-0041 O(1) channel-tag resolver with publish-side `oras tag` channel moves (v0.2.13), and the pin-enforcement-survives-poll-failure resilience fix with the new `UpstreamReachable` condition (v0.2.14) — each adversarially verified. Accumulated CLI walk-fixes (app logs inner-name, app status pods+services+claims, scaffold --needs, app add publicness probe, skip-if-current loader, Tier-1 autoUpgrade opt-out, group-qualified Argo wait) ship as platform-cli v0.2.5. Closes 2.4g; 2.4 ≠ Phase 2 closure (§6 M2 stays open until 2.5/2.6/2.10–2.12). | v0.2.5 (monorepo, local) + operator/platform-stack v0.2.11–v0.2.14 (workflow-made on push) |
| 2026-06-05 | 2.4h (image tag→digest resolution & auto-rollout, ADR 0040) — the Application controller resolves `spec.base.image`'s tag to its current registry digest each reconcile (a manifest HEAD reading `Docker-Content-Digest`, anonymous or via a covering `SourceCredential`/`dockerconfigjson`), pins the Deployment to `repo@sha256:<digest>`, and records `status.image.{tag,resolved,resolvedAt}` + an `ImageResolved` condition — so a moved mutable tag auto-rolls the workload (push→deploy). Opt-out `spec.base.imagePolicy.resolve: digest|off` (default `digest`, all tiers); resolution is throttled to ~60s (mirrors the PlatformController), best-effort, and NEVER blocks the rollout (failure → verbatim tag + `ImageResolved=False`). The OCI resolver is a hand-rolled HEAD client (delegating image-ref parsing to `oci_distribution::Reference`; the HEAD reads the digest the tag points at, so multi-arch indexes resolve) over an injected `RegistryHttp` seam (unit-tested without a live registry). Built subagent-driven (12 TDD tasks) on `feat/2.4h-image-digest`; a whole-feature adversarial review (6 dimensions × 3-skeptic) found 14 confirmed issues — a critical no-throttle storm, plus tag-not-recorded-on-failure, digest-format-validation, per-env `imagePolicy` merge, and duplicate cred-listing majors — all fixed + verified before release. New `apprafter_image_resolve_total{result}` metric. Argo CD untouched (operator patches its own child Deployment out-of-band). CRD-additive (optional `imagePolicy` + `status.image` fields), change=safe, default-ON all tiers. 2.4 ≠ Phase 2 closure. | operator/platform-stack v0.2.15 (workflow-made on push) + cli v0.2.6 (monorepo, local) |
| 2026-06-05 | 2.4h hotfix — the v0.2.15 `Application` CRD shipped `imagePolicy` (base + per-env) and `status.image` with both `properties` and `additionalProperties: false`, which the apiserver rejects ("additionalProperties and properties are mutual exclusive"). So the v0.2.15 CRD apply failed server-side, the operator child app never synced (it stayed on the pre-2.4h operator), and image resolution silently did nothing (verbatim tag, no `status.image`). `helm lint` doesn't validate CRD structural schemas, so it slipped past every gate and only the e2e (post-publish) caught it. Remove the three invalid lines (closed CRD objects rely on structural-schema pruning), add `scripts/check-crd-structural.sh` (wired into `just lint`) to fail on `additionalProperties: false` in any CRD, and add `scripts/validate-crds.sh` / `just crd-validate` (ephemeral kind cluster applies every rendered CRD + asserts Established) as the real-apiserver gate to run before CRD-changing releases. The fixed CRD was validated against a real apiserver. Same operator code as v0.2.15; chart-only fix. A cluster stuck on the failed v0.2.15 sync recovers once v0.2.16 is channel-latest. | operator/platform-stack v0.2.16 (workflow-made on push) |
| 2026-06-05 | **2.6 needs.redis → Dragonfly (ADR 0042) — CLOSED** (sub-subphases 2.6-1…2.6-8). An `Application` declaring `needs.redis` now gets an isolated Redis-compatible connection on a pool of lazy shared Dragonfly instances, reusing the 2.3/2.4 machinery unchanged (generate → schedule → provision → DSN-inject → snapshot → GC). **2.6-1** (platform-stack-only): always-on `dragonfly-operator` component (`dragonfly-system`, sync-wave -5, OCI Helm repo via enableOCI) + a seeded `redis-integrated` ServiceProvider (backend `dragonfly`, `dbnum=1024`/`num_shards=1` tier-tunable knobs); no shared instance until the first claim (lazy). **2.6-2** (pure schema, CRD-additive): `persistent?` on `#ServiceNeed` (4 mirrors) + `status.{instance,dbnum}` allocation fields on ResourceClaim + a relaxed RetainedClaim (CNPG fields made optional, new optional dragonfly fields, `required` narrowed to [claimRef, provider, backend, retainUntil]) — validated against a real apiserver (`just crd-validate`). **2.6-3**: provisioner `Backend::Dragonfly` arm — lazy pool-instance create per persistence class + a pure dbnum allocator over the live-claim source of truth + Dragonfly RBAC. **2.6-4**: per-claim `$N`-pinned ACL user driven imperatively over the `redis` crate (new dep, trait-seam'd `RedisAdmin` for unit-testability), recycle-safe `FLUSHDB`-before-`SETUSER`, a two-key connection Secret (`REDIS_URL` DB-pinned + `REDIS_CHANNEL_PREFIX`), and the dragonfly RetainedClaim snapshot branch. **2.6-5**: an ACL reconcile loop re-pins claim users after an instance restart (runtime ACL state is lost on pod reload; password recovered from the connection-Secret DSN). **2.6-6**: renderer needs→env generalised to a list of (env-var, secret-key) pairs + the webhook reserved-env guard generalised to reject literal `REDIS_URL`/`REDIS_CHANNEL_PREFIX` under `needs.redis`. **2.6-7**: GC backend dispatch — the dragonfly path runs `FLUSHDB` + `ACL DELUSER` (both idempotent / instance-gone-tolerant) and deletes the connection Secret + snapshot; the freed dbnum returns to the pool implicitly. **2.6-8**: `e2e/needs-redis-walk.sh` (k3d, 10-phase: generate → schedule → provision → resume + DSN → `$N`-ACL cross-DB isolation proof → delete + snapshot → force-GC → FLUSHDB/DELUSER proof) wired into `Justfile` + a staggered `e2e-redis-nightly.yml`, an `operator-guide/needs-redis-walk.md` manual walk, and the coordinated release re-syncing the lockstep the 0.2.17 platform-stack-only yank broke. 2.6 ≠ Phase 2 closure (§6 M2 stays open until 2.5/2.10–2.12). No cli/Cargo.toml bump (CLI unchanged — redis claims surface via the generic 2.4g `app status`); no monorepo tag. | operator+admission-webhook charts v0.2.16 → v0.2.18, platform-stack currentVersion → 0.2.19 (compatibility entry change=safe, operatorVersion v0.2.18); operator/platform-stack v* workflow-made on push |
| 2026-06-06 | **2.6 needs.redis — live-walk validation + walk-fix release (v0.2.20).** The first end-to-end run of `e2e/needs-redis-walk.sh` on a real (kind/podman) cluster surfaced **5 operator/webhook integration bugs every unit / CRD / whole-feature-review gate had passed**: (1) the Dragonfly CR omitted `spec.replicas` → a 0-replica StatefulSet (the operator wires `replicas` with no default) → no instance pod → the provisioner could never reach it for ACL; (2) it passed `--maxmemory_policy=noeviction`, a Redis-ism Dragonfly rejects ("Unknown command line flag") → crashloop (Dragonfly is noeviction by default via `--cache_mode=false`); (3) the ACL grant used `+client|setname`-style per-subcommand tokens Dragonfly's parser rejects — dropped (CLIENT init + EVAL stay usable under `+@all -@dangerous`); (4) the dbnum allocator's read-allocate-write raced under kube-rs' default unbounded reconcile concurrency, so two `needs.redis` claims on one instance both picked the same DB — an **isolation breach** — fixed by `Controller` `concurrency(1)` (leader election makes allocation fully serial); (5) the operator-only-CREATE webhooks gated break-glass on `system:masters`, but kubeadm ≥1.29 (k8s 1.35 / kind) issues admin.conf under `kubeadm:cluster-admins` — accept both. With these fixed the walk is GREEN: provision → `$N` isolation (SELECT/MOVE/COPY/SWAPDB → NOPERM) → EVAL DB-confinement + undeclared-key reject → client-init/BLPOP queues → restart re-pin → persistent restart-durable → §8 reattach → delete+snapshot → GC FLUSHDB/DELUSER (scoped). The e2e harness also gained kind+podman auto-run (no docker daemon needed) and an `APPRAFTER_E2E_LOCAL_OPERATOR=1` build-from-working-tree mode for pre-push validation. Lesson: every automated gate passed yet 5 real operator bugs survived to the live walk — the manual walk is irreplaceable. v0.2.19 is broken (the replicas bug) → v0.2.20 supersedes it. | operator+admission-webhook charts v0.2.19→v0.2.20, platform-stack currentVersion 0.2.19→0.2.20 (compatibility change=safe, operatorVersion v0.2.20); operator/platform-stack v* workflow-made on push |
| 2026-06-06 | **2.6b needs.disk + named multi-claims (ADR 0043) — CLOSED** (launch slice, sub-subphases 2.6b-1…2.6b-6). `needs` generalises from a `[type]: ServiceNeed` map to a **closed struct of `(type, name)` entries**, each `scalar OR array` (`OneOrMany<T>`): the Application controller generates one `ResourceClaim` per entry (`<app>-<type>[-<name>]`) and the renderer disambiguates injected env-vars — a named pg claim injects `DATABASE_URL_<NAME>` (UPPER_SNAKE), an unnamed single stays `DATABASE_URL`. **Disk is a ResourceClaim, not a renderer-only StatefulSet** (the launch-minimal slice of the broad plan.md sketch, per ADR 0043 §Decision): **2.6b-1** schema across the mirrors (CUE `#DiskClaim` + scalar\|array `needs` keys, kube-rs `OneOrMany`/`Needs`/`NeedEntry`, CRD `needs` via `x-kubernetes-preserve-unknown-fields` per key + `status.volumeClaimRef`); **2.6b-2** the (type,name) claim-gen engine + env disambiguation + webhook (type,name) guards (acceptance proven on a pg array BEFORE disk); **2.6b-3** provisioner `Backend::Disk` arm (SSA-applies an **unowned** RWO PVC on the provider's storage class, writes `status.volumeClaimRef`, single-pass ready — does not wait for the `WaitForFirstConsumer` bind) + the seeded `disk-local` ServiceProvider (T1 local-path); **2.6b-4** the renderer mounts the PVC + pivots the Deployment to `strategy:Recreate` + disk webhook guards (replicas>1 reject, unique name/mountPath, size as a k8s quantity); **2.6b-5** disk GC — RetainedClaim disk fields (volumeClaimRef + namespace), `gc_drop_disk` deletes the unowned PVC, cancel-on-reprovision + a live-guard make reattach safe; **2.6b-6** `e2e/needs-disk-walk.sh` (kind+podman, LOCAL_OPERATOR) GREEN end-to-end in ~3.5 min (generate → schedule → provision → resume+mount+Recreate → data durability across a pod restart → delete + RetainedClaim + unowned PVC survives grace → reattach with data intact → force-GC drops the PVC) + a needs.pg-array multi-claim assertion (2 CNPG DBs + `DATABASE_URL_PRIMARY`/`_ANALYTICS`), wired into `Justfile` + a staggered `e2e-disk-nightly.yml` (07:00 UTC); an `operator-guide/needs-disk-walk.md` manual walk + a dev-guide `needs` reference. **The live walk surfaced 3 operator/CRD bugs every unit/CRD/review gate had passed** — the readiness gate required `status.connectionSecretRef`, which a disk claim never sets (its output ref is `status.volumeClaimRef`), so a `needs.disk` app hung in AwaitingResourceClaim forever; the ServiceProvider CRD `type` enum lacked `disk`; the ResourceClaim `spec.size` t-shirt enum rejected `1Gi` (relaxed to per-type: quantity for disk, t-shirt for service) — plus a harness-only Phase-4 flaw (disk provisions instantly — single-pass local-path + a pre-warmed CNPG — so the transient `AwaitingResourceClaim` phase is unobservable by polling; the gate is now proven deterministically by claim-generation + the Phase-7 mount/env assertions, the pause logic by unit tests). `app scaffold --needs` was widened to `redis` + `disk` (closing a pre-existing mismatch: the redis walk doc already documented `--needs redis`, which the CLI rejected). Release gates: cargo fmt/clippy -D/test --workspace (cli+operator) + just lint + cue vet + `just crd-validate` (7 CRDs Established) + `just platform-stack-check` + strict mkdocs. 2.6b ≠ Phase 2 closure (§6 M2 stays open until 2.5/2.10–2.12). | operator+admission-webhook charts v0.2.20→v0.2.21, platform-stack currentVersion 0.2.20→0.2.21 (compatibility change=safe, operatorVersion v0.2.21); cli v0.2.6→v0.2.7 (monorepo tag v0.2.7, local — scaffold changed the binary); operator/platform-stack v* workflow-made on push |
| 2026-06-07 | **2.6b validated on the published release + e2e follow-ups.** After the user pushed (CI published operator/admission-webhook v0.2.21 + platform-stack 0.2.21; `operator/v0.2.21` + `platform-stack/v0.2.21` tags on origin), all four e2e walks ran GREEN on the user's kind+podman host (disk/gitops/pg/redis) against the published 0.2.21. Two post-release follow-ups: (1) the operator + admission-webhook Dockerfiles used an unqualified `FROM rust:1.88-alpine3.21`, which makes podman with multiple `unqualified-search-registries` PROMPT interactively for the registry — hanging the LOCAL_OPERATOR disk-walk (~35 min on 2-3 prompts); qualified to `docker.io/library/rust:...` (a no-op for docker buildx and the produced image). (2) Now that 2.6b ships in the released image, `just e2e-disk` + `e2e-disk-nightly.yml` default to the PUBLISHED operator like the pg/redis walks; the build + branch-CRD-apply + additive-RBAC are gated behind APPRAFTER_E2E_LOCAL_OPERATOR (pre-release working-tree validation only). Verified: a non-LOCAL disk-walk bootstraps platform-stack 0.2.21 from ghcr, skips Phase 1b, and goes GREEN in 2m52s — independently proving the released v0.2.21 operator implements the full needs.disk chain (the shipped operator ClusterRole already grants the PVC create/delete RBAC). | de943fc + 9f2293f on origin/master (post-release; no version bump — the Dockerfile change produces an identical image, the rest is e2e/CI infra) |
| 2026-06-07 | **2.9 per-environment deploy (ADR 0044) — CLOSED** (subagent-driven, T1–T12). Environment selection becomes a **deploy-time, per-Application** property — a new optional `Application.spec.environment` (injected by the cue-cmp from the Argo Application's `APPRAFTER_APP_ENV` plugin env) selects which `spec.environments.<env>` override unifies onto base via the operator's `effective_spec`; the **inert cluster-wide `APPRAFTER_ENV`** operator var + `Context.env_name` are removed (and the application-scoped MigrationPlan gate `find_blocking_migration_plan` rewired to `app.spec.environment` — its `if let Some(e)` guard keeps base-only matching on name+namespace, no regression). **Scalar model** (ADR 0044, not a single-CR fan-out — cross-namespace ownerReferences are disallowed): the SAME env-agnostic manifest is deployed per env via `apprafter app add --env <env>` as ONE self-contained Argo CD Application `<name>-<env>` in a user-chosen namespace, grouped by the existing `apprafter.io/application` + `apprafter.io/environment` labels; `app status`/`remove` aggregate by `apprafter.io/application`; a new optional `PlatformStack.spec.defaultEnvironment` is the CLI's soft default; the admission webhook rejects a `spec.environment` not in `spec.environments`. CLI: `app add --env` + interactive env (from declared) / namespace (existing-or-new) pickers. **The live per-env e2e walk (`e2e/gitops-walk-per-env.sh`, Approach A — side-loads operator + webhook + the working-tree cue-cmp) surfaced a CRITICAL bug that every host test had masked**: Argo CD exposes `spec.source.plugin.env` vars **prefixed with `ARGOCD_ENV_`**, so the cue-cmp read the bare `$APPRAFTER_APP_ENV` and **never injected `spec.environment` in-cluster** — 2.9 would have shipped completely inert (every deploy rendered base, `--env` silently ignored). Fixed (cue-cmp reads the `ARGOCD_ENV_`-prefixed form, falls back to bare) + an `ARGOCD_ENV_` regression case (`test-inject.sh` now 15/15, wired into the CMP CI check); walk then GREEN end to end (replicas 1 vs 2 + TIER dev/prod per env, status aggregation, surgical `remove --env`). CRD-additive (optional `Application.spec.environment` + `status.environment`, optional `PlatformStack.spec.defaultEnvironment`), change=safe. 2.9 ≠ Phase 2 closure (§6 M2 stays open until 2.10–2.12); spec.md §3.1 reconciliation (CUE-unification→override-wins replace; substrate→scalar user-chosen ns) deferred to Phase-2-close actualization (tracked in ADR 0044 §References). | operator+admission-webhook charts v0.2.21→v0.2.22, platform-stack currentVersion 0.2.21→0.2.22 (compatibility change=safe, operatorVersion v0.2.22), **argocd-cue-cmp 0.1.7→0.1.8** (the ARGOCD_ENV_ injection fix — platform-stack derives the tag from version.cue); cli v0.2.7→v0.2.8 (monorepo tag v0.2.8, local); operator/platform-stack/argocd-cue-cmp v* workflow-made on push |
| 2026-06-07 | **2.9 merge-verified + adversarial-review follow-ups.** After the push, all release workflows succeeded (operator/webhook v0.2.22 + images, platform-stack 0.2.22, cli v0.2.8, **cue-cmp v0.1.8** — the in-cluster injection fix now in a real image); the lone red `e2e-k3d` was a confirmed k3s-apiserver infra flake (failed in the argocd helm-install, before any 2.9 code) and went GREEN on re-run. A 6-dimension adversarial review (each finding independently verified, default-refute) confirmed 2 real CLI gaps (3 rejected, incl. a refuted selector-eviction worry; 1 "stale comment" was a working-tree artifact the review's own verify-agents introduced — origin/master was clean): (1) `app add --env` was not DNS-1123-validated, so on the manifest-unrenderable degrade path an invalid env reached `kubectl apply` with a cryptic RFC-1123 error → now `validate_dns_1123(env)` fails fast; (2) `app logs`/`app rollback` looked up the bare `<name>` and so failed for per-env apps (`<name>-<env>`) — now `--env`-aware via a shared `resolve_app_for_command` (resolves `<name>-<env>`, patches/streams the resolved Argo app, and emits a per-env guidance error when `--env` is omitted on a per-env app). | cli v0.2.8→v0.2.9 (fixes bddc8fe + 21ca63d; monorepo tag v0.2.9, local; CLI-only — no operator/platform-stack/cue-cmp change); release-cli workflow-made on push |
| 2026-06-09 | **2.10 needs → CiliumNetworkPolicy egress auto-derivation (ADR 0045) — CLOSED** (subagent-driven, T1–T5). The Application controller emits ONE `CiliumNetworkPolicy` per Application (`<rendered-name>-egress`, owner-referenced, built via `serde_json::json!` → `DynamicObject` per the external-CR precedent): pods go egress default-deny and are allowed only DNS + same-namespace (profile-gated) + the external internet (profile-gated) + one rule per declared network `need` (pg → cnpg-system:5432, redis → dragonfly-system:6379; disk has no network surface). The apply is gated on a one-time startup probe of the `ciliumnetworkpolicies.cilium.io` CRD, so non-Cilium clusters render but never apply. Breadth is config-driven via a new optional `PlatformStack.spec.network.egress.profile` (`internet` default \| `internal` \| `strict`), resolved cluster-wide; managed by `apprafter platform egress show` / `set <profile>` (no raw kubectl). **The live kind+Cilium enforcement walk (`e2e/needs-networkpolicy-walk.sh`, 8 phases, run in a rootful sandbox-run microVM) surfaced 2 product bugs every unit / CRD / review gate had passed**: (1) the pg connection-target selected `postgresql.cnpg.io/cluster` (deprecated) instead of the live `cnpg.io/cluster`, so the egress allow-rule matched zero endpoints and every `needs.pg` app's egress to Postgres was silently dropped — the unit tests encoded the same wrong literal, so only the walk caught it; (2) `apprafter platform egress set` applied its partial PlatformStack under the bootstrap field manager `apprafter-cli`, so SSA pruned the REQUIRED `spec.source` / `spec.values` and the apiserver rejected the object — fixed with a dedicated `apprafter-cli-egress` manager + a regression test. Plus a walk-harness fix (branch RBAC rendered without `-n` defaulted Release.Namespace to `default`, and `kubectl apply --force-conflicts` then overwrote the ClusterRoleBinding subject → operator 403 on every cluster-scoped grant). Walk then GREEN end-to-end (CNP emission → noproxy→pg DROPPED / web→pg FORWARDED → internet/internal/strict switching via the CLI). CRD-additive (optional `network.egress.profile`), change=safe. 2.10 ≠ Phase 2 closure (§6 M2 stays open until 2.11–2.12). | operator+admission-webhook charts v0.2.22→v0.2.23, platform-stack currentVersion 0.2.22→0.2.23 (compatibility change=safe, operatorVersion v0.2.23); cli v0.2.9→v0.2.10 (monorepo tag v0.2.10, local); operator/platform-stack v* workflow-made on push |
| 2026-06-09 | **2.11 SealedSecrets (Tier-1 secrets) — CLOSED (launch-scope shipped; Backstage UI deferred post-launch).** The launch secrets-management capability (FEATURE_TRACKER CP2) was already shipped as the forward-carved S0 slice (v0.1.175): the bitnami sealed-secrets controller (`component_sealed-secrets.cue`, `fullnameOverride` pins the name so the CLI can fetch the cert), native client-side sealing in `cli-providers::k8s::sealing` (RSA-OAEP-SHA256 ⊗ AES-256-GCM, bitnami wire format), and `apprafter secret seal`. The CLI never holds the private key — it fetches the controller cert live via the kube API service-proxy. Deliverable #3 (publish a static public key to `manifests/tier-1/sealed-secrets/`) is **obsolete** — superseded by that live fetch. The two remaining deliverables — the Backstage "Encrypt secret" wizard (#4) and the SealedSecrets posture warning (#5) — are **deferred to the post-launch portal bundle (PL1)**: they are not in the launch-minimum Backstage plugin set (launch-checklist §1.1 = Application/ResourceClaim/AccessGrant/Notifications) and ride with the other deferred portal surfaces (the MigrationPlan approval UI; ADR 0036 "a Backstage approval surface follows in the post-launch bundle"; hosted-portal buildout = CP4). Docs-only closure — the controller + seal shipped earlier, nothing new built. 2.11 ≠ Phase 2 closure (§6 M2 stays open until 2.12). | docs-only (closure); secrets capability shipped earlier in v0.1.175 — no version bump, no monorepo tag |
| 2026-06-10 | **2.12 Application.env value references (ADR 0046) — CLOSED; closes the M2 platform-services core (gate 2.10–2.12).** (subagent-driven, T0–T8.) An `Application.spec.*.env` value is now a literal string, a **claim reference** (a bare CUE selector `claim.<type>.<field>` — composed `url` or decomposed `user`/`pass`/`host`/`port`/`db`/`channelPrefix`; named `claim.<type>.<name>.<field>` for array claims; pg + redis only at launch), or an **external-secret reference** (braceless `secret: "<name>/<key>"`). At the CR level each ref is a string-discriminated marker (`{claim: "pg.url"}` / `{secret: "stripe/api-key"}`); the operator's pure renderer resolves it to a container `EnvVar{valueFrom: secretKeyRef}`. The provisioner writes CANONICAL decomposed connection-Secret keys (`url user pass host port db` + redis `channelPrefix`) and drops the composed `DATABASE_URL`/`REDIS_URL`/`REDIS_CHANNEL_PREFIX` key names (`acl_reconcile` reads `pass` directly). **The 2.4e `DATABASE_URL` auto-injection is removed** (this is its proper replacement; the literal-collision webhook guard is gone). The cue-cmp (**0.1.8→0.1.9**) injects the current schema + a generated `claim` binding (the comprehension runs in the manifest's package referencing an exported `#ClaimFieldsFor` table — a cross-package `#MkClaim & {_needs}` does not re-evaluate — via a two-pass needs-extraction over an env-agnostic union); **`apprafter app scaffold` stops vendoring the schema**, and a new **`apprafter app validate`** runs the same injection locally (parity-verified against the cue-cmp fixtures). Validation is layered: `cue vet` (claim refs) + admission webhook (format) + runtime (missing external Secret → Application `Ready=False` reason `EnvSecretMissing`). **Live walks GREEN on kind+podman** (`needs-env-refs-walk` — three sources + missing-secret→NotReady→recover; `needs-disk-walk` — named `claim.pg.<name>.url`; `needs-redis-walk` — `claim.redis.url`/`channelPrefix`), all updated for the removed auto-inject; **no product bugs surfaced** (unit suites covered them). CRD-additive (`env` value node → `x-kubernetes-preserve-unknown-fields`), change=safe. **2.12 closes the M2 platform-services core (plan gate 2.10–2.12): §6 M2 box flipped + spec.md §3.1/§4.4/§4.5 actualized (Revision 11). Post-gate Phase-2 items follow (2.16a per-claim rotation = launch follow-on; 2.17 = M2-checklist housekeeping; 2.13–2.16 Notifications SR:D-dropped) — not M2-functionality gating.** | operator+admission-webhook charts v0.2.23→v0.2.24, platform-stack currentVersion 0.2.23→0.2.24 (compatibility change=safe, operatorVersion v0.2.24), **argocd-cue-cmp 0.1.8→0.1.9** (schema injection — platform-stack derives the tag from version.cue); cli v0.2.10→v0.2.11 (monorepo tag v0.2.11, local — `app validate` + scaffold changed the binary); operator/platform-stack/argocd-cue-cmp v* workflow-made on push |
| 2026-06-12 | **Platform force-recheck + gated-upgrade freeze fix (0.2.28).** `apprafter platform status`/`upgrade` now stamp an `apprafter.io/recheck-requested` annotation → the operator force-re-polls the OCI upstream (bypasses the 60s throttle + the ~6h `checkInterval`), so neither command shows a stale `availableVersion` (`--cached` opts out; graceful on an old operator). **Also fixes the live-walk-found reconcile-FREEZE on gated upgrades:** the ADR 0048 approval surface (operator v0.2.25 / platform-stack 0.2.26) GETs the `platform-migration-anchor` ConfigMap for the MigrationPlan ownerRef, but the operator ClusterRole lacked `configmaps` AND the GET was fatal — so on any `requires-restart` upgrade the PlatformController 403'd and aborted BEFORE the status write, freezing `availableVersion` + the gate it couldn't create (GitOps deadlock; manual configmaps-RBAC break-glass required to unstick). Fix = add `configmaps` get/list/watch to the operator ClusterRole + make the anchor ownerRef best-effort (`anchor_uid_from_get`: `Ok(None)`/`Err` → un-owned, still CLI-approvable plan; reconcile + status always proceed; 3 TDD tests). **Plus** a second walk-found A4 bug: the root-App "pending approval" banner never showed because the `apprafter.io/upgrade-*` annotation surface was stamped only inside the `spec.source` patch, which a HELD (gated) upgrade skips — now reconciled independently via a `pending_upgrade_annotations_differ` diff (SET gated / PRUNE on approval). **Yanks platform-stack 0.2.26 + 0.2.27** (both ship the buggy operator v0.2.25; 0.2.27's F1/F2 were chart-only). Diagnosis fully exonerated the publish/resolver path (`:stable` digest == 0.2.27, compat doc + cap correct) — the freeze was the only fault. | operator+admission-webhook charts v0.2.25→v0.2.26, platform-stack currentVersion 0.2.27→0.2.28 (compatibility change=requires-restart, operatorVersion v0.2.26; 0.2.26 + 0.2.27 marked yanked); cli v0.2.12→v0.2.13 (monorepo tag v0.2.13, local — force-recheck changed the binary); operator/platform-stack v* workflow-made on push |
| 2026-06-12 | **Two more live-walk operator fixes (0.2.29, requires-restart).** (1) **GC of superseded platform MigrationPlans** — plan names are keyed on (from→to), so when a yank advanced the target 0.2.27→0.2.28 the prior pending-approval plan was orphaned beside the new one (user saw 26-27 + 26-28); the controller now lists platform plans and deletes any pending-approval plan ≠ the current gate (`superseded_platform_plan_names`; approved/executing/completed/rejected + app-scope untouched). (2) **Stable minimal Deployment selector** — the renderer reused the FULL `make_labels` set as the IMMUTABLE `spec.selector`, so 2.9's label-set widening (`apprafter.io/application` + `.../environment`) wedged every pre-existing Deployment (422 `field is immutable` every reconcile → no image roll / no spec change; hit the user's landing-* apps). Selector is now `{apprafter.io/application: <name>}` (== the Deployment name, unique per ns, never grows); the Application controller delete+recreates any Deployment whose live selector differs (`selector_needs_migration`). **Upgrade recreates EVERY Deployment once (brief per-app downtime).** Both unit-tested. Operator-only (no CLI). | operator+admission-webhook charts v0.2.26→v0.2.27 (lockstep, build.rs-asserted), platform-stack currentVersion 0.2.28→0.2.29 (compatibility change=requires-restart, operatorVersion v0.2.27); operator/platform-stack v* workflow-made on push |
| 2026-06-12 | **Root App tile pending-upgrade signal + broadened GC (0.2.30, requires-restart).** The ADR 0048 `argoproj.io_Application` health "banner" was **empirically disproven on a kind+Argo probe** (the lesson from A5 applied — test, don't assume): Argo applies that customization only to Application resources that are CHILDREN in another app's tree, never to a top-level app's OWN tile (its health = worst-of aggregate of managed `.status.resources`) — so the root App stayed Healthy despite the annotation. Validated fix: the operator stamps `apprafter.io/upgrade-pending` (+from/to/class/plan) on the chart-managed `platform-migration-anchor` ConfigMap (it IS in the root App's `.status.resources`) + a `ConfigMap` health customization returns **Suspended** for it → the root App tile rolls up to Suspended (purple pause) in the Applications LIST. Live-confirmed end-to-end: SSA annotation survives Argo syncs with no OutOfSync (no ignoreDifferences); key is `ConfigMap` NOT `_ConfigMap` (the latter silently yields nil — caught only by the live probe); SET→Suspended / CLEAR→Healthy. Dead `argoproj.io_Application` customization removed; operator RBAC += `configmaps` update/patch. **Plus** the MigrationPlan GC is broadened + runs every reconcile (keep only the current gate + mid-rollout approved/executing; delete stale-pending AND terminal completed/rejected/failed) so the tree shows ≤1 active gate (walk-found: 24-25+25-26+26-28 completed all lingered). Both unit-tested + the tile mechanism + exact chart Lua validated on a live Argo. Operator-only. | operator+admission-webhook charts v0.2.27→v0.2.28 (lockstep), platform-stack currentVersion 0.2.29→0.2.30 (compatibility change=requires-restart, operatorVersion v0.2.28); operator/platform-stack v* workflow-made on push |
| 2026-06-12 | **MigrationPlan GC delete RBAC fix (0.2.31, requires-restart).** The GC shipped in 0.2.29 + broadened in 0.2.30, but the operator ClusterRole granted only get/list/watch/create/patch/update on `migrationplans` — **no `delete`** — so every GC delete 403'd (best-effort, logged) and completed plans piled up anyway (found after reaching 0.2.30: 28-29 + 29-30 lingered). Adds the `delete` verb; the running operator prunes superseded plans on its next reconcile. **Same class as the 0.2.28 configmaps-RBAC freeze** — a code path needing a verb the chart RBAC didn't grant (2nd occurrence; lesson: when operator code gains a new verb on a resource, the ClusterRole must gain it too). Operator image rebuilds binary-identical (RBAC is chart-template-only) but the tag bumps → pods restart → requires-restart. | operator+admission-webhook charts v0.2.28→v0.2.29 (lockstep), platform-stack currentVersion 0.2.30→0.2.31 (compatibility change=requires-restart, operatorVersion v0.2.29); operator/platform-stack v* workflow-made on push |
