{
  "name": "${{ values.name }}",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "${{ values.description }}",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@onebun/core": "^0.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
