{
  "name": "@apprafter/bun-http-starter",
  "version": "0.1.37",
  "private": true,
  "license": "MIT",
  "description": "AppRafter golden-path starter — minimal OneBun HTTP service.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@onebun/core": "^0.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
