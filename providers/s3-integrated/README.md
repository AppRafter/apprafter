# providers/s3-integrated/

S3-compatible ServiceProvider backed by MinIO or Garage. Each `ResourceClaim` gets a dedicated bucket and IAM user. Used by application workloads and for platform backups.
