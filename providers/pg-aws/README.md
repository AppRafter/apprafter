# providers/pg-aws/

PostgreSQL ServiceProvider that delegates to AWS RDS. Intended for Tier 4 compliance-driven workloads. Provisioning via the AWS SDK; runtime credentials are issued through workload identity.
