# schemas/

CUE schemas for every platform CRD (`Application`, `ServiceProvider`, `ResourceClaim`, `AccessGrant`, `MigrationPlan`, `ExternalSurface`, `Infrastructure`, `*Plugin`). The schemas are the source of truth: they drive admission validation, OpenAPI v3 CRD generation, and code generation for the operator.
