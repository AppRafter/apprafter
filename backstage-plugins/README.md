# backstage-plugins/

TypeScript plugins for Backstage: `Application` view, `ResourceClaim` view, `AccessGrant` self-service, build report, cost view, Hubble flow visualizer, software templates. Plugin backends use OneBun where applicable.
