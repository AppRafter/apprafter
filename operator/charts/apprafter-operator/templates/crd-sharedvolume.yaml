# GENERATED by crdgen from schemas/v1alpha1 (SharedVolume via _crdMetas) — DO NOT EDIT.
# Run `just gen-crds` to regenerate; edit the CUE schema instead.
# SPDX-License-Identifier: MIT
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    argocd.argoproj.io/sync-options: ServerSideApply=true
    argocd.argoproj.io/sync-wave: '-5'
  labels:
    {{- include "apprafter-operator.labels" . | nindent 4 }}
  name: sharedvolumes.apprafter.io
spec:
  group: apprafter.io
  names:
    kind: SharedVolume
    listKind: SharedVolumeList
    plural: sharedvolumes
    shortNames:
    - sv
    singular: sharedvolume
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: string
    - jsonPath: .spec.size
      name: Size
      type: string
    - jsonPath: .status.refCount
      name: Refs
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              class:
                type: string
              size:
                type: string
            required:
            - size
            type: object
          status:
            properties:
              capacity:
                properties:
                  capacityBytes:
                    type: integer
                  usedBytes:
                    type: integer
                type: object
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      type: string
                  required:
                  - type
                  - status
                  - lastTransitionTime
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              pvcRef:
                type: string
              ready:
                type: boolean
              refCount:
                minimum: 0
                type: integer
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
