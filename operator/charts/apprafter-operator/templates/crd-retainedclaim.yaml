# GENERATED by crdgen from schemas/v1alpha1 (RetainedClaim via _crdMetas) — DO NOT EDIT.
# Run `just gen-crds` to regenerate; edit the CUE schema instead.
# SPDX-License-Identifier: MIT
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    argocd.argoproj.io/sync-options: ServerSideApply=true
    argocd.argoproj.io/sync-wave: '-5'
  labels:
    {{- include "apprafter-operator.labels" . | nindent 4 }}
  name: retainedclaims.apprafter.io
spec:
  group: apprafter.io
  names:
    kind: RetainedClaim
    listKind: RetainedClaimList
    plural: retainedclaims
    shortNames:
    - rclaim
    singular: retainedclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.claimRef.name
      name: SourceClaim
      type: string
    - jsonPath: .spec.backend
      name: Backend
      type: string
    - jsonPath: .spec.retainUntil
      name: RetainUntil
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              aclUser:
                type: string
              backend:
                type: string
              claimRef:
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              cnpgCluster:
                type: string
              cnpgNamespace:
                type: string
              connectionSecretNamespace:
                type: string
              connectionSecretRef:
                type: string
              database:
                type: string
              databaseObjectName:
                type: string
              dbnum:
                exclusiveMaximum: true
                maximum: 1024
                minimum: 0
                type: integer
              instance:
                type: string
              passwordSecretName:
                type: string
              provider:
                type: string
              retainUntil:
                type: string
              role:
                type: string
              volumeClaimNamespace:
                type: string
              volumeClaimRef:
                type: string
            required:
            - claimRef
            - provider
            - backend
            - retainUntil
            type: object
            x-kubernetes-validations:
            - message: RetainedClaim spec is immutable
              rule: self == oldSelf
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
