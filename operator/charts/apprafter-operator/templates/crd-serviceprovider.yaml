# GENERATED by crdgen from schemas/v1alpha1 (ServiceProvider via _crdMetas) — DO NOT EDIT.
# Run `just gen-crds` to regenerate; edit the CUE schema instead.
# SPDX-License-Identifier: MIT
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    argocd.argoproj.io/sync-options: ServerSideApply=true
    argocd.argoproj.io/sync-wave: '-5'
  labels:
    {{- include "apprafter-operator.labels" . | nindent 4 }}
  name: serviceproviders.apprafter.io
spec:
  group: apprafter.io
  names:
    kind: ServiceProvider
    listKind: ServiceProviderList
    plural: serviceproviders
    shortNames:
    - sp
    singular: serviceprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.backend
      name: Backend
      type: string
    - jsonPath: .status.health
      name: Health
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              backend:
                minLength: 1
                type: string
              config:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              type:
                enum:
                - pg
                - jetstream
                - clickhouse
                - redis
                - s3
                - notifications
                - disk
                - shared-disk
                type: string
            required:
            - type
            - backend
            type: object
          status:
            properties:
              health:
                enum:
                - healthy
                - degraded
                - unhealthy
                - unknown
                type: string
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
