# GENERATED by crdgen from schemas/v1alpha1 (SourceCredential via _crdMetas) — DO NOT EDIT.
# Run `just gen-crds` to regenerate; edit the CUE schema instead.
# SPDX-License-Identifier: MIT
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    argocd.argoproj.io/sync-options: ServerSideApply=true
    argocd.argoproj.io/sync-wave: '-5'
  labels:
    {{- include "apprafter-operator.labels" . | nindent 4 }}
  name: sourcecredentials.apprafter.io
spec:
  group: apprafter.io
  names:
    kind: SourceCredential
    listKind: SourceCredentialList
    plural: sourcecredentials
    shortNames:
    - srccred
    singular: sourcecredential
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              git:
                properties:
                  backend:
                    x-kubernetes-preserve-unknown-fields: true
                  repoPrefixes:
                    items:
                      pattern: ^.+$
                      type: string
                    minItems: 1
                    type: array
                required:
                - backend
                - repoPrefixes
                type: object
              registry:
                properties:
                  backend:
                    x-kubernetes-preserve-unknown-fields: true
                  hosts:
                    items:
                      pattern: ^.+$
                      type: string
                    minItems: 1
                    type: array
                required:
                - backend
                - hosts
                type: object
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      type: string
                  required:
                  - type
                  - status
                  - lastTransitionTime
                  type: object
                type: array
              coveredHosts:
                items:
                  type: string
                type: array
              coveredRepoPrefixes:
                items:
                  type: string
                type: array
              lastAppliedSpec:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              lastValidated:
                type: string
              phase:
                type: string
            type: object
            x-kubernetes-preserve-unknown-fields: true
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
