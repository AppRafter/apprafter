// AppRafter Application manifest scaffolded by `apprafter
// app scaffold` (blank template — runtime auto-detection
// surfaced no markers).
//
// This is your code under your license — feel free to add
// your own SPDX-License-Identifier header on top. Every
// field below carries a TODO; fill them in before
// committing. The CMP plugin renders this file at sync
// time. Check it locally with `apprafter app validate`, which
// lays out a workspace and injects the AppRafter v1alpha1
// schemas for you — a bare `cue vet` here fails, because
// nothing is vendored beside this file.

package apprafter

import v1alpha1 "apprafter.io/schemas/v1alpha1"

{{app_name_camel}}: v1alpha1.#Application & {
	metadata: {
		// TODO: DNS-1123 lowercase name. Free to rename.
		name: "{{app_name}}"

		// TODO: namespace the AppRafter operator watches.
		// Match `apprafter app add --namespace` (default
		// `apprafter`) unless you've carved out a tenant-
		// specific namespace.
		namespace: "{{namespace}}"

		labels: {
			"apprafter.io/component": "user-app"
		}
	}

	spec: base: {
		{{image_comment}}
		image: "{{image_placeholder}}"

		// TODO: replica count. Start with 1; scale once you
		// have observed load.
		replicas: 1

		expose: {
			// TODO: container port your app listens on.
			port: {{primary_port}}

			// TODO: to expose publicly, set network: "public" and add
			// a hostname (under a registered allowedDomains zone).
			// network:  "public"
			// hostname: "app.example.com"
		}{{needs_block}}

		// TODO: env map. Literal values only in v1alpha1.
		// env: {
		//     "LOG_LEVEL": "info"
		// }
	}

	// TODO: per-environment overrides.
	// spec: environments: {
	//     dev:  { replicas: 1 }
	//     prod: { replicas: 3 }
	// }
}
