# The same fence, with an info string

Identical body, identical command, one `sh` added to the opening
delimiter. Nothing else about the fence differs, so the finding on the
twin is provably about the info string and not about what it holds.

```sh
apprafter app list
```
