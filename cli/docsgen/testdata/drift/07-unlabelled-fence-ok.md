# The same fence, with an info string

Identical body, identical command, one `sh` added to the opening
delimiter. Nothing else in this pair differs, so the finding on the twin
is provably about the info string and not about what the fence holds.

```sh
apprafter app list
```
