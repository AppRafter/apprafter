# A fence with no info string

The command inside resolves — this page is wrong about nothing except the
missing tag. The tag is needed for highlighting regardless, and requiring
it removes the one edit that could otherwise turn a finding green while
looking innocent in a diff.

```
apprafter app list
```
