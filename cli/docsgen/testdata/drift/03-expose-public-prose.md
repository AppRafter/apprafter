# A drifted field in a prose bullet

The scanner emits no plain-prose block at all — only fences, spans and
front matter. A backticked token in a bullet arrives as an inline span,
which is the surface most of this corpus writes field names on.

- `expose.public`: set it to `true` and the operator renders an
  HTTPRoute on the platform Gateway.
