# A command that never shipped

A `promote` subcommand was advertised as the promotion path between
environments. It is not in the clap tree and never has been, so a reader
following this page gets an unknown-subcommand error. The invocation is
left to the fence alone, so the finding cannot come from anywhere else on
the page.

```sh
apprafter promote staging
```
