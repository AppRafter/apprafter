node_modules/
.design-bundle/
dist/
.astro/
.next/
.env
.env.local
.env.*.local
*.log
.DS_Store

# Bun 1.3+ uses text bun.lock (committed). Older binary bun.lockb is
# never produced; ignore the .bak just in case.
bun.lockb.bak
bun.lockb

# Next.js auto-regenerates this on every `next dev` / `next build` —
# never edited by hand, never useful to track.
cms/next-env.d.ts

# Payload generated types — regenerated on build, but we DO commit
# the file so landing/web can typecheck without running the cms.
# (so the file is intentionally NOT ignored — keep this comment to
#  remind future contributors)
# !cms/payload-types.ts
