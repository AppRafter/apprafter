# SPDX-License-Identifier: MIT
# Generated by AppRafter — see cli-providers::k8s::admission_webhook.
apiVersion: v1
kind: Namespace
metadata:
  name: apprafter-system
  labels:
    apprafter: "true"
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: admission-webhook-tls
  namespace: apprafter-system
  labels:
    apprafter: "true"
spec:
  secretName: admission-webhook-tls
  dnsNames:
    - admission-webhook.apprafter-system
    - admission-webhook.apprafter-system.svc
    - admission-webhook.apprafter-system.svc.cluster.local
  issuerRef:
    name: apprafter-selfsigned
    kind: ClusterIssuer
---
apiVersion: v1
kind: Service
metadata:
  name: admission-webhook
  namespace: apprafter-system
  labels:
    apprafter: "true"
spec:
  type: ClusterIP
  selector:
    app: admission-webhook
  ports:
    - name: https
      port: 443
      targetPort: 8443
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admission-webhook
  namespace: apprafter-system
  labels:
    apprafter: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admission-webhook
  template:
    metadata:
      labels:
        app: admission-webhook
        apprafter: "true"
    spec:
      containers:
        - name: admission-webhook
          image: ghcr.io/apprafter/admission-webhook:placeholder
          ports:
            - name: https
              containerPort: 8443
          env:
            - name: PORT
              value: "8443"
            - name: TLS_CERT_PATH
              value: /tls/tls.crt
            - name: TLS_KEY_PATH
              value: /tls/tls.key
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8443
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8443
              scheme: HTTPS
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: tls
              mountPath: /tls
              readOnly: true
      volumes:
        - name: tls
          secret:
            secretName: admission-webhook-tls
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: admission-webhook.apprafter.io
  labels:
    apprafter: "true"
  annotations:
    cert-manager.io/inject-ca-from: apprafter-system/admission-webhook-tls
webhooks:
  - name: applications.apprafter.io
    sideEffects: None
    failurePolicy: Fail
    admissionReviewVersions:
      - v1
    timeoutSeconds: 10
    clientConfig:
      service:
        name: admission-webhook
        namespace: apprafter-system
        path: /validate
        port: 443
    rules:
      - apiGroups:
          - apprafter.io
        apiVersions:
          - v1alpha1
        operations:
          - CREATE
          - UPDATE
        resources:
          - applications
        scope: Namespaced
