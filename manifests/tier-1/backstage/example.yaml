# SPDX-License-Identifier: MIT
# Generated by AppRafter — see cli-providers::k8s::backstage_manifests.
apiVersion: v1
kind: Namespace
metadata:
  name: backstage
  labels:
    apprafter: "true"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: backstage-config
  namespace: backstage
  labels:
    apprafter: "true"
data:
  app-config.yaml: |
    # Generated by AppRafter — see cli-providers::k8s::backstage_app_config.
    app:
      title: AppRafter Backstage
      baseUrl: https://backstage.example.com
    backend:
      baseUrl: https://backstage.example.com
      listen:
        host: 0.0.0.0
        port: 7007
      cors:
        origin: https://backstage.example.com
        methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
        credentials: true
      database:
        client: better-sqlite3
        connection: ":memory:"
    auth:
      environment: production
      providers:
        guest:
          dangerouslyAllowOutsideDevelopment: true
    catalog:
      rules:
        - allow: [Component, System, API, Resource, Location]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backstage
  namespace: backstage
  labels:
    apprafter: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backstage
  template:
    metadata:
      labels:
        app: backstage
        apprafter: "true"
    spec:
      containers:
        - name: backstage
          image: ghcr.io/apprafter/backstage:placeholder
          ports:
            - containerPort: 7007
          readinessProbe:
            httpGet:
              path: /healthcheck
              port: 7007
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthcheck
              port: 7007
            initialDelaySeconds: 30
            periodSeconds: 30
          volumeMounts:
            - name: app-config
              mountPath: /app/app-config.yaml
              subPath: app-config.yaml
              readOnly: true
      volumes:
        - name: app-config
          configMap:
            name: backstage-config
            items:
              - key: app-config.yaml
                path: app-config.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: backstage
  labels:
    apprafter: "true"
spec:
  type: ClusterIP
  selector:
    app: backstage
  ports:
    - name: http
      port: 80
      targetPort: 7007
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backstage
  namespace: backstage
  labels:
    apprafter: "true"
spec:
  parentRefs:
    - name: backstage-gateway
  hostnames:
    - backstage.example.com
  rules:
    - backendRefs:
        - name: backstage
          port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: backstage-gateway
  namespace: backstage
  labels:
    apprafter: "true"
spec:
  gatewayClassName: cilium
  listeners:
    - name: https
      protocol: HTTPS
      port: 443
      hostname: backstage.example.com
      tls:
        mode: Terminate
        certificateRefs:
          - name: backstage-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: backstage-tls
  namespace: backstage
  labels:
    apprafter: "true"
spec:
  secretName: backstage-tls
  issuerRef:
    name: apprafter-selfsigned
    kind: ClusterIssuer
  dnsNames:
    - backstage.example.com
